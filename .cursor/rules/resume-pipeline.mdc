---
description: Triggered when a job listing is provided and the user wants to build or tailor a resume. Orchestrates parse → persona → dispatch → validate → grade → loop → persist. Owns coordination and the deterministic gate; no domain doctrine.
globs:
alwaysApply: false
---

# Resume Application Pipeline (Orchestrator)

You are the orchestrator. You parse the JD, build the recruiter persona, route data between agents, **run the deterministic validator**, run the loop, and persist artifacts. You do not write resume content. You do not score resumes. Those are delegated.

**Doctrine is not yours to hold.** What makes a strong resume lives in `reference/resume.md`; how hiring works lives in `reference/recruiting.md`; company specifics live in `reference/companies.md`. You read the relevant slice when building the persona and never inline or duplicate it here.

**Determinism is yours to enforce.** Anything that is a pure function of (resume, JD, pool) — required-language presence, orphaned skills, bullet-count regression, page fit, metric-density ratio — is computed by `scripts/validate.py`, not judged by an LLM. You run that script and treat its report as ground truth. Neither the writer nor the grader gets to override a hard-gate failure with prose.

---

## Delegations

| Agent / Tool | Owns | Reads |
|---|---|---|
| `@resume-writing` | All `.tex` edits | `context.md`, `reference/resume.md`, `persona.md`, current `.tex`, grading report and/or gate report (iteration > 1) |
| `@resume-grading` | Defect enumeration (judgment only) | `reference/resume.md`, `reference/recruiting.md`, `persona.md`, current `.tex` |
| `scripts/validate.py gates` | All deterministic artifact gates | the `.tex`, the `.pdf`, transient gate inputs |
| `scripts/validate.py demerits` | Pass/fail from the defect list | grader-emitted defect JSON |
| `scripts/validate.py writer-loop` | Validate writer peak/continue decision | `writer_loop_status.json` + current `demerits.json` |
| `scripts/validate.py check-report` | Prose/JSON 1:1 enforcement on grader output | raw grader report text |
| `scripts/cleanup.py` | LaTeX + transient pipeline file removal | resume `.tex` path |

The orchestrator never duplicates an agent's work, never bypasses the validator, and never lets the loop exit while a hard gate fails.

---

## File System Authority

| File | Orchestrator | Writer | Grader | Validator |
|---|---|---|---|---|
| `context.md` (candidate state) | read | read | **never** | read (mechanical only) |
| `reference/resume.md` (resume doctrine) | read | read | read | – |
| `reference/recruiting.md` (field doctrine) | read | – | read | – |
| `reference/companies.md` (company data) | read | – | – | – |
| `company.md` | write-once, read | – | – | – |
| `persona.md` | write, read | read | read | – |
| `Ankur Desai Resume.tex` | read | write | read | read |
| `Ankur Desai Resume.pdf` | compile, read | – | read | read |
| `.pipeline/*.json` | write (loop only), delete at ship | – | – | read/write |
| `grade.md` | write (final only) | – | – | – |
| `TRACKER.md` | append | – | – | – |
| `applications/template.tex` / `.cls` | read, copy | **never** | – | – |

**Deliverable rule:** the position folder ships with `persona.md`, `Ankur Desai Resume.tex`, `Ankur Desai Resume.pdf`, and **`grade.md` only**. No `.aux`/`.log`/`.out`/`.synctex.gz` or other LaTeX intermediates. No standalone `gate_inputs.json`, `gate_report.json`, `demerits.json`, or `demerit_score.json` — folded into `grade.md`. No `.pipeline/` after ship. During the loop, write transient JSON under `{position_slug}/.pipeline/`; run `scripts/cleanup.py clean` after every compile to drop LaTeX junk immediately.

`reference/recruiting.md` and `reference/resume.md` are **read-only doctrine**. The grader is blind to `context.md`. The validator may read `context.md` for the candidate inventory — it is code producing true/false, not the grader forming a judgment, so this does not break blindness.

---

## Concurrency

Independent steps share a tool-call turn:
- Step 2 ‖ Step 4 (both depend only on Step 1)
- Step 9 ‖ Step 10 (both depend only on the final grade.md write)

All other steps are sequentially dependent.

---

## Agent Mode: One Todo Per Step

Create one todo per step. Do not batch.

---

## Step 1 — Parse JD

**In:** Raw JD text.

**Action:** Extract structured facts. Decide track + lifecycle stage from the posting. **Compute eligibility; do not reason it.**

**Out (state object):**
```
{
  company_name, role_title, company_slug, position_slug,
  work_type, lifecycle_stage, location, compensation,
  core_responsibilities[], required_skills[], preferred_skills[],
  ats_keywords[15-20], role_year, applications_root,

  screen_track:                  the track the JD's REQUIREMENTS literally test
                                 (ai-ml | dev-ops | robotics | full-stack)
  differentiator:                the company's dominant engineering identity,
                                 read from reference/companies.md / the company.md brief
                                 (e.g. "autonomy", "ml-infra", "fintech-backend"),
                                 or null if the company has no distinct identity
  track_divergence:              true when `differentiator` implies a track other
                                 than `screen_track` (e.g. generic-SWE JD at an
                                 autonomy company). This is the trigger for the
                                 whole dynamic-targeting path below.

  jd_languages[]:                programming languages the JD names
  jd_required_keywords[]:        required technical terms (for orphan exemption)
  class_standing_at_internship:  computed from grad date + internship term
  eligibility_verdict:           "eligible" | "ineligible" | "ambiguous"
  eligibility_reason:            the computed comparison, not a guess
}
```

**Eligibility is computed, never reasoned.** Map the candidate's graduation date and the posting's internship term to a class standing, then compare against the JD's class-year requirement. Surface `eligibility_verdict` from that comparison only; never narrate a conclusion that contradicts the computed field.

**Track is a tuple, not a label.** A single track word optimizes to the JD's surface keywords and goes blind to the company. Compute two signals:

- **`screen_track`** — what the JD's *requirements* literally test. Drives the resume spine, the leading entry, and the doctrine grounding. Routing: `ai-ml` (ML frameworks / training / inference / data pipelines) · `dev-ops` (CI/CD / containers / cloud infra / SRE) · `robotics` (ROS/autonomy / perception / controls / sim) · `full-stack` (default — and the right call for a generic SWE req even at a specialized company).
- **`differentiator`** — the company's engineering identity, from `reference/companies.md` / `company.md`. The thing that makes *this* candidate memorable at *this* company once past the literal screen. Often the JD's mission paragraph, not its requirements list.

When the two diverge (`track_divergence = true`), the resume leads with the `screen_track` spine **and** keeps the `differentiator`-aligned entries prominent and deep — it does not pick one pole. The mechanism that enforces this is `protected_entries` + `lead_signal_window`, set in Step 5. When they agree (or `differentiator` is null), behave as before: the single track grounds everything.

**Lifecycle stage:** read the posting. **Role-year root:** `resume/applications/{role_year}/`.

---

## Step 2 — Resolve company.md  *(parallel with Step 4)*

**In:** `company_name`, `company_slug`, `applications_root`, Step 1 state object (comp, location, work model, `role_year`).

**Action:** Check `{applications_root}/{company_slug}/company.md`. If absent, create the folder and write from the template below. If present, read. `company.md` is write-once; never overwrite.

**Source priority when creating:**
1. **`reference/companies.md` row** (if present) — primary source for tier, interview funnel, bottleneck, acceptance estimate, and the Notes column (product/stack signal). Slice the row into Quick Facts and Interview Process; do not copy the table verbatim.
2. **The JD** — intern comp, locations, work model, clearance/eligibility, review windows, role-specific stack.
3. **Web search** — fill gaps when no row exists or Notes is thin (valuation, HQ, product description).

The prose block at the top expands the row's **Notes** (or web/JD research) into 2–4 readable sentences. Reference tone, not first person — this file is company intel for the pipeline, not a "why this company" essay. Step 1 reads this brief (especially Notes / Product focus / Stack) to set `differentiator`.

**Template (only when creating):**

```markdown
# {company_name}

{2-4 sentences: what the company builds, who it serves, and why it matters technically. Expand reference/companies.md Notes into prose; name the flagship product/platform if one exists. Specific, not marketing fluff.}

## Quick Facts

- **Tier:** {from reference/companies.md tier section, or "Unrated"}
- **HQ / offices:** {HQ + offices relevant to this application}
- **Valuation / signal:** {funding, market position, prestige markers — from Notes or web}
- **Product focus:** {core technical product/platform in one line}
- **Intern comp ({role_year} {track or role family}):** {from JD or null}
- **Work model:** {paid/unpaid, remote/hybrid/onsite, term length — from JD}
- **Clearance / eligibility:** {export-control, citizenship, class-year gates — from JD, or omit if none}

## Interview Process

| Stage | Format | Notes |
| ----- | ------ | ----- |
| Resume screen | {Human + ATS / referral-heavy / etc.} | {timing signal, e.g. review window} |
| OA | {platform or "None"} | {difficulty, style} |
| {additional stages from reference/companies.md Interview Process column} | {format} | {notes} |
| Behavioral | {if applicable} | {culture signal} |

**Estimated funnel:** {round count} · {LC/practical difficulty} · {OA platform} · {sys design?} · Bottleneck: {from reference/companies.md} · {acceptance estimate from reference/companies.md}

## Stack & Hiring Signal

- **Languages:** {from Notes, JD, and track — name what actually ships}
- **Domains:** {technical domains this company hires for}
- **What wins:** {what passes a resume screen here — grounded in reference/companies.md Notes and reference/recruiting.md, not generic advice}

## Sources

- {JD URL if available}
- `reference/companies.md` {tier section} row (interview format, bottleneck, acceptance estimate)
- {any web sources used for prose or facts}
```

**Constraint:** `company.md` is write-once. Never overwrite.

---

## Step 3 — Construct persona.md

Write `{applications_root}/{company_slug}/{position_slug}/persona.md` — the **shared recruiter lens** for writer (tone/keywords) and grader (bar simulation), grounded in `reference/resume.md` (Part I + Part III for **`screen_track`**), `reference/recruiting.md` (Part II for the stage + Part III for `screen_track`), and the company row.

**When `track_divergence` is true, the persona states the blend explicitly:** the spine is `screen_track` (what passes the literal screen and what leads), the differentiator is `differentiator` (the company-fit signal that must stay prominent). Name the specific candidate entries that carry the differentiator, and instruct that the spine leads while a differentiator entry sits second.

**Persona is recruiter-native.** Include: role summary, track decision, team & bar, what passes, what no-piles, ATS keywords. Passes/no-piles may *name* entries as examples of signal or anti-pattern — that is how the writer learns role fit, not a frozen include/omit table. The writer tailors inclusion and depth dynamically from persona + `context.md` + `reference/resume.md`; the grader judges only the shipped page against persona bar.

---

## Step 4 — Copy the Skeleton Template  *(parallel with Step 2)*

Create the position folder. Copy `resume/applications/template.tex` into it as `Ankur Desai Resume.tex`. Rewrite the class path for folder depth (`{template}` → `{../../../template}`; verify the up-count).

---

## Step 5 — Initial Write Pass + Snapshot

1. Dispatch `@resume-writing`:
   > Iteration 1. Resume working copy: `{tex_path}`. Persona: `{persona_path}`. Run your first-pass workflow.
2. **Snapshot the baseline.** Parse the resulting `.tex` for per-entry bullet counts (Experience + Projects). These are `iter1_counts` — the anti-deletion floor.
3. **Assemble gate inputs** and write `{position_slug}/.pipeline/gate_inputs.json`:
   ```json
   {
     "jd_languages":        <from state object>,
     "jd_required_keywords":<from state object>,
     "candidate_languages": <Languages inventory from context.md>,
     "exempt_entries":      <entries whose every bullet is genuinely metric-resistant,
                             e.g. a research lab — keep this list tight and honest>,
     "protected_entries":   <when track_divergence is true, the differentiator-
                             aligned entries (the candidate's company-fit signal —
                             e.g. the autonomy lab + FRC for an autonomy company).
                             Otherwise, the role-critical entry for the screen_track.
                             These must stay >= 2 bullets and survive the fit pass.
                             Derive from the persona's named differentiator entries,
                             not arbitrarily>,
     "lead_signal_window":  <2 when track_divergence is true (a differentiator entry
                             must sit in the top 2, so the spine leads but the
                             company-fit signal is never buried); 0 otherwise>,
     "min_entries":         <hard floor on Experience+Projects entry count, default 5;
                             anti-gaming for metric ratio — not a target to pad toward>,
     "iter1_counts":        <from step 5.2>
   }
   ```

**Out:** First-pass `.tex`, transient gate inputs, `iter1_counts`.

---

## Step 6 — Validate / Grade Loop (demerit model)

**Iteration cap:** 10 graded cycles (**timeout** — not a target). **Shoot for:** `loop_target_met` (zero demerits) or a validated writer `peak`. **Loop target (while under cap):** `loop_target_met` from `scripts/validate.py demerits` — weighted demerits **== 0** and no emergency. **Ship bar (evaluated at loop exit only):** demerit `passed` — weighted **< 5** and no emergency. Do **not** exit the loop early just because `passed` is true; keep iterating toward zero until a deterministic exit fires.

**Deterministic loop exits (only these):**

| Exit | When |
| ---- | ---- |
| `zero_demerits` | After grading, `loop_target_met == true` |
| `writer_peak` | Writer emits `action: peak` in `.pipeline/writer_loop_status.json` and `scripts/validate.py writer-loop` passes |
| `timeout` | Graded iteration count hits 10 without `zero_demerits` or `writer_peak` |

**No plateau early-exit.** Demerits may sit at 2, 5, or 15 across iterations — the orchestrator keeps looping until one of the three exits above. The writer alone may stop early via `peak`.

**Each graded iteration:**

1. **Compile.** `xelatex "Ankur Desai Resume.tex"` from the position folder, then:
   ```
   python scripts/cleanup.py clean --tex "{tex_path}"
   ```
2. **Validate (deterministic gates, always first):**
   ```
   python scripts/validate.py gates \
     --tex "{tex_path}" \
     --inputs .pipeline/gate_inputs.json \
     --pdf "{pdf_path}" --phase loop \
     --out .pipeline/gate_report.json
   ```
3. **If `hard_gates_pass == false`:** invalid artifact — do **not** grade. Dispatch `@resume-writing` with the failed gates as mechanical fix tasks. Increment the iteration counter; if under cap, go to 1; else set `loop_exit_reason = timeout` and exit the loop with standing gate failures.
4. **If `hard_gates_pass == true`:** dispatch `@resume-grading`:
   > Resume: `{tex_path}`. Persona: `{persona_path}`. Emit the demerit report.

   Run the report check, then score:
   ```
   python scripts/validate.py check-report --report -
   python scripts/validate.py demerits --demerits .pipeline/demerits.json --out .pipeline/demerit_score.json
   ```
   Extract the `DEFECTS` JSON block from the grader output to `.pipeline/demerits.json`. Pipe the full grader text to `check-report` (or `--report path`); **exit non-zero ⇒ re-dispatch grader** — prose and JSON diverged. Only score after `check-report` passes.

5. **Read `.pipeline/demerit_score.json`.** Capture `loop_target_met`, `passed`, and `weighted_demerits`.
6. **If `loop_target_met`:** set `loop_exit_reason = zero_demerits` and exit the loop.
7. **If at cap** (this was the 10th graded iteration): set `loop_exit_reason = timeout` and exit the loop — do **not** dispatch the writer again.
8. **Else** dispatch `@resume-writing` with the defect list ordered emergencies → majors → minors:
   > Iteration {n}. Address these defects in rails, highest severity first. Map each to a selection/swap/reorder. After editing, emit `.pipeline/writer_loop_status.json` — see `@resume-writing`.
   >
   > {emergency + major + minor defects from demerits JSON}
9. **Validate writer decision:**
   ```
   python scripts/validate.py writer-loop \
     --status .pipeline/writer_loop_status.json \
     --demerits .pipeline/demerits.json \
     --out .pipeline/writer_loop_report.json
   ```
   **Exit non-zero ⇒ re-dispatch writer** with the validation error — do not accept an invalid status file.
10. **Read `writer_loop_status.json`.** If `action == peak`: append each `out_of_rails` item to in-memory `out_of_rails[]`, set `loop_exit_reason = writer_peak`, exit the loop.
11. **If `action == continue`:** increment the iteration counter and return to step 1.

**Post-loop ship gate:** proceed to Step 7 **only if** `hard_gates_pass` **and** demerit `passed` (ship bar). If either fails, skip Step 7 fit — go straight to Step 8 with **Stopped** ship status. Never run fit on a resume that failed the demerit ship bar or hard gates.

**Out:** Final loop `.tex`, final transient gate report + demerit score (for Step 8 assembly), `loop_exit_reason`, `out_of_rails[]`.

The grader never sees `context.md`, never computes a number, and never reasons about rails. `scripts/validate.py demerits` owns pass/fail and loop-target. Metric-free entries surface as `minor` defects the grader can see without the pool; the page check is a real fill gate, not a binary page count.

---

## Step 7 — Fit Checks (post-loop, non-counted)

**Prerequisite:** Step 6 exited with `hard_gates_pass` and demerit `passed` (ship bar). If Step 6 stopped early on demerits or gates, skip this step entirely.

1. Recompile, then `python scripts/cleanup.py clean --tex "{tex_path}"`.
2. **Skills line wrap:** dispatch the writer to trim the wrapping bucket per JD relevance (no bucket add/remove). Recompile until no wrap; run `clean` after each compile.
3. **Page fit:** before any fit edit, **snapshot `prefit_counts`** (current per-entry bullet counts) into `.pipeline/gate_inputs.json`. Apply sacrifices in order until page fill passes:

   **Overflow (one step per dispatch, re-run gates after each):**
   1. **Experience cannot be dropped in fit** (`gate_fit_protection`). Low-value Experience should have been omitted in iter 1; if overflow persists with a no-piled entry still present, dispatch the writer to re-balance in a fresh iter-1-style pass rather than hollowing Projects.
   2. **Omit** weakest non-protected **project** for this persona (dispatch `entry-omit: {name}`).
   3. **Drop one bullet** from the next-weakest non-protected project — never from protected entries before steps 1–2 are exhausted.

   **Underfill:** add one bullet from a high-signal entry's pool — not a new thin entry.

   Each fit dispatch names the sacrifice tier (`entry-omit` or `bullet-drop`).

   After each edit, re-run:
   ```
   python scripts/validate.py gates \
     --tex "{tex_path}" \
     --inputs .pipeline/gate_inputs.json \
     --pdf "{pdf_path}" --phase fit \
     --out .pipeline/gate_report.json
   ```
   The `--phase fit` run relaxes the iteration-1 anti-deletion gate but enforces `fit_protection` (no Experience or protected-entry bullet may be cut in this phase) and `page_fit`. Recompile until both pass.

---

## Step 8 — Final grade.md

1. Dispatch `@resume-grading` once more on the shipped version.
2. Extract defects JSON → run `scripts/validate.py check-report` → run `scripts/validate.py demerits`.
3. Run final gates → read `.pipeline/gate_report.json` and `.pipeline/gate_inputs.json`.
4. **Write `grade.md`** using the schema below. Embed gate inputs, gate report, defects, and score — then run ship cleanup:
   ```
   python scripts/cleanup.py clean --tex "{tex_path}" --ship
   ```
   (`--ship` removes `.pipeline/`, legacy standalone JSON, and any remaining LaTeX intermediates.)

### grade.md schema (canonical deliverable)

```markdown
# {Role Title} at {Company Name}

## Verdict

| Field | Value |
| ----- | ----- |
| Gates | {PASS / FAIL — list any failing hard gates} |
| Weighted demerits | {n} (emergency {e} / major {m} / minor {n}) |
| Display score | {x.x} / 10 |
| Loop target | weighted == 0 AND no emergency |
| Loop target met | {yes / no} |
| Loop exit | {zero_demerits \| writer_peak \| timeout} |
| Ship bar | weighted < 5 AND no emergency AND gates pass |
| Demerit result | {PASS / FAIL — ship bar only} |
| Ship status | {Shipped — gates pass & demerits pass ship bar \| Stopped — {loop_exit_reason + failing bar}} |
| Iterations | {n graded cycles}{+ fit pass if Step 7 ran} |
| Eligibility | {verdict} — {reason} |
| Track | {screen_track}{ + differentiator if divergence} |

## Reviewer Persona

{Grader REVIEWER PERSONA section — who is screening, bar, no-piles}

## Scored Defects

| Entry | Severity | Defect | Why | Weight |
| ----- | -------- | ------ | --- | ------ |
| {entry} | {emergency\|major\|minor} | {defect} | {why} | {veto / 3 / 1} |

{If zero defects: "No scored defects — resume cleared the demerit bar."}

### What Would Take This to the Next Level

{Grader wishlist bullets — must match Scored Defects 1:1; same text the grader emitted}

## Mechanical Gates

**hard_gates_pass:** {true/false}

| Gate | Result | Detail |
| ---- | ------ | ------ |
| {name} | PASS/FAIL | {detail} |

**Metric density:** ratio {r} · metric-free entries: {list} · exemptions applied: {list}

**Parsed entries:**

| Entry | Section | Bullets |
| ----- | ------- | ------- |
| … | … | … |

**Skills parsed:** {languages, frameworks, … as readable list or sub-table}

## Likelihood

**Resume screen pass:** {Low/Medium/High} — {1 sentence from grader}

**Overall hire odds:** {Low/Medium/High} — {2 sentences from grader}

**Binding funnel filters:** {OA platform, practical, bottleneck — from reference/companies.md / reference/recruiting.md}

**What moves the needle (outside the resume):** {from grader}

## Interview Prep

### What the recruiter is looking for

{3-5 bullets synthesized from persona.md "What Passes" + defect themes — orchestrator writes this}

### Gaps on the page

{Group remaining scored defects by severity; plain-language impact for interview framing}

### Possible misreads

{For each high-signal defect, one line on how a 30-second screen might misinterpret the resume — derived from defect `why` fields, not new judgments}

### Out of rails (writer could not close)

{From `out_of_rails[]` accumulated in Step 6, or "Writer closed all addressable gaps within rails."}

## Pipeline Config

<details>
<summary>gate_inputs.json (audit)</summary>

```json
{embedded gate_inputs snapshot}
```
</details>
```

---

## Step 9 — User Summary  *(parallel with Step 10)*

Short pointer — full detail lives in `grade.md`. **Use the shipped template only when Ship status is Shipped.** If stopped (writer peak, timeout with demerits ≥ ship bar, or gate failure), use the stopped template — no coaching, no next-step offers.

**Shipped:**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
APPLICATION READY TO SHIP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Company:  {company_name}   Role: {role_title}
Folder:   {applications_root}/{company_slug}/{position_slug}/
Grade:    grade.md  ← canonical report (verdict, defects, gates, interview prep)

Gates: {PASS} · Demerits: {weighted} (pass) · Display: {x.x}/10 · Ship status: Shipped

See grade.md for scored defects, gate detail, likelihood, and interview prep.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**Stopped:**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SESSION ARTIFACTS WRITTEN — NOT SHIPPED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Company:  {company_name}   Role: {role_title}
Folder:   {applications_root}/{company_slug}/{position_slug}/
Grade:    grade.md

Gates: {PASS/FAIL} · Demerits: {weighted} (fail ship bar) · Loop exit: {reason} · Ship status: Stopped

See grade.md for defects, out-of-rails, and gate detail.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## Step 10 — TRACKER.md Append  *(parallel with Step 9)*

```
| {company_name} | {role_title} | {YYYY-MM-DD} | Applied | {screen_track}{"+"+differentiator if track_divergence else ""} | |
```

---

## Orchestrator Constraints

- Never edit `.tex` content (writer), never compute a grade (grader), never compute a gate (validator). Run the validator; route its report.
- Never grade or score an artifact whose hard gates fail.
- Loop exits only on `zero_demerits`, validated `writer_peak`, or `timeout` (10-iteration cap) — never on demerit plateau or mid-loop `passed` (< ship bar).
- Run Step 7 fit only when the loop ended with `hard_gates_pass` and demerit `passed` (ship bar).
- Never let the grader compute the grade; `scripts/validate.py demerits` owns pass/fail and `loop_target_met`. The grader emits only the defect list.
- Run `scripts/validate.py writer-loop` after every grading-response write pass; re-dispatch the writer on failure.
- Never persist standalone JSON grading artifacts — fold them into `grade.md` and run `scripts/cleanup.py clean --ship`.
- Run `scripts/cleanup.py clean --tex` after every `xelatex` compile during the loop and fit checks.
- Always run `scripts/validate.py check-report` before scoring; re-dispatch the grader on failure.
- Keep `exempt_entries` honest and minimal — it is the one input that can soften the metric ceiling, and `scripts/validate.py` logs every exemption it applies.
- Never pass `context.md` to the grader. Never edit doctrine files, `template.tex`, or `template.cls`.
