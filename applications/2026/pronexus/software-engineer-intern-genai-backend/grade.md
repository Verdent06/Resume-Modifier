# Gen AI/Backend Software Engineer Intern (Python) at ProNexus

## Verdict

- **Score:** 9.0 / 10 (1 weighted demerit — 0 emergency, 0 major, 1 minor)
- **Eligibility:** Eligible — current student (`Expected May 2028`), no visa sponsorship needed, Remote USA matches posting geography.
- **Track:** full-stack + agentic-ai
- **Pipeline:** 3 cycle(s) · exit: writer_peak

## Screen Review

### First read

- Strong agentic-backend fit: Vylet leads with LangSmith eval + Pydantic consensus gates (50%→90% faithfulness), a Dockerized LangGraph/Celery pipeline (30x speedup), asyncpg Postgres DAL with injection-safe SQL, and live $1,500 MRR — directly mirrors ProNexus's extract/classify/enrich agent workflows and sourcing domain.
- Full-stack spine is credible: SignalWeaver ships FastAPI async REST (9.1s p50), React/TypeScript dashboard, pytest/GitHub Actions CI, and Postgres persistence; MDC delivers a sole-engineer Flask REST API on EC2 plus an ETL eliminating ~800 hours across 400 PACs.
- Binding ding: Lyndbrook is a single enrichment/sourcing bullet — useful domain adjacency but thin on a fast scan; second pool bullet cannot fit one page without overflow.

### Demerits

- **minor** · `Lyndbrook Capital` · single thin bullet — one sourcing line without a second engineering decision reads thinner than the contract likely was.

### Misreads

- A rushed reader may under-weight enrichment/sourcing depth because Lyndbrook's only line is entity aggregation without a scoring or integration follow-on visible on the page.

### Interview angles

- **Lead with:** Vylet's LangSmith eval harness, Pydantic consensus gates, LangGraph/Celery background pipeline, and asyncpg Postgres layer; SignalWeaver FastAPI + pytest CI + React/TS client; MDC production Flask API and metric-heavy ETL.
- **Defend:** Lyndbrook depth → enrichment/sourcing adjacency to expert intelligence workflows; second bullet (Review Velocity scoring, 800→280 shortlist) exists but page budget forced single-bullet presentation *(out of rails: second Lyndbrook bullet graded two-page overflow in iter-2)*.
- **Depth prep:** FastAPI endpoint design, Postgres schema/query patterns, prompt + JSON-schema iteration, lightweight eval loops, and background job architecture (Celery/Redis); skim Supabase docs as Postgres-adjacent surface.

## Likelihood

- **Resume screen:** High — agentic GenAI backend + production Python APIs + Postgres + sourcing/enrichment adjacency is exactly what a two-founder expert-sourcing startup human screen rewards.
- **Overall hire odds:** Medium — human-read startup funnel favors this ship story; founder technical conversation is the binding filter.
- **Funnel filters:** Likely human/founder resume screen → practical backend/agent conversation or take-home; no standard OA; remote USA, meaningful hours expected.
- **Outside the resume:** Founder outreach (Tom Myers / Erica Dippold, Antler network); live Vylet eval + FastAPI demo; prep prompt/schema iteration and Postgres depth for the technical conversation (`recruiting.md` §5 startups).
