# AI Full Stack Engineer Intern at Gesture

## Verdict

- **Score:** 1.0 / 10 (9 weighted demerits — 0 emergency, 2 major, 3 minor)
- **Eligibility:** Eligible — `Expected May 2028` is a current CS student, which satisfies the JD's "currently pursuing CS, Software Engineering, or related." Onsite Denver is a location commitment, not an eligibility gate.
- **Track:** full-stack + agentic-ai
- **Pipeline:** 4 cycle(s) · exit: writer_peak

## Screen Review

### First read

- Strong agentic-AI + startup fit: Vylet leads with a Dockerized LangGraph pipeline, a LangSmith eval/consensus harness (50%→90% faithfulness), a cross-stack debugging fix (79%→89%), and a live $1,500-MRR product with paying clients — exactly the agentic depth and ship velocity Gesture's identity rewards.
- Full-stack spine is present: sole-engineer Flask REST API on AWS EC2 (MDC), and a React/TypeScript + FastAPI REST + Docker/GitHub Actions app (SignalWeaver); Python and TypeScript are demonstrated in bullets, not just listed.
- Binding ding: the JD's Fern stack (Firebase, Express, Node.js) and GCP are absent everywhere, and the React proof sits in Projects rather than in the Experience fold — a real stack-alignment gap on a ~30-second scan.

### Demerits

- **major** · `resume` · React client buried — only SignalWeaver shows React/TypeScript dashboard work and it sits under three Experience entries; the first-read front-end signal is agent/backend-heavy Vylet.
- **major** · `Technical Skills` · Fern-stack floor absent — the JD calls out Node.js, Express, Firebase, and GCP; none appear in Skills or any bullet — only Python/FastAPI/Flask/AWS on the page.
- **minor** · `Michigan Data Consulting (MDC)` · redundant second bullet — repeats sole-engineer ownership and stakeholder scoping without a new metric or technical decision.
- **minor** · `Technical Skills` · LangChain missing — the JD pairs LangChain with LangGraph; Skills and bullets show LangGraph/LangSmith only.
- **minor** · `CaseStudyPrep.AI` · Angular not React — Experience-section front-end work is Angular/RxJS while the JD centers React; React proof is relegated to Projects.

### Misreads

- A rushed reader may bucket the page as an "agentic-backend / pipeline founder" rather than an end-to-end full-stack builder, because the lead entry is LangGraph/eval/debugging and the React surface lives lower in Projects.
- The Fern/GCP absence can read as "wrong stack" on a keyword scan even though the underlying full-stack competencies (REST APIs, a real client, cloud/Docker/CI, polyglot backends) are all demonstrated — the transferable skills are there, the exact keywords are not.

### Interview angles

- **Lead with:** Vylet's agentic LangGraph pipeline, LangSmith eval/consensus hardening (50%→90%), the diagnosed 79%→89% cross-stack defect fix, and paying-customer traction; SignalWeaver's React/TypeScript + FastAPI REST + Docker/CI full-stack build; MDC's sole-engineer Flask REST API shipped on AWS EC2; CaseStudyPrep's production voice-AI and real-time front-end (60 FPS, sub-5ms main thread).
- **Defend:** Fern stack / GCP → transferable REST/backend + React/cloud/Docker depth; eager to pick up Firebase/Express/Node/GCP fast on Gesture's stack *(out of rails: none in pool or swap sets — genuine gap)*; React-in-Experience → React shipped in SignalWeaver, Angular/RxJS at CaseStudyPrep is the same client-side muscle *(out of rails: no React Experience-section bullet in pool)*; LangChain → LangGraph/LangSmith orchestration is directly adjacent *(out of rails: no LangChain in inventory)*; MDC redundant bullet → the only metric-bearing MDC line (ETL, ~800 hrs/400 PACs) could not fit one page without cutting Vylet's higher-value eval bullet *(out of rails: page-budget trade graded net-negative)*.
- **Depth prep:** agentic-workflow orchestration and eval/hardening (LangGraph nodes, consensus gates, LangSmith); full-stack trade-offs (why REST, why this DB, client/state decisions in React); debugging across UI→DB; and a quick pre-read of Firebase/Express/Node.js on GCP so the Fern-stack conversation has concrete footing.

## Likelihood

- **Resume screen:** High — a live agentic product with paying users plus production LLM/voice-AI and full-stack (FastAPI/React/Docker/AWS) evidence is what an early-stage startup human screen rewards; the Fern-stack keyword gaps are secondary to the shipping story at the top.
- **Overall hire odds:** Medium — clearing the PDF is plausible at a human-read-first startup funnel, but the onsite loop stress-tests end-to-end full-stack builds and agentic depth live, where missing Node/Express/GCP/Firebase proof and React-in-Projects can surface under trade-off questioning.
- **Funnel filters:** No standard big-tech OA (small startup); likely human/founder resume screen → practical technical conversation or take-home → onsite founder loop at the Denver office; onsite Denver commitment and low ($600–800/mo) stipend are self-selection filters.
- **Outside the resume:** Warm referral or founder-direct outreach (LinkedIn/X) to skip the cold pile (`recruiting.md` §4–§5 startups); apply early; prep a live Vylet/SignalWeaver walkthrough with stack trade-offs and a fast Fern/GCP ramp narrative for the practical build/debug conversation (`recruiting.md` §11).
