# Software Engineering Intern, BS, Summer 2027 at Google

## Verdict

- **Score:** 8.0 / 10 (2 demerits — 0 emergency, 0 major, 2 minor)
- **Eligibility:** eligible — Expected May 2028 vs Summer 2027 internship is junior/penultimate year; returns to the Bachelor's program after; US citizen; BS req not MS
- **Track:** full-stack + large-scale / distributed systems / Google-product engineering
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- MDC leads with a shipped Python ETL + Flask REST API on EC2 (~800 hours saved across 400 PACs) — production SWE a host can route.
- Vylet sits second: Dockerized Redis/Celery workers, Gemini embeddings, 30x pipeline — the distributed-systems differentiator, still wearing a PE/founder title.
- Binding ding: Granular C++ never sizes the systems win, and Vylet's founder/PE tagline can still bucket agentic GTM at host match.

### Demerits

- **minor** · `Vylet` · off-axis PE/agentic framing — Docker/Redis/Celery and Gemini embeddings are real distributed work, but the Founder / PE/search-fund title still invites an agentic-GTM bucket
- **minor** · `Granular Synthesizer Plugin` · metric-free — Zero-alloc MemoryPool and lock-free SPSC FIFO are the C++/parallel signal, but neither bullet sizes latency, xruns, or CPU

### Misreads

- Vylet's founder/PE tagline can park the candidate as an agentic-GTM intern even though Redis/Celery workers and Gemini embeddings are the host-match signal.
- Granular without a number can read as hobby DSP rather than C++ memory/concurrency next to Google-scale systems.

### Interview angles

- **Lead with:** MDC sole-owned Flask REST + Requests/Pandas ETL (shipped API); Vylet Docker/Redis/Celery workers and Gemini embeddings DAL (distributed + Google-product adjacency); Granular C++ MemoryPool / lock-free SPSC (parallel/systems); CaseStudyPrep ONNX VAD 40% cost cut plus 27% S3 recovery; SignalWeaver React/TypeScript + 49ms pgvector search
- **Defend:** Vylet PE/LangGraph title *(out of rails: header is canonical pool copy; remaining bullets are more GTM/agentic, not less)* — pivot to Redis/Celery workers and Gemini freshness, not the search-fund product story. Granular has no xrun/latency/CPU metric *(out of rails: pool has no verbatim impact-metric bullet; swap sets cannot invent one)* — script the audio-thread constraints and what you would measure. Java, Go, C#, Android, Chrome internals, and Google-internal infra are not in the inventory — do not add them.
- **Depth prep:** Google Docs Easy–Med DS&A with no IDE (`companies.md`: 2–3 rds, bottleneck host match); lock-free atomics and C++ hot-path rules; one STAR ship/debug story (CaseStudyPrep S3 URL recovery or Vylet SQL freshness); a 30-second host-match pitch (distributed pipeline + C++ systems, not PE lead-gen)

## Likelihood

- **Resume screen:** High — Production Flask REST on EC2 leads; Python, C++, and TypeScript appear through use; Expected May 2028 is visible; one page. Two minors do not sink a referral-backed intern screen.
- **Overall hire odds:** Medium — 2–3 Easy–Med Google Docs rounds then host match (~4–5%). The page clears generic SWE; conversion is Docs DS&A plus whether a host hears distributed/C++ rather than PE lead-gen or hobby DSP.
- **Funnel filters:** Resume screen (referral-heavy) → no OA in `companies.md` (some 2026 US intern reports mention an OA) → 2–3 Google Docs coding rounds, no intern sys design → host match bottleneck · ~4–5%; US 12–14 weeks; returning-to-school gate
- **Outside the resume:** This packet is a referral email to a Googler, not a Careers click. Drill Google Docs mediums; rehearse the synth’s memory/concurrency trade-offs and the Vylet worker/Gemini story; intern behavioral is a filter round (`recruiting.md`)
