# Software Engineer, Intern (Summer 2027) at Sentry

## Verdict

- **Score:** 7.0 / 10 (3 demerits — 0 emergency, 0 major, 3 minor)
- **Eligibility:** eligible — `Expected May 2028` (rising senior returning to school after Summer 2027); enrolled CS undergrad; prior internships on page; US work-authorized without sponsorship; able to relocate to San Francisco (housing/relocation covered)
- **Track:** full-stack (screen) + open-source developer-tooling / Python+TS full-stack (differentiator; no track divergence)
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- Leads with the employed `CaseStudyPrep.AI` Software Engineer Co-op (voice-AI production wins: 40% inference-cost cut, sub-27% upload-failure fix in RxJS/TypeScript) — an employed-team engineering first impression, not "solo founder," and it demonstrates JS/TS in shipped work.
- Full-stack spine is on-axis and complete: `SignalWeaver` hits every layer a Sentry screen tests — FastAPI async REST backend, React/TypeScript dashboard, Postgres, Docker Compose, and a GitHub Actions CI pipeline — with two clickable public repos; Python demonstrated in bullets across Vylet/MDC/SignalWeaver.
- AI-native signal reads without buzzwording: Vylet's Dockerized LangGraph pipeline (30x speedup) and LangSmith eval (50→90% faithfulness) show AI tooling wired into shipped delivery, matching Sentry's "build in the AI-native middle" framing.

### Demerits

- **minor** · `Granular Synthesizer Plugin` · off-axis project slot — deep C++/JUCE audio-thread systems work shows zero Python/TS or full-stack signal for a Python/Django + React/TS shop; the #2 project slot spends depth on real-time audio rather than reinforcing the dev-tooling spine.
- **minor** · `resume` · no external open-source contribution — every linked GitHub repo is a self-authored build; at an open-source-first employer that contributes to public OSS daily, there is no visible PR/contribution into an external project to prove that fit.
- **minor** · `SignalWeaver` · React only in a personal project — the React/TypeScript frontend evidence lives solely in a project; the titled (paid) role's frontend is Angular/RxJS, so React depth for a React/TS shop is unproven in shipped-to-users work.

### Misreads

- A rushed skim could bucket Granular as an "audio hobby project" and miss that it is on the page as polyglot craft ("picks up a new language and runs") + a live public repo, reading the candidate as narrower on the web/dev-tooling axis than the rest of the page shows.
- All-personal repos can be misread as "hasn't worked in a shared codebase / upstream" even though the CI-on-main and Co-op signals are present — a warm referral or a real upstream PR closes this instantly.

### Interview angles

- **Lead with:** `CaseStudyPrep.AI` production voice-AI wins (ONNX/Silero cost cut, RxJS fault-tolerance) as the employed-team shipping story; `SignalWeaver` as the end-to-end full-stack build you own spec-to-ship (FastAPI + React/TS + Postgres + Docker + GitHub Actions CI) — narrate it the way Sentry's "own a project end-to-end" JD frames the role.
- **Defend:** the solo-heavy footprint — Founder/sole-engineer/personal-repos — *(out of rails: pool has no external-OSS-contribution bullet and no titled-role React bullet; Granular→Lyndbrook swap judged net-worse and does not add team signal)*. Answer honestly: co-op collaboration, stakeholder-scoped delivery at MDC, and git/PR-on-main CI workflow; state that you're ready to work inside a shared codebase and would open a real PR to Sentry's public repos.
- **Depth prep:** DS&A / practical live coding (Sentry is "No HackerRank" — expect real-code problem solving, not gotcha algo); full-stack trade-offs you can defend on `SignalWeaver` (why FastAPI, why Postgres, why the CI stages); AI-tooling workflow narration (LangGraph/LangSmith eval design); Python idioms and one React deep-dive to counter the "React only in a project" gap.

## Likelihood

- **Resume screen:** High — every binding screen signal is present and on-axis: three production experiences with metrics, Python demonstrated in bullets, a full-stack app hitting all layers plus CI, clickable public repos, AI-native pipeline work, a prior internship title, and `Expected May 2028` clearing eligibility. Sentry reads the PDF early (Ashby) with no OA gate, so this page carries unusual weight.
- **Overall hire odds:** Medium — because there is no HackerRank gate and the loop rewards defensible shipped work the candidate can narrate, this converts to a screen more reliably than an OA-gated big-tech pipeline; but Sentry is a high-brand scale-up with scarce, competitive intern seats, and the loop still tests DS&A and depth live. Calibrated against the tracker, this sits just below the 8–9 full-stack cohort (CTC 9, Roblox 9, DV 9, Axon 8) — clean screen, loop-and-timing decide it.
- **Funnel filters:** ~3–4 rounds · practical (not LeetCode-gauntlet) difficulty · No HackerRank/OA · light/no sys design for interns · Bottleneck: resume screen + practical technical rounds · enrolled-student eligibility · work-authorized (no sponsorship needed).
- **Outside the resume:** Apply within ~72 hours of the req opening — the intern cycle runs absurdly early (Aug–Oct) and the first wave gets the open headcount (`recruiting.md` §8). Land a referral up the ladder (HM > recruiter > engineer) to move from cold pile to warm read (`recruiting.md` §4). Because Sentry is open-source-first, a single merged PR into a Sentry public repo directly de-risks the open-source-fit gap flagged above.
