# Software Engineer Intern — Summer 2027 at CACI (Ypsilanti RST)

## Verdict

- **Score:** 8.0 / 10 (2 demerits — 0 emergency, 0 major, 2 minor)
- **Eligibility:** eligible — Expected May 2028, still enrolled through Summer 2027; CS is a named related field; U.S. citizen / TS-eligible (citizenship is an Eightfold knockout, not a page class-year miss); JD has no GPA or class-year floor
- **Track:** full-stack + radar / SAR / remote-sensing software
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- One-page UMich CS intern resume: MDC Flask REST on AWS EC2 leads, then a deep C++ lock-free / zero-allocation / CMake DSP **plugin** in slot two, then a real-time co-op, then FastAPI + pytest CI — on-axis for Ypsilanti RST whose bottleneck is the resume, not an OA.
- C++ and Python are proven in bullets; AWS is through use; class year is in window; no Java/Rust/CUDA/radar invented. LoRA/ML-research is off the page.
- Binding dings: Granular has no sized runtime metric; Linux/Unix is never named.

### Demerits

- **minor** · `Granular Synthesizer Plugin` · metric-free — lock-free SPSC, zero-alloc `processBlock`, Gaussian-LUT scheduler, and a CMake real-time safety audit are the C++/DSP signal, but none sizes callback latency, xruns, or CPU
- **minor** · `resume` · Linux/Unix absent — desired environment appears nowhere; Docker and EC2 imply it, a 30-second Linux scan is empty

### Misreads

- A rushed RST skim can file this as "web intern plus hobby audio, no Linux" before the lock-free C++ and shipped Python land.

### Interview angles

- **Lead with:** Granular zero-allocation `MemoryPool`, lock-free SPSC, fractional-accumulator scheduler, CMake VST3/AU real-time audit (prototype-to-application C++ / DSP); MDC sole-ownership production Flask API on EC2; SignalWeaver pytest + GitHub Actions; CaseStudyPrep Web Worker / 27% upload-failure debug
- **Defend:** Granular has no xrun/latency/CPU metric on the page. *(out of rails: pool has no verbatim impact-metric bullet; swap sets cannot invent one.)* Unix/Linux is implied by Docker/EC2 but unnamed. *(out of rails: Unix/Linux never appears in the pool or skills inventory.)* No Java, Rust, CUDA, Jira, or radar internship — say C++ and Python are the interview languages; do not claim them. *(out of rails: those stacks are absent from pool and inventory.)* Voice AI co-op is intern delivery / real-time, not a sensor claim
- **Depth prep:** C++ real-time (slab allocators, SPSC, `processBlock` constraints, CMake release gates); Python production (Flask/EC2, Docker, pytest CI); light DS&A for a 2–3 round Easy loop; code-review / test-plan stories

## Likelihood

- **Resume screen:** High — eligible class year, C++ in bullets with lock-free/CMake depth as the #2 entry, Python shipped on AWS; two minors dent the scan, they do not flip it to a no
- **Overall hire odds:** Medium — no published OA, so most front-end elimination is this PDF, then 2–3 Easy rounds at a ~20–25% C-tier defense intern accept rate. Citizenship/TS willingness is a hard form knockout this page cannot show
- **Funnel filters:** Eightfold apply → resume screen (binding) → 2–3 Easy technical/behavioral · no published OA on this RST req · no intern sys design · U.S. citizenship + willingness to obtain Top Secret
- **Outside the resume:** Answer citizenship **Yes**, clearance in-hand **No**, TS willingness **Yes**; confirm Ypsilanti onsite (Northville commute, valid US DL); apply in the first wave (posted 2026-09-04); no CACI contact in `network.md`
