# Software Engineer Intern - Backend Focused - Winter 2027 at Rippling

## Verdict

- **Score:** 9.0 / 10 (1 demerits — 0 emergency, 0 major, 1 minor)
- **Eligibility:** eligible — `Expected May 2028` is after Winter 2027 (mid-January–April); currently enrolled, junior-year winter with return to school
- **Track:** full-stack + compound HR/IT/Finance workforce platform / employee-graph backend
- **Pipeline:** 3 cycle(s) · exit: writer_peak

## Screen Review

### First read

- MDC leads as sole-engineer Python backend: Requests/Pandas ETL across 400 PACs and a production Flask REST API on AWS EC2, scoped with a real stakeholder — the intern-shaped Python delivery the JD asks for, even though the title is Data Engineer.
- Spine is backend services, not frontend: Vylet’s 79%→89% ownership-verification fix, asyncpg DAL, and pure-Python consensus gate; SignalWeaver async FastAPI at 9.1s p50 / 15.2s p99 plus 49ms pgvector search and Docker/CI.
- Binding ding: the only Software Engineer Co-op is Voice AI / Angular / ONNX with zero Python — a recruiter hunting “prior SWE intern/co-op utilizing Python” will not count it as that proof.

### Demerits

- **minor** · `CaseStudyPrep.AI` · titled SWE co-op is not Python — The only Software Engineer Co-op on the page is Voice AI — Angular, RxJS, ONNX, S3 — with zero Python. A screen looking for prior SWE intern/co-op utilizing Python will not count this entry as that proof; Python backend sits under Data Engineer, Founder, and a project.

### Misreads

- A rushed screen can bucket CaseStudyPrep as the intern/co-op proof, then bounce the packet for “no Python intern,” missing that MDC is the Python production delivery and Vylet/SignalWeaver are the Python services.

### Interview angles

- **Lead with:** MDC Flask REST + Requests/Pandas ETL as titled production Python backend with stakeholder ownership; Vylet 79%→89% defect fix, asyncpg DAL, and pure-Python Node 3 consensus (no LLM) as platform-shaped Python; SignalWeaver FastAPI p50/p99 and 49ms pgvector search as instrumented APIs
- **Defend:** CaseStudyPrep is a real SWE Co-op but client-side Voice AI, not Python — pivot to MDC as the Python intern-equivalent and do not claim the co-op used Python *(out of rails: CaseStudyPrep pool has no Python bullet; swap sets cannot bridge; omitting it drops the only SWE Co-op title)*
- **Depth prep:** HackerRank Med–Hard DSA (graphs, DP, strings) before apply — OA is the bottleneck and often lands within days; walk Flask REST + EC2 ownership; Node 3 weakest-link consensus and asyncpg freshness logic; FastAPI instrumentation (why p50 vs p99). Behavioral is a filter (`recruiting.md` §6), not the differentiator. Do not invent Rippling HRIS/payroll/App Studio experience.

## Likelihood

- **Resume screen:** High — Flask REST, FastAPI, a pure-Python consensus gate, and an asyncpg DAL are on the page; one minor is a title/language split, not a missing backend language
- **Overall hire odds:** Medium — A-tier; HackerRank OA (Med–Hard DSA, often within days) is the bottleneck. Resume likely clears; 1–2 live HackerRank rounds decide it. Encouragement clause means the split Python-intern proof is not an auto-knockout
- **Funnel filters:** Rippling ATS (rolling) → HackerRank OA (4 MCQ + 2–3 coding, Med–Hard DSA) → 1–2 live HackerRank tech (60 min); bottleneck OA; ~2–5% **[directional]**; hybrid SF 3/2, Winter 2027 13 weeks; JS/HTML/CSS not a floor on this backend req; `Expected May 2028` eligibility
- **Outside the resume:** Submit now (rolling; OA often within days). Drill graph/DP/string mediums before the HackerRank lands. A Rippling engineer referral beats polishing the co-op-title gap
