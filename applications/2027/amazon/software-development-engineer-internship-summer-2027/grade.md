# Software Development Engineer Internship - Summer -2027 (USA) at Amazon

## Verdict

- **Score:** 8.0 / 10 (2 demerits — 0 emergency, 0 major, 2 minor)
- **Eligibility:** eligible — Expected May 2028 is inside Oct 2027–Sep 2029; Summer 2027 leaves Fall 2027 + Winter 2028; B.S. CS; 18+
- **Track:** full-stack
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- MDC leads with a shipped Flask REST API on AWS EC2 plus a Requests/Pandas ETL (~800 hours off 400 PACs) — classic SDE intern spine (scalable service, ownership, routine cloud).
- CaseStudyPrep sits second with a 27% S3-upload failure recovery and a Web Worker under 5ms / 60 FPS — operational excellence without pretending Amazon employment.
- Binding dings: Granular (the C++ language proof) has no sized outcome, and testing on the page is pytest inside GitHub Actions, not unit tests on the Flask or Redis services.

### Demerits

- **minor** · `Granular Synthesizer Plugin` · metric-free — CMake/VST3 release packaging and a lock-free SPSC FIFO prove C++ systems depth, but there is still no latency, throughput, or failure number a recruiter can size — easy to bucket as hobby DSP instead of scalable-services flavor.
- **minor** · `resume` · testing evidence is CI-only — leading SignalWeaver with GitHub Actions pytest still only shows tests inside a CI pipeline; there are no unit tests on the Flask REST API or Redis/Celery workers the JD's SDLC/CI/CD line is hunting.

### Misreads

- Granular without a number can read as hobby DSP rather than the C++ systems proof this SDE req is hunting.
- Vylet’s LangGraph launch line can get the page bucketed as an ML/agent intern; the Redis/Celery workers and the diagnosed 79%→89% qualification defect are the backend/ops read.

### Interview angles

- **Lead with:** MDC sole-engineer Flask REST on EC2; CaseStudyPrep 27% S3-upload recovery and Web Worker under 5ms / 60 FPS; Vylet Redis/Celery recurring workers plus the name-collision defect fix; SignalWeaver FastAPI p50/p99 plus GitHub Actions pytest; Granular C++ zero-alloc audio thread and lock-free SPSC
- **Defend:** Granular has no callback-latency number on the page *(out of rails: pool bullets 1–6 have no impact metric; swap sets cannot invent one)* — script the audio-thread rules and what you would measure. Unit tests are not on Flask or Redis *(out of rails: live pool’s only test mention is SignalWeaver pytest CI)* — talk pytest on SignalWeaver and how you would add tests on a matching service. Do not claim Java, Go, Rust, Kafka, or AWS-as-deep-expert. LangGraph is pipeline infra, not an ML-research lead.
- **Depth prep:** Amazon custom OA (~90 min: ~70 min coding, ~15 min Workstyles / Leadership Principles, ~5 min survey; doctrine also names two coding questions + a systems-design scenario block). Easy–Med DS&A. STAR mapped to Ownership, Dive Deep, Bias for Action, Customer Obsession from MDC production API and CaseStudyPrep fault-tolerant uploads. No intern sys design (`companies.md`).

## Likelihood

- **Resume screen:** High — one page, class year in window, shipped APIs, databases, CI, C++ through use; OA is the binding intern filter
- **Overall hire odds:** Medium — A-tier ~1–2% large class; resume is not the first elimination; Amazon custom OA then 1–3 Easy–Med rounds still drop most of the intern class
- **Funnel filters:** amazon.jobs resume → Amazon custom OA · OA + 1–3 rds · Easy–Med · no intern sys design · Bottleneck: OA · ~1–2% (large class) · in-person US intern sites · conferral Oct 2027–Sep 2029 + remaining term after internship
- **Outside the resume:** Timed OA prep plus LP Work Style consistency; apply in the first wave; Winter 2027 OK if asked, do not select Fall 2027
