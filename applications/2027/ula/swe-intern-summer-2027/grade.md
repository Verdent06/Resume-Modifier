# Software Engineering Internship Summer 2027 at United Launch Alliance

## Verdict

- **Score:** 9.0 / 10 (1 demerit — 0 emergency, 0 major, 1 minor)
- **Eligibility:** eligible — Expected May 2028; Summer 2027 is after sophomore year / rising junior; JD requires completed sophomore; GPA 3.66 ≥ 3.00 cumulative no rounding; STEM CS + Economics; U.S. citizen / U.S. Person / ITAR; Fall 2027 enrollment; 10 weeks Denver
- **Track:** full-stack + aerospace / launch-vehicle / mission-critical / embedded-adjacent
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- One-page UMich CS intern resume: MDC Flask REST on AWS EC2 leads, then a deep C++ lock-free / zero-allocation / CMake real-time **plugin** in slot two, then a real-time co-op, then FastAPI + Docker / GitHub Actions CI — on-axis for ULA–Denver whose bottleneck is the resume, not an OA.
- C++ and Python are proven in bullets; AWS, Docker, and GitHub Actions are through use; class year and GPA are in window; no C / Shell / Perl / Java / launch internship invented. LoRA/ML-research is off the page.
- Binding ding: Granular has no sized runtime metric (latency / xrun / CPU).

### Demerits

- **minor** · `Granular Synthesizer Plugin` · metric-free — CMake real-time safety audit, zero-alloc `processBlock`, lock-free SPSC, and 16-voice stealing are the C++/V&V signal, but none sizes callback latency, xruns, or CPU

### Misreads

- A rushed launch skim can file this as "web intern plus hobby audio, no flight software" before the lock-free C++ and shipped Python / lab CI land.

### Interview angles

- **Lead with:** Granular CMake real-time safety audit, zero-allocation `MemoryPool`, lock-free SPSC, 16-voice stealing (deterministic C++ / V&V-adjacent); MDC sole-ownership production Flask API on EC2; SignalWeaver pytest + GitHub Actions / Docker Compose; CaseStudyPrep Web Worker / 27% upload-failure debug
- **Defend:** Granular has no xrun/latency/CPU metric on the page. *(out of rails: pool has no verbatim impact-metric bullet; swap sets cannot invent one.)* No C, Shell, Perl, or a "JavaScript" Skills label — TypeScript/Angular through use is the honest JS-family proof; C++ and Python are the interview languages. No launch internship, clearance, Snowflake, Databricks, Copilot, Fusion, or Tableau. Voice AI co-op is intern delivery / real-time, not a flight-software claim
- **Depth prep:** C++ real-time (slab allocators, SPSC, `processBlock` constraints, CMake release gates); Python production (Flask/EC2, Docker, pytest CI); V&V / test-plan stories; light DS&A for a 2–3 round Easy loop. Do not fake embedded MCU or 6DOF simulation

## Likelihood

- **Resume screen:** High — eligible class year and GPA, C++ in bullets with lock-free/CMake depth as the #2 entry, Python shipped on AWS, Docker/GitHub Actions CI; one minor dents the scan, it does not flip it to a no
- **Overall hire odds:** Medium — no published OA, so most front-end elimination is this PDF, then 2–3 Easy rounds at a ~12–18% B-tier launch intern accept rate. U.S. Person / ITAR is a hard form knockout this page cannot show
- **Funnel filters:** SuccessFactors apply → resume screen (binding, first-come) → recruiter phone → 1–2 Easy engineer interviews (STAR + project walk) · no published OA · no intern sys design · U.S. Person / ITAR · GPA 3.00 at apply · completed sophomore · Fall 2027 enrollment · Denver 10 weeks
- **Outside the resume:** Apply in the first-come window (posted 2026-09-08); form email `verdent06@gmail.com`; U.S. citizen **Yes**, sponsorship **No**; upload unofficial transcript when asked; confirm Denver 10 weeks and Fall 2027 enrollment. No ULA contact in `network.md`
