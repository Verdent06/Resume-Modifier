# Summer 2027 Software Engineer Intern at Peraton

## Verdict

- **Score:** 9.0 / 10 (1 demerit — 0 emergency, 0 major, 1 minor)
- **Eligibility:** eligible — Expected May 2028 + Summer 2027 term = rising junior; JD is geared toward rising junior or senior; GPA 3.66 ≥ 3.0 desired; U.S. citizen (citizenship is an iCIMS knockout, not a page miss)
- **Track:** full-stack + national-security IRAD / Strategic Innovation Center
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- One-page UMich CS intern resume: MDC Flask REST on AWS EC2 leads, then a deep C++ lock-free / zero-allocation / CMake plugin in slot two, then a titled SWE co-op, then FastAPI + Postgres + pytest CI — on-axis for SIC IRAD whose bottleneck is the resume, not an OA.
- Python and C++ are proven in bullets; AWS, Docker, PostgreSQL, and pytest are through use; class year is in window; GPA 3.66; no Java invented.
- Binding ding: Granular has no sized runtime metric.

### Demerits

- **minor** · `Granular Synthesizer Plugin` · metric-free — lock-free SPSC, zero-alloc `processBlock`, and a CMake real-time safety audit are the C++/OOP signal, but none sizes callback latency, xruns, or CPU

### Misreads

- A rushed SIC skim can file this as "web intern plus hobby audio, no Java" before the lock-free C++ and shipped Python land.

### Interview angles

- **Lead with:** MDC sole-ownership production Flask API on EC2 and stakeholder-scoped 5-month delivery (Agile analog); Granular zero-allocation `MemoryPool`, lock-free SPSC, CMake VST3/AU real-time audit (OOP/architecture + unit-test analog); SignalWeaver pytest + GitHub Actions; CaseStudyPrep Angular / 27% upload-failure debug
- **Defend:** Granular has no xrun/latency/CPU metric on the page. *(out of rails: pool has no verbatim impact-metric bullet; swap sets cannot invent one.)* No Java — say Python and C++ are the interview languages; do not claim Java or Bash-as-homelab. *(out of rails: Java and Bash are absent from pool and Languages inventory.)* Clearance in-hand is **None**; this intern is No Clearance Required. Voice-AI co-op is intern delivery / web reliability, not an ML-research claim
- **Depth prep:** C++ real-time (slab allocators, SPSC, `processBlock` constraints, CMake release gates); Python production (Flask/EC2, Docker, pytest CI); light DS&A for a 2–3 round Easy loop; STAR on MCFN scoping and the CaseStudyPrep failure-rate debug

## Likelihood

- **Resume screen:** High — eligible class year, C++ in bullets with lock-free/CMake depth as the #2 entry, Python shipped on AWS; one minor dents the scan, it does not flip it to a no
- **Overall hire odds:** Medium — no published OA, so most front-end elimination is this PDF, then Easy project + STAR at a ~20–25% C-tier defense intern accept rate. Citizenship and Herndon/Blacksburg 10-week / 35–40h availability are form knockouts this page cannot show
- **Funnel filters:** iCIMS apply → resume screen (binding) → recruiter (citizenship, Herndon or Blacksburg onsite, June–August 10-wk) · intern OA unpublished on this IRAD req · no intern sys design · U.S. citizenship · No Clearance Required
- **Outside the resume:** Answer citizenship **Yes**, clearance in-hand **None**; confirm Herndon **or** Blacksburg onsite for June–August at 35–40h; desired salary **$25/hour** (posted rate); apply in the first wave (captured 2026-09-09); no Peraton contact in `network.md`
