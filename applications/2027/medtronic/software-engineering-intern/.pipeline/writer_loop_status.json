{
  "action": "peak",
  "out_of_rails": [
    {
      "entry": "MDC",
      "defect": "third bullet metric-free",
      "why": "Pool has four bullets; 1 carries the 800-hour metric and is already the lead, 3 is Flask/EC2 (already bullet 2). Remaining third-bullet options 2 and 4 are both metric-free. Inventing a number is forbidden.",
      "attempted": [
        "Iter-2 layout used pool 1+3+2; grader flagged pool 2 (aggregation / 'stop rebuilding spreadsheets') as metric-free and restating bullet 1",
        "Iter-3 swapped pool 2 for pool 4 (stakeholder / sole-engineer ownership) — still no impact metric in the pool text",
        "Scanned full MDC pool 1–4 — only pool 1 has a sized impact (~800 hours / 400 PACs); cannot invent a metric for 2 or 4",
        "Cannot drop the third bullet (no-deletion / iteration-1 depth) or add a fourth (page_fill already tight)"
      ]
    },
    {
      "entry": "Granular Synthesizer Plugin",
      "defect": "metric-free",
      "why": "Pool 1–6 are architectural C++ (slab, SPSC, scheduler, polyphony, delay buffer, CMake/processBlock audit) with no users, latency, xrun, or adoption witness. Inventing metrics is forbidden.",
      "attempted": [
        "Iter-1 graded pool 1+2+6 (MemoryPool, SPSC, CMake/processBlock audit) — still metric-free (1 minor)",
        "Iter-2 swapped pool 6 for pool 5 (delay ring buffer / zipper-noise) — still metric-free and scored worse (3 minors); reverted",
        "Iter-3 restored pool 1+2+6; scanned pool 3 (Gaussian LUT / grain scheduler) and pool 4 (16-voice polyphony) — technical counts only, no screen-usable impact metric",
        "Swap sets cannot add latency/users; no other entry supplies a C++ systems witness with sized impact"
      ]
    },
    {
      "entry": "SignalWeaver",
      "defect": "single bullet",
      "why": "A second bullet overflowed the page in gate-fix; deepening vs page_fill is unfixable in-rails. Dropping the entry would hollow Projects. Inventing Java or a medical-device internship is forbidden.",
      "attempted": [
        "Iter-1 had two SignalWeaver bullets (FastAPI p50/p99 + React dashboard) and overflowed; gate-fix cut to one pgvector line to pass page_fill",
        "Iter-2/3 kept exactly one bullet (pool 4 pgvector 49ms/99ms) per gate-fix — adding any second pool bullet (1–3, 5–7) reopens page overflow",
        "Scanned full SignalWeaver pool 1–7 — all metric-bearing but none can share the page with Granular at 3 + MDC at 3 without failing page_fill",
        "Cannot omit SignalWeaver (Projects would empty) or steal a bullet from a protected/high-signal Experience entry to make room"
      ]
    }
  ]
}
