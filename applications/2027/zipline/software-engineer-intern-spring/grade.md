# Software Engineer Intern (Spring 2027) at Zipline

## Verdict

- **Score:** 8.0 / 10 (2 demerits — 0 emergency, 0 major, 2 minor)
- **Eligibility:** eligible — Expected May 2028 + Spring 2027 (Jan–Apr) term = during junior year / completed second year; JD requires completed second year
- **Track:** full-stack + autonomy / autonomous logistics
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- CaseStudyPrep leads on user-facing real-time product (Web Worker / sub-5ms / 60 FPS; 27% upload recovery; 40% inference-cost cut) — the full-stack screen and a systems differentiator both show in the top half.
- Python and TypeScript appear through use (Vylet/MDC/SignalWeaver FastAPI + React/TS dashboard); Flask/EC2 and Docker/Redis cover backend and deploy; Granular carries lock-free C++ next to the consumer-apps spine.
- Binding ding: Granular has no sized runtime outcome, and MDC's Flask/EC2 hook has no traffic/latency number — the take-home will probe both.

### Demerits

- **minor** · `Granular Synthesizer Plugin` · metric-free — MemoryPool, SPSC FIFO, and the processBlock audit prove real-time C++ discipline, but nothing sizes the plugin
- **minor** · `Michigan Data Consulting (MDC)` · API bullet unquantified — Flask REST API on AWS EC2 is the production backend-plus-cloud claim and lands with no traffic, latency, or consumer metric; 400 PAC / ~800 hour scale sits on the ETL bullet

### Misreads

- Granular without a number can read as weekend DSP rather than the lock-free / zero-alloc systems proof an autonomy screener wants.
- MDC's unquantified Flask/EC2 line can be bucketed as "class project on a VM" instead of a shipped stakeholder API.

### Interview angles

- **Lead with:** CaseStudyPrep failure recovery and real-time UI-thread offload; Granular zero-alloc `processBlock`, lock-free SPSC, real-time safety audit; Vylet name-collision defect (79% → 89%) as product ownership; SignalWeaver React/TypeScript + FastAPI as the consumer web/Python backend spine
- **Defend:** Granular has no callback-latency / xrun / CPU number on the page *(out of rails: full pool has no allowlisted runtime impact metric; 16-voice swap dropped JUCE/VST3 derivation and still no latency)*; MDC API has no traffic/latency — point to the ETL scale (400 PACs / ~800 hours) and sole-engineer EC2 delivery *(out of rails: Flask bullet has no traffic metric in the pool)*; JD names Go and React Native the inventory does not have — do not fake them; Python + React/TypeScript web is the honest mapping
- **Depth prep:** take-home + project presentation (bottleneck) — walk Granular real-time constraints and MDC API ownership; React/FastAPI feature stories from SignalWeaver; CS fundamentals for the resume deep-dive; no standard LC OA to grind, but DS&A still shows in the tech screen

## Likelihood

- **Resume screen:** High — Python and TypeScript through use, user-facing product in the lead, real-time C++ in projects, one clean page, eligible under the second-year gate
- **Overall hire odds:** Medium — B-tier ~3–8%; resume clears a Greenhouse human screen, but the take-home plus project defense decides the offer
- **Funnel filters:** Greenhouse resume screen → recruiter → resume deep-dive / tech screen → substantial take-home → final loop with project presentation · no standard LC OA · bottleneck: take-home + project defense · completed-second-year eligibility (met) · Spring 2027 (Jan–Apr) onsite South San Francisco
- **Outside the resume:** Apply in the first Greenhouse wave; warm referral into the recruiter screen; mocks aimed at defending Granular RT design and MDC API ownership as the presentation spine
