# Software Engineering Winter Internship at Rubrik

## Verdict

- **Score:** 9.0 / 10 (1 demerits — 0 emergency, 0 major, 1 minor)
- **Eligibility:** eligible — Expected May 2028 = Spring 2028 matches JD window (December 2027 or Spring 2028); UMich CS+Econ; junior during Jan–May 2027
- **Track:** full-stack
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- MDC leads with a production Flask REST API on AWS EC2 shipped into a nonprofit's public-facing workflow, then quantified ETL (~800 hours / 400 PACs) — finished backend delivery that maps onto "write code that will ship."
- Vylet is a live B2B Docker/Redis/Celery product with a named 79%→89% defect; CaseStudyPrep carries Angular/RxJS production debug (27% upload-failure, 60 FPS). SignalWeaver closes the FE+BE spine: React/TypeScript, FastAPI REST, Postgres/pgvector, Docker/CI.
- Binding ding on the PDF: Granular's lock-free C++ (the JD concurrency/C++ exhibit) has no sized latency closer, so a rushed screen can skim it as an audio hobby. Binding ding on the *funnel* is the 90-min HackerRank OA, not this page.

### Demerits

- **minor** · `Granular Synthesizer Plugin` · real-time C++ with no sized latency closer — Lock-free SPSC and slab allocation are the right concurrency/systems exhibit for this JD, but neither bullet lands a microsecond, fps, or throughput number, so a rushed screen cannot size the claim and may skim the audio plugin as a hobby instead of C++ proof.

### Misreads

- Granular can skim as an audio-plugin hobby instead of the JD's C++ / concurrency line.
- MDC's Data Engineer title can skim as a data-analyst intern until the Flask REST / EC2 bullets are read.
- CaseStudyPrep's Voice AI co-op title can skim as notebook ML; the page actually leads with S3 reliability and a Web Worker, not Silero training.

### Interview angles

- **Lead with:** MDC Flask REST on AWS EC2 + PAC ETL; Vylet Docker/Redis/Celery pipeline and the 79%→89% name-collision fix; SignalWeaver React/TypeScript + FastAPI p50/p99 + GitHub Actions CI; CaseStudyPrep Angular/RxJS S3 upload repair and Web Worker latency; Granular lock-free SPSC / zero-heap audio thread as the honest concurrency story
- **Defend:** Granular has no µs/ms/fps closer *(out of rails: pool bullets 1–6 have no impact-metric latency unit; swap sets cannot invent one)* — narrate lock-free SPSC + zero-heap `processBlock()` as the same class of constraint as concurrent storage/recovery code. No Java, Go, or Objective-C on the page — JD is one-or-more languages; walk Python + TypeScript + C++. No prior AppSec / Zero Trust product — JD treats security as assignment flavor, not a malware-research bar. MDC is a campus consulting engagement, not prior professional employment — say so if asked. SignalWeaver is a 90-ticker research harness, not external users — point to MDC's nonprofit workflow and Vylet's three paying clients
- **Depth prep:** Timed LC mediums (some hard) for the confirmed 90-min HackerRank OA (`companies.md`; `recruiting.md` Part I §1); walk Flask/REST + EC2, React/TypeScript + FastAPI trade-offs, a named production defect (Vylet qualification or CaseStudyPrep expired S3 URLs), and lock-free SPSC / slab allocation if they probe C++; light intern LLD in the 1h practical round; people-leader project grill on shipped ownership

## Likelihood

- **Resume screen:** High — one page, GPA 3.66, production Flask REST on EC2, Angular/RxJS frontend debug, React/TypeScript + FastAPI + Docker/CI, C++ concurrency in the field, and a live B2B product; one framing ding, not a no-pile
- **Overall hire odds:** Medium — B-tier ~3–8% with bottleneck at **OA** then tech (`companies.md`). The PDF deserves a Greenhouse human read; conversion still dies on Med–Hard HackerRank and the algo/practical loop
- **Funnel filters:** Greenhouse **8171088** resume + knockouts (Palo Alto Jan 11–May 7 2027 onsite, HackerRank OA yes, US university, sponsorship) → HackerRank ~90 min LC Med–Hard **[directional, r/csMajors US intern]** → ~30-min recruiter → 1h algo → 1h practical/LLD → ~45-min people-leader **[directional, US intern 2025–26]** · Bottleneck: **OA** then tech · ~3–8% **[directional, B-tier security + OA-gated]**
- **Outside the resume:** Apply in this first-wave window (first_published 2026-09-18). Confirm full-time Palo Alto Jan 11–May 7 2027. Answer expected graduation **Spring 2028** honestly. Form email **verdent06@gmail.com**. Prep timed LC mediums/hards for the 90-min HackerRank. Do not invent Java, Go, Objective-C, Kubernetes, Snowflake, Databricks, Copilot, or Fusion
