# Summer 2027 Intern:: Software Engineer II at Walmart

## Verdict

- **Score:** 10.0 / 10 (0 demerits — 0 emergency, 0 major, 0 minor)
- **Eligibility:** eligible — Expected May 2028 ∈ December 2027–August 2028; US citizen, no sponsorship needed
- **Track:** ai-ml + retail-scale
- **Pipeline:** 2 cycle(s) · exit: zero_demerits

## Screen Review

### First read

- Thirty-second scan registers a Python + applied-AI intern: Dockerized LangGraph with an eval lift, ONNX inference, and a LoRA fine-tune with held-out accuracy — the written floor on R-2630029.
- Container/cloud and a React dashboard sit on the same page (Vylet Docker, S3, EC2, SignalWeaver Compose), so the retail-scale systems identity is visible, not buried under notebook ML.
- Binding ding is not on the resume. Funnel bottleneck is HackerRank then tech rounds (`companies.md` Walmart Labs). No Java/Spring/Cosmos — honest plus-only gap, not a screen no.

### Demerits

No demerits — clean screen.

### Misreads

- A skimmer who only reads the Vylet founder line may bucket this as a PE-tools startup resume and miss the Python consensus gate, ONNX, and LoRA — walk those first in a screen call.
- Voice-AI on CaseStudyPrep can read as an audio-product intern. It is on-device ML + an S3 upload-failure fix, not a DSP hobby.

### Interview angles

- **Lead with:** Vylet Dockerized LangGraph + LangSmith eval (50% → 90% faithfulness) and the pure-Python Node 3 consensus gate; SignalWeaver LoRA (81% → 96% held-out); CaseStudyPrep ONNX VAD (40% inference-cost cut) and the 27% S3 failure-rate fix.
- **Defend:** no Java, Scala, Spring Boot, or Cosmos DB in inventory — plus-only on this JD, ramp on the assigned stack. Have not owned Walmart-scale associate systems. MDC is extracurricular on the Workday form (CaseStudyPrep + Vylet only as prior experience). SpaceXAI Campus Lead Ambassador is extracurricular if asked.
- **Depth prep:** HackerRank Easy–Med (`companies.md`). Walk LoRA training/eval, LangGraph node contracts, Docker/Compose, presigned-URL retry, Pandas ETL → Flask on EC2. STAR for agile/cross-functional and a production defect (name-collision or S3).

## Likelihood

- **Resume screen:** High — Python through use, applied AI/ML with metrics, systems evidence in the lead window.
- **Overall hire odds:** Medium — C-tier retail-tech; HackerRank then ~3 tech + behavioral rounds; bottleneck tech rounds; ~10–15% (`companies.md` Walmart Labs). Resume is not the binding filter.
- **Funnel filters:** Workday knockouts (grad window, no sponsorship) → HackerRank OA (Easy–Med) → ~3 rounds, no intern sys design. Housing if >50 miles from Home Office.
- **Outside the resume:** Apply in this first-wave window (posted ~2026-09-11). Prep HackerRank mediums. No Walmart contact in `network.md` — a UMich alum in Bentonville or Sunnyvale still beats cold Workday.
