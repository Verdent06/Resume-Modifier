# 2027 Internship - Sensor Systems/Data Analytics at Johns Hopkins Applied Physics Laboratory (JHU APL)

## Verdict

- **Score:** 8.0 / 10 (2 demerits — 0 emergency, 0 major, 2 minor)
- **Eligibility:** eligible — Expected May 2028 student intern for Summer 2027; GPA 3.66 ≥ 3.0; CS related field; U.S. citizen for Secret
- **Track:** ai-ml
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- Lyndbrook leads with multi-source EPA ECHO + MassGIS aggregation into a scored 280-lead shortlist (35% precision) — the fusion analog this AMDS seat actually screens for.
- MDC Pandas ETL (~800 hours / 400 PACs) plus Vylet's pure-Python consensus score and SQL DAL, then SignalWeaver LoRA 81%→96% held-out and an out-of-sample regression, read as ingest → score/fuse → evaluated ML — not `model.fit()`.
- Binding ding: a Voice AI co-op still sits in Experience, and the only SQL-through-use line never sizes impact.

### Demerits

- **minor** · `CaseStudyPrep.AI` · off-axis voice-AI intern — Software Engineer Co-op (Voice AI) with Silero VAD / ONNX Whisper filtering is client-side audio inference, not multi-source fusion, scoring, stats, or held-out ML on sensor-like data. Occupies a titled Experience slot on an AMDS skim.
- **minor** · `Vylet` · SQL/DAL bullet metric-free — The asyncpg / injection-safe SQL freshness bullet is the only on-page SQL proof, but it closes on architecture (re-scrapes, timestamp validation) with no sized impact.

### Misreads

- CaseStudyPrep.AI can bucket the candidate as a voice/audio intern rather than an applied-analytics intern, because the title is Voice AI and the only bullet is VAD/Whisper.
- The SQL/DAL line can read as database plumbing rather than the language-floor proof a data-analytics screen wants, because it never sizes the freshness win.

### Interview angles

- **Lead with:** Lyndbrook multi-source EPA/MassGIS entity resolve + Review Velocity 35% precision; MDC Requests+Pandas ETL on irregular filings; Vylet pure-Python triangulated scoring (registry + crawl + query); SignalWeaver LoRA held-out accuracy and out-of-sample regression
- **Defend:** Voice AI co-op is real intern delivery — analog is on-device inference/eval, not sensors; do not invent radar/RF *(out of rails: CaseStudyPrep.AI pool is all voice-AI; loop anti-deletion and min_entries=5 forbid omitting the Experience entry; Granular would be worse audio DSP)*. SQL is on the page only in the unquantified DAL bullet — walk re-scrape/freshness as the outcome even though the line has no number *(out of rails: only SQL pool bullet has no impact metric; swapping it drops SQL-through-use)*. No Tableau, Power BI, Snowflake, Databricks, MATLAB, classified sensor work, or clearance-in-hand
- **Depth prep:** multi-source join/normalize/score (PWSID entity resolve); scoring precision vs. false positives; LoRA data split / overfit story and honest 3.39% R²; Python+SQL talking points (no standard OA — still expect tool fluency); 15–20 min project talk of Lyndbrook or SignalWeaver; STAR on multidisciplinary delivery (MDC/Lyndbrook stakeholders); citizenship / Interim Secret eligibility (eligible, not already cleared)

## Likelihood

- **Resume screen:** High — Python/SQL through use, Pandas ETL, multi-source scoring, LoRA with held-out accuracy, out-of-sample regression, stats coursework, and GPA 3.66 all register in one pass on a resume-first B-tier UARC intern req
- **Overall hire odds:** Medium — Bottleneck is the resume and this page is on-axis for applied analytics, but a Voice AI co-op in Experience costs a beat, technical panels (possible project talk) still eliminate, and the page never names sensors. B-tier UARC ~10–15% is the ceiling analog
- **Funnel filters:** iCIMS resume → recruiter/HM screen (citizenship/clearance) → technical panels (often 2–4h; possible 15–20 min project talk). No standard intern OA. No intern sys design. Bottleneck: resume (`companies.md` JHU APL B-tier). Interim Secret by start / final Secret; U.S. citizenship; GPA 3.0
- **Outside the resume:** Apply in the first iCIMS wave (posting was ~4 days old as of 2026-09-04); prep the project talk and clearance/citizenship form answers; do not invent Tableau/Snowflake, radar, or clearance-in-hand
