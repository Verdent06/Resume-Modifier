━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
RESUME GRADING REPORT (DEMERIT MODEL)
Role: Software Engineer Intern, Applications Engineering at Tesla
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

REVIEWER PERSONA
Tesla Applications Engineering intern screener (B-tier, Fremont, Winter/Spring 2027). Bar is general SWE plus infrastructure and services — APIs, Docker/cloud, internal tools, full-stack web — not Autopilot-ML, vehicle firmware, or chip EDA. I look for a JD-floor language in bullets (Python and/or C++), operated services, and software-generalist breadth, then a HackerRank medium OA; the resume's job is team-match.

DEFECTS  (machine-read by scripts/validate.py demerits — valid JSON, this block only)
```json
{
  "defects": [
    {"entry": "resume", "severity": "minor", "defect": "C++ skills-only", "why": "C++ is listed under Languages with zero bullet or project evidence; Tesla names C/C++ as a floor option and a recruiter who scans polyglot proof will treat the Skills-line claim as unbacked"},
    {"entry": "CaseStudyPrep.AI", "severity": "minor", "defect": "voice-AI/ONNX closer", "why": "The co-op's last bullet is Silero VAD via ONNX Runtime cutting Whisper inference cost 40% — an on-device ML/inference closer on an Applications Engineering infra/services screen, so a skim can bucket this entry as voice-AI rather than production web/S3 services"}
  ]
}
```

WHAT WOULD TAKE THIS TO THE NEXT LEVEL
- C++ sits only on the Skills line — put it in a bullet or drop the claim so the polyglot floor doesn't look inflated.
- CaseStudyPrep closes on ONNX/VAD/Whisper cost-cut instead of the S3/Angular reliability work, so the entry can read as voice-AI/ML on a team that wants internal tools and infra.

LIKELIHOOD ESTIMATE
─────────────────────
Resume Screen Pass:  High — one page, Python/Flask/AWS, Docker/Celery, FastAPI/React/CI in bullets; the two dings are skim-quality, not filter misses
Overall Hire Odds:   Medium — Tesla intern funnel is OA- then tech-round gated (HackerRank medium, bottleneck tech rounds, ~5–8%). This page should win Applications Engineering team-match; the offer still dies on timed coding and live technicals.
What Moves the Needle (outside the resume): Timed HackerRank Easy–Medium (arrays, hashes, graphs) plus a referral; be ready to walk Flask/EC2, Docker Compose/CI, and the S3 presigned-URL failure without the ONNX story leading.
