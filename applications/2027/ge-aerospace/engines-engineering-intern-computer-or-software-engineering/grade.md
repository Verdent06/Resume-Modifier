# Engines Engineering Intern – Computer or Software Engineering – US – Summer 2027 at GE Aerospace

## Verdict

- **Score:** 9.0 / 10 (1 demerits — 0 emergency, 0 major, 1 minor)
- **Eligibility:** eligible — GPA 3.66 ≥ 3.0; B.S. CS matches CS/CE/SE; Summer 2027 + Expected May 2028 = rising senior (upperclassmen preferred, no hard class-year filter). U.S. Person / no-sponsorship is an apply-form knockout, not a page miss.
- **Track:** full-stack + aerospace / mission-critical / real-time / embedded-adjacent
- **Pipeline:** 3 cycle(s) · exit: writer_peak

## Screen Review

### First read

- CaseStudyPrep leads on Web Worker / sub-5ms / 60 FPS real-time systems plus a 27% fault-tolerant upload recovery — production latency and reliability, not a Voice-AI product pitch.
- Granular sits #2 with C++ lock-free SPSC, zero-alloc `processBlock`, and a real-time safety audit — the engines differentiator is in the top half; MDC Flask/ETL plus a 49ms pgvector search back the CS/SWE + data/compute screen.
- Binding ding: Granular never sizes the systems win (no latency / xrun / CPU number).

### Demerits

- **minor** · `Granular Synthesizer Plugin` · metric-free — Lock-free SPSC, zero-alloc processBlock, and a real-time safety audit are the embedded-adjacent signal this engines shop wants, but none of the three bullets sizes the outcome (callback latency, xruns, CPU)

### Misreads

- Granular without a number can read as hobby DSP rather than mission-critical real-time discipline — a skim may underrate the C++/embedded-adjacent evidence GE engines work rewards.

### Interview angles

- **Lead with:** Granular Synthesizer — zero-alloc `processBlock`, lock-free SPSC FIFO, atomics, real-time safety checklist; CaseStudyPrep Web Worker / <5ms / 60 FPS and 27% upload-failure recovery as production real-time under latency constraints; MDC sole-owned Flask REST + Requests/Pandas ETL as the data/compute delivery
- **Defend:** Granular has no xrun/latency/CPU metric on the page *(out of rails: pool has no verbatim impact-metric bullet; swap sets cannot invent one)* — script the audio-thread constraints and what you would measure next. Do not claim citizenship, an aviation internship, or engine-controls hardware. If asked about Vylet, pivot to the 79%→89% defect fix and Docker/Celery workers, not the PE/agentic product story.
- **Depth prep:** lock-free concurrency, C++ memory/audio-thread rules, deterministic constraints; Flask/ETL ownership and the Web Worker real-time story; STAR on initiative, teamwork, and learning (JD desired characteristics). No standard intern OA — expect recruiter + HM technical/behavioral.

## Likelihood

- **Resume screen:** High — real-time lead, C++ lock-free in the top two, Python data/API plus a 49ms search line, languages in bullets, one clean page, resume is the gate
- **Overall hire odds:** Medium — B-tier industrial funnel is resume-gated then an Easy–Med 2–3 round loop (~10–15%); the page clears the screen, but U.S. Person / no-sponsorship is a binary apply-form knockout and the HM still has to hear real-time and test/integration live
- **Funnel filters:** Workday resume screen → recruiter phone → HM technical + STAR behavioral; no standard intern OA; no intern system design; bottleneck: resume · ~10–15%; onsite Cincinnati OH or Lynn MA; export-controlled U.S. Person; 3.0 GPA hard
- **Outside the resume:** Apply in the first wave of this 2026-08-17 posting; answer U.S. Person honestly on Workday; a GE Aerospace / UMich alumni referral (HM > recruiter > engineer); prep STAR plus a lock-free/`processBlock` walkthrough and one data-pipeline story
