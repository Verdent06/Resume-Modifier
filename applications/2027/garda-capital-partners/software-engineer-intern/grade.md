# Software Engineer Intern at Garda Capital Partners

## Verdict

- **Score:** 8.0 / 10 (2 demerits — 0 emergency, 0 major, 2 minor)
- **Eligibility:** eligible — pursuing B.S. CS (and Economics), Expected May 2028; Summer 2027 intern is a rising senior; JD has no class-year window beyond a listed degree
- **Track:** full-stack (FI relative-value / front-office-risk R&T flavor; no track divergence)
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- Python+SQL show up in work: MCFN Flask/ETL on EC2, Vylet asyncpg SQL validation, SignalWeaver FastAPI/Postgres/pgvector — the intern language floor is not Skills-only.
- C++ lock-free audio-thread work (MemoryPool slab, SPSC FIFO) plus a TypeScript dashboard bullet cover the statically typed ask.
- Binding ding: Granular cannot be sized (no latency number), and Vylet’s header still reads founder-GTM before the SQL/debug.

### Demerits

- **minor** · `Granular Synthesizer Plugin` · metric-free — MemoryPool and SPSC FIFO bullets never land a latency, callback-budget, or xrun number; the C++ systems proof this screen needs cannot be sized in 30 seconds.
- **minor** · `Vylet` · GTM founder header — The tagline still leads with $1,500 MRR and PE/search-fund product, so a rushed screen can bucket the entry as founder-GTM rather than the SQL DAL and defect-fix engineering the intern floor tests.

### Misreads

- A rushed screen could bucket Granular as hobby audio DSP rather than the C++ systems sample, because the lead has no measured performance witness.
- Vylet’s MRR/search-fund header can re-bucket the page as a founder/GTM story after a strong Python production top.

### Interview angles

- **Lead with:** MDC sole-ownership Flask REST + Pandas ETL (campaign-finance data, ~800 hours / 400 PACs); Vylet injection-safe SQL DAL and the 79%→89% name-collision fix (unaided debug); Granular zero-allocation `processBlock()` / lock-free SPSC; SignalWeaver FastAPI p50/p99 and pgvector search (research assistant, not investment advice).
- **Defend:** No trading-desk internship and no FI relative-value book — CS + Economics plus financial-data systems is the honest plus *(out of rails: Vylet GTM founder header — canonical tagline cannot be rewritten)*. Granular has no latency/xrun number in the pool — narrate the real-time safety constraint (no heap, no mutex on the audio thread) as the ship gate *(out of rails: Granular latency/callback/xrun metric — pool is architecture/capacity/release-audit only)*. Gemini embeddings on Vylet are storage next to SQL validation, not an ML intern seat.
- **Depth prep:** Timed Python + SQL and DS&A until a medium is a ~20-minute solve (resume + OA bottleneck; OA platform unpublished). Walk Flask/EC2 delivery, asyncpg timestamp validation, C++ MemoryPool/SPSC/memory-ordering, and SignalWeaver FastAPI/pgvector instrumentation. OOP + OS talking points from coursework and the audio-thread constraints.

## Likelihood

- **Resume screen:** High — Python+SQL in production bullets, C++ and TypeScript in work, GPA 3.66, eligible May 2028; two minors dent polish, they do not flip a Greenhouse human screen.
- **Overall hire odds:** Medium — B-tier hedge-fund intern funnel (~5–8%) with resume + unpublished OA as the bottleneck. The resume can buy the OA; timed coding (and possibly SQL/math) is what eliminates. Not Citadel/HRT prestige, not a no-pile.
- **Funnel filters:** Greenhouse 6146213004 → resume screen → candidate-reported SWE intern OA (platform unpublished; reports mix timed math/verbal with SQL/coding) → tech · Light intern sys design · Bottleneck: resume + OA · NYC onsite Summer 2027.
- **Outside the resume:** Timed Python/SQL plus DS&A; apply in this opening wave; no fake FI desk story in the OA or screens.
