# Software Engineer Intern at Valeo

## Verdict

- **Score:** 5.0 / 10 (5 demerits — 0 emergency, 1 major, 2 minor)
- **Eligibility:** eligible — JD requires a current Bachelor's in CE, SWE, or related field; B.S. Computer Science and Economics, Expected May 2028, is related. No class-year knockout. Visa/sponsorship not stated on REQ2026076575.
- **Track:** full-stack + automotive SDV / embedded C++ middleware / hardware-software / ISO 26262
- **Pipeline:** section-fix · exit: ship (user-binding: Granular stays Project)

## Screen Review

### First read

- Page opens on CaseStudyPrep.AI (Angular / RxJS / Whisper) then MDC ETL and Vylet LangGraph — a 7-second Troy scan can clock a web intern.
- Lock-free C++ (MemoryPool, SPSC FIFO, processBlock, CMake real-time checklist) is present and deep, but last under Projects.
- Binding dings: differentiator below the fold; Granular never sizes a real-time outcome; Linux never appears.

### Demerits

- **major** · `resume` · lead off-axis — Opens on a Voice AI / Angular co-op, then campaign-finance ETL and LangGraph; lock-free C++ sits last under Projects, so a skim reads web intern.
- **minor** · `Granular Synthesizer Plugin` · metric-free — Four lock-free / scheduler / CMake bullets never size callback time, xruns, or CPU. Architecture is clear; a systems screener cannot tell whether the real-time claims held.
- **minor** · `resume` · no Linux evidence — JD work is C/C++ and Python on embedded and Linux platforms; Linux appears in neither Skills nor any bullet. Only OS on the page is Granular's macOS universal binary.

### Misreads

- C++ under Projects after two web/data Experience entries can be bucketed as hobby audio, not SDV-middleware discipline.
- Missing Linux plus a macOS-universal CMake story can look like the candidate has never touched the JD's platform.

### Interview angles

- **Lead with:** Granular lock-free C++ (MemoryPool, SPSC, scheduler, CMake real-time audit) as the hardware-adjacent systems story — even though it is under Projects. Then MDC Flask REST on EC2 and CaseStudyPrep sized debug (27% upload-failure recovery, <5ms / 60 FPS).
- **Defend:** Recruiter may say the page leads off-axis because C++ is in Projects — do not re-section; Granular is a personal VST3/AU plugin (no titled role, no dates) and stays a Project per `resume.md` I.5. Script the 30-second C++ walk so the skim ding dies in conversation. Granular has no callback-latency / xrun / CPU number *(out of rails: pool has architecture sizes only; swap sets cannot invent a runtime metric)*. Linux is absent *(out of rails: not in skills inventory, swap sets, or any live pool bullet)* — do not claim Linux; say Docker/EC2/CMake honestly. Do not claim ISO 26262, AUTOSAR, or QNX.
- **Depth prep:** C++ atomics / acquire-release / zero-alloc hot paths; walk one Python debug story (CaseStudyPrep S3 URL recovery or Vylet name-collision). This loop is a verbal project interview, not a LeetCode OA.

## Likelihood

- **Resume screen:** Medium — C++ and Python are on the page with a real Flask API and sized debug, but the SDV-fit C++ is below the fold on a resume-gated (no-OA) screen, so a skim can pass this over as a web intern.
- **Overall hire odds:** Medium — no OA, so the usual intern killer is absent; remaining 2–3 Easy rounds still filter on a verbal C++/debug walk-through and behavioral. Peer funnel is ~15–25%.
- **Funnel filters:** Workday resume screen → recruiter/HM phone → Easy verbal technical (C/C++, Python, APIs, debug). No standard intern OA. No intern system design. Nearby Troy intern mirrors often add a no-sponsorship knockout on the apply form; this req does not print one. Mid-size / non-tech-tech is resume-first (`recruiting.md` Part I §5).
- **Outside the resume:** A Valeo Troy or Michigan-auto referral (HM > recruiter > engineer > cold). Walk the lock-free C++ and one debug story; intern behavioral is a filter round.
