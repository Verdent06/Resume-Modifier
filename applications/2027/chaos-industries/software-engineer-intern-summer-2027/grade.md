# 2027 Summer- Software Engineer Intern at CHAOS Industries

## Verdict

- **Score:** 9.0 / 10 (1 demerit — 0 emergency, 0 major, 1 minor)
- **Eligibility:** eligible — Expected May 2028; Summer 2027 is after sophomore / rising junior; JD requires currently pursuing BS/MS in CE/CS/Math/Physics/related with no class-year gate; B.S. Computer Science
- **Track:** full-stack + real-time / embedded-adjacent / defense sensor-sync / CDN
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- One-page UMich CS intern resume: real-time Voice AI co-op leads, lock-free C++ plugin in slot two, then Flask/AWS delivery, Dockerized production, and pgvector/React storage+frontend — generic SWE screen clears and the CDN differentiator is visible in the top half.
- C++ and Python are through use; Angular/RxJS, Flask/EC2, React, Docker, and pgvector cover frontend, APIs, cloud, and storage. No Java, JAX, C-as-C, or firmware invented.
- Binding ding: Granular is deep real-time C++ with no sized runtime outcome, so a skim can read hobby DSP instead of constraint-driven systems work.

### Demerits

- **minor** · `Granular Synthesizer Plugin` · metric-free — MemoryPool slab and lock-free SPSC prove real-time C++ discipline, but nothing sizes the plugin — no callback latency, xrun rate, CPU budget, or user/scale number

### Misreads

- A rushed defense skim can file this as "voice-AI / PE lead-gen intern plus hobby synth" before the lock-free C++ and shipped Python/storage land.

### Interview angles

- **Lead with:** Granular zero-alloc `MemoryPool` and lock-free SPSC (deterministic C++ under a callback constraint); CaseStudyPrep Web Worker / 5ms / 60 FPS and 27% upload-failure recovery; MDC sole-engineer Flask REST on EC2 plus 400-PAC ETL
- **Defend:** Granular has no callback-latency / xrun / CPU number on the page. *(out of rails: full 6-bullet pool has no allowlisted runtime impact metric; 16-voice / 64-slot / 8192-LUT / 2^17 swaps do not satisfy the ask; swap sets cannot invent one.)* No Java, JAX, C-as-distinct, firmware, MCU, RTOS, ROS, or oscilloscope work. Vylet is PE lead-gen / LangGraph — production Docker/Redis, not a defense internship. SignalWeaver is financial research storage+UI, not CDN sync
- **Depth prep:** C++ real-time (slab allocators, SPSC, `processBlock` constraints); Python production (Flask/EC2, Docker); storage (pgvector/Postgres) and frontend (Angular/React) for the generic SWE screen. Live tech bar is unpublished — do not assume HackerRank/CodeSignal. Be ready to relate Granular + group delivery to embedded + hardware + collaboration without claiming MCU firmware

## Likelihood

- **Resume screen:** High — eligible class year, C++ in bullets with lock-free depth as #2, Python shipped on AWS, frontend/storage/cloud through use; one minor dents the scan, it does not flip it to a no
- **Overall hire odds:** Medium — no published OA, so most front-end elimination is this PDF, then a thinly documented recruiter → live tech → panel loop at an Unrated defense-tech intern bar. Converting depends on walking constraints honestly and not overselling firmware
- **Funnel filters:** Greenhouse apply → human resume screen (binding; OA unpublished) → recruiter ~30 min → live tech → panel · U.S. Person / export-control form questions (clearance not necessarily required) · El Segundo onsite / relocate · no class-year gate
- **Outside the resume:** Apply in the first wave (posted 2026-09-08); packet sample or short note tying experience to embedded + hardware + group work (coach-owned, not this resume); form email `verdent06@gmail.com`; U.S. citizen **Yes**, sponsorship **No**, willing to relocate to El Segundo **Yes**. No CHAOS contact in `network.md`
