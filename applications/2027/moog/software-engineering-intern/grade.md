# Intern, Software Engineering at Moog Inc.

## Verdict

- **Score:** 9.0 / 10 (1 demerits — 0 emergency, 0 major, 1 minor)
- **Eligibility:** eligible — GPA 3.66 ≥ 3.0; B.S. CS + Economics at UMich, Expected May 2028 (still enrolled Summer 2027); JD has no class-year exclusive; U.S. citizen vs export-controlled U.S. person
- **Track:** full-stack + avionics / flight-systems / mission-critical military-aircraft software
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- One-page UMich CS intern resume: MDC Flask REST on AWS EC2 leads, Granular C++ lock-free / zero-alloc / CMake real-time audit sits in slot two, then a real-time co-op and FastAPI + Docker / GitHub Actions CI — on-axis for Mineral Wells MAG whose bottleneck is the resume, not an OA.
- C++ and Python are proven in bullets; AWS, Docker, and GitHub Actions are through use; class year and GPA are in window; no Ada / C / Java / C# / MATLAB invented.
- Binding ding: Granular has no sized runtime metric (latency / xrun / CPU).

### Demerits

- **minor** · `Granular Synthesizer Plugin` · metric-free — CMake real-time safety audit, zero-alloc `processBlock`, and lock-free SPSC are the MAG systems transfer, but none sizes callback latency, xruns, or CPU

### Misreads

- A rushed avionics skim can file this as "web intern plus hobby audio, no flight software" before the lock-free C++ and shipped Python / CI land.

### Interview angles

- **Lead with:** Granular CMake real-time safety audit, zero-allocation `MemoryPool`, lock-free SPSC (deterministic C++ / V&V-adjacent); MDC sole-ownership production Flask API on EC2; SignalWeaver pytest + GitHub Actions / Docker Compose; CaseStudyPrep 27% upload-failure recovery and Web Worker / <5ms / 60 FPS
- **Defend:** Granular has no xrun/latency/CPU metric on the page *(out of rails: pool has no verbatim impact-metric bullet; swap sets cannot invent one)* — script the audio-thread constraints and what you would measure next. Do not claim a clearance, a flight-display internship, or Ada/C/Java/C#/MATLAB. Voice-AI co-op is intern delivery / real-time, not an avionics claim. Export-control is U.S. person / citizenship on the form, not a clearance-in-hand line
- **Depth prep:** C++ real-time (slab allocators, SPSC, `processBlock` constraints, CMake release gates); Python production (Flask/EC2, Docker, pytest CI); integration and test-plan stories; STAR on teamwork, communication, and desire to learn (U.S. Internship Program blurb). No published intern OA on this Mineral Wells req — expect recruiter phone then HM project walk

## Likelihood

- **Resume screen:** High — eligible CS + 3.66 GPA on the page, C++ proven in bullets in slot two, Python/Flask API + FastAPI/CI spine, one clean page; the metric ding does not flip a no
- **Overall hire odds:** Medium — C-tier defense/avionics funnel is resume-first then Easy recruiter + HM project walk (~20–25%); the page clears the screen, but Mineral Wells onsite relocate and export-control U.S. person are form knockouts this PDF cannot carry, and the HM still has to hear real-time and integration live
- **Funnel filters:** Workday `moog.wd5` resume screen (binding; posting closes 2026-10-01) → recruiter phone (enrollment, GPA 3.0, U.S. person, Mineral Wells relocate) → HM/technical project walk → possible panel · Easy · No published intern OA on this req · No intern sys design · Bottleneck: resume · ~20–25%
- **Outside the resume:** Apply before the 2026-10-01 Workday close; form email `verdent06@gmail.com`; answer U.S. person / citizenship honestly; confirm Mineral Wells relocate. Prep a lock-free `processBlock` walkthrough plus one shipped API/integration story
