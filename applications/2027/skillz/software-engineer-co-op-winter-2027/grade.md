# Co-op, Software Engineer at Skillz Inc

## Verdict

- **Score:** 10.0 / 10 (0 demerits — 0 emergency, 0 major, 0 minor)
- **Eligibility:** ineligible — enrolled CS bachelor's and GPA 3.66 ≥ 3.3 clear those floors; program begins Jan 11 2027 so graduate-by is Jan 11 2028; Expected May 2028 is after that date. Honest Greenhouse answer on the combined enrollment/graduation Yes/No is **No**.
- **Track:** full-stack + payments-infra / real-money gaming platform
- **Pipeline:** 1 cycle(s) · exit: zero_demerits

## Screen Review

### First read

- MDC leads with a quantified campaign-finance ETL (~800 hours / 400 PACs) then a production Flask REST API on AWS EC2 and stakeholder-scoped sole-engineer delivery — generic intern SWE plus finance-adjacent shipping registers immediately.
- Vylet in the lead window carries a Dockerized pipeline (30x), a named production defect (79%→89%), and injection-safe SQL timestamp freshness — data-integrity analog for payment/payout backends, not a Skills-line Java claim.
- SignalWeaver closes the full-stack spine: FastAPI REST (9.1s p50 / 15.2s p99), React/TypeScript dashboard, Docker Compose + GitHub Actions CI with pytest. Binding ding on the PDF: none. Binding ding on the *application* is the graduation-window form question.

### Demerits

No demerits — clean screen.

### Misreads

- None material on the page — it reads as Python/TypeScript full-stack with financial-data and API depth, not an ML-research intern and not a Java/Go payments specialist. A form reviewer who never opens the PDF will still auto-reject on the graduation window.

### Interview angles

- **Lead with:** MDC sole-engineer Flask REST on AWS EC2 + Requests/Pandas ETL (~800 hours / 400 PACs); Vylet Docker/Redis/Celery pipeline, 79%→89% name-collision fix, and injection-safe SQL timestamps; SignalWeaver FastAPI REST + React/TypeScript + GitHub Actions/pytest; CaseStudyPrep Angular/RxJS S3 presigned-URL recovery (27% upload-failure)
- **Defend:** No Java or Go — inventory is Python/TypeScript/SQL (C++ is Skills-only); narrate ramp on JVM/Go if the team is Java/Go-heavy, do not invent them. Flask API line has no QPS/latency — walk the 5-month MCFN sole-engineer contract. SignalWeaver 9.1s p50 is batch research latency, not a deposit-auth SLA. No PCI, card rails, payment-processor SDKs, or Skillz platform experience — analog only. CaseStudyPrep (Dec 2025–May 2026) overlaps MDC (Jan–May 2026) — explain honestly. Graduation window: May 2028 is after Jan 11 2028 — do not argue the form into a Yes
- **Depth prep:** Easy–Med DS&A if a HackerRank analog is issued (`companies.md`: intern OA unpublished; full-time analog is HackerRank); Flask/REST + EC2 deploy; SQL freshness/validation (Vylet asyncpg); Docker/Celery + GitHub Actions/pytest; production-debug STAR (Vylet name-collision, CaseStudyPrep expired S3 URLs); recruiter screen on Las Vegas 5-day onsite + university confirmation of the full Jan 11–Aug 20 term

## Likelihood

- **Resume screen:** High — one page, GPA 3.66, Python/TypeScript/SQL through use, REST, AWS, Docker, CI, Git, production debug, payments-adjacent data integrity; a human C-tier screen should not bounce this PDF
- **Overall hire odds:** Low — binary knockout on the graduation window (`recruiting.md` Part I §1) dominates. If that form is answered No, the resume never gets a fair read. Among *eligible* applicants the resume-bottleneck estimate is ~15–25%; that does not apply here
- **Funnel filters:** Greenhouse resume (bottleneck) → recruiter phone → HackerRank (full-time analog; intern OA unpublished) · no intern sys design · ~15–25% among eligible · GPA 3.3 · enrolled at start **and** graduate ≤1 year after program begins · onsite Las Vegas 5 days/40h
- **Outside the resume:** Answer the graduation-window question **No**. Apply in the first wave (first_published 2026-09-03) only if still submitting despite the knockout. No Skillz contact in `network.md`. Do not invent Java/Go/PCI
