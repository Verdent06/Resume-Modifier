# 2027 Spring Co-op - Global Technology & Solutions - Software Engineer (SWE) at TD Bank

## Verdict

- **Score:** 10.0 / 10 (0 demerits — 0 emergency, 0 major, 0 minor)
- **Eligibility:** eligible — enrolled 4-year CS, Expected May 2028 matches JD preferred, GPA 3.66 ≥ 3.2 preferred; Spring 2027 / Winter Term 2 is in window
- **Track:** full-stack + bank-IT/GTS + applied-AI
- **Pipeline:** 1 cycle(s) · exit: zero_demerits

## Screen Review

### First read

- One-page UMich CS+Econ intern resume: production Flask REST on AWS EC2, TypeScript/React plus Angular through use, Docker/CI, and a named debug (79%→89%; 27% S3 recovery) — this is a generic GTS SWE scan, not ML-research.
- Bank-IT / messy-data work sits in the lead window (campaign-finance PAC pipeline; PE/search-fund product; financial-research platform). Applied-AI interest is visible (LangGraph, on-device VAD) without taking the spine.
- Binding ding: none at screen. Java and Jira are preferred, not demonstrated — honest omission, not a fabrication.

### Demerits

No demerits — clean screen.

### Misreads

- A rushed screener matching “Java / Jira preferred” may bucket this as a Python intern who merely collided with Angular, and miss the owned React/TypeScript dashboard plus the JS-family debug story.
- LangGraph + Voice AI + VAD can read as an AI-product intern if the reader skips the MDC Flask/ETL lead.

### Interview angles

- **Lead with:** MDC Requests + Pandas ETL (~800 hours / 400 PACs) then Flask REST on EC2 to MCFN; Vylet Dockerized LangGraph + name-collision debug 79%→89% + injection-safe SQL freshness; CaseStudyPrep Angular/RxJS S3 recovery (27%) and Silero VAD / ONNX (40% inference-cost cut); SignalWeaver FastAPI + React/TypeScript + Docker Compose / GitHub Actions pytest.
- **Defend:** No Java and no Jira — inventory is Python / TypeScript / SQL; narrate ramp willingness on JVM or Jira if the team uses them, do not invent. Angular is a MIME-rejection aside, not owned UI; the owned JS surface is SignalWeaver’s React/TypeScript dashboard. AI is required *interest*, not an ML intern seat — LangGraph and on-device VAD are the honest proof; do not lead with LoRA. Flask API line has no QPS — walk the 5-month sole-engineer MCFN contract. Mount Laurel is 4 days onsite; confirm the relocation stipend from Ann Arbor / Northville.
- **Depth prep:** Unpublished intern OA — treat the recruiter-scheduled interview as STAR (Impact, Growth, Culture of Care) plus a short JavaScript-family and REST walkthrough. Easy–Med DS&A if a coding screen appears (FT analog: Java/REST; do not invent Java). Flask/EC2 deploy; React/TypeScript dashboard; RxJS/S3 recovery; Docker + GitHub Actions/pytest. Not intern sys design.

## Likelihood

- **Resume screen:** High — eligible May 2028 / 3.66 GPA, production Flask REST on AWS, TypeScript/React plus Angular through use, Docker/CI, and applied-AI interest on a full-stack spine clear this C-tier resume-first GTS gate.
- **Overall hire odds:** Medium — TD GTS intern bottleneck is the resume (~15–25%); the published next step is a recruiter-scheduled interview with no named OA. The page should get a read; remaining risk is a cold Workday apply, Mount Laurel 4-day onsite from Michigan, a behavioral filter, and stack talk that may wander into Java they correctly did not claim.
- **Funnel filters:** Workday `TD_Bank_Careers` R_1509773 resume screen (bottleneck) → recruiter reaches out to schedule an interview (JD) · intern OA unpublished · no intern sys design · ~15–25% · May 2028 preferred · 3.2 GPA preferred · on-site Mount Laurel 4 days/week · apply by 2026-10-11
- **Outside the resume:** Apply in this first-wave window (posted 2026-09-12, close 2026-10-11). Confirm relocation-stipend / Mount Laurel 4-day onsite. Short STAR set (MDC stakeholder/cloud, Vylet defect, CaseStudyPrep S3 recovery). Do not invent Java or Jira. No TD contact in `network.md`.
