# Software Developer Intern at Kimley-Horn

## Verdict

- **Score:** 6.0 / 10 (4 demerits — 0 emergency, 1 major, 1 minor)
- **Eligibility:** eligible — Expected May 2028; pursuing B.S. CS; legally authorized to work in the U.S. without sponsorship; available in-office Cary/Raleigh Summer 2027
- **Track:** full-stack
- **Pipeline:** 8 cycle(s) · exit: writer_peak

## Screen Review

### First read

- MDC opens with a production Flask REST API on AWS EC2 plus an ETL that cut ~800 hours of PAC research — real API/deploy ownership, titled Data Engineer.
- Vylet is a live Dockerized product ($1,500 MRR, Redis/Celery, SQL DAL, 79%→89% qualification fix) — production and iterative bug-fix, not a web UI.
- Angular/RxJS (CaseStudyPrep) and the React/TypeScript + FastAPI + Postgres + Docker/CI app (SignalWeaver) are on the page, below the fold.

### Demerits

- **major** · `resume` · lead off-axis — Experience opens Data Engineer + LangGraph founder; Angular and the React/FastAPI/Postgres app sit third and in Projects, so a 30-second Angular full-stack screen sees no front end in the top half.
- **minor** · `CaseStudyPrep.AI` · Angular proof framed as voice-AI audio-DSP — the Angular/RxJS bullets are S3/MIME/Web Worker audio work; a T&I intern screen has to translate that into internal-web product language.

### Misreads

- A rushed T&I screen can file this as a data/AI-pipeline intern and bounce before reaching the Angular co-op and SignalWeaver web stack.

### Interview angles

- **Lead with:** MDC Flask REST + EC2 delivery to MCFN; SignalWeaver React/TypeScript dashboard + FastAPI + Postgres + Docker/GitHub Actions; CaseStudyPrep Angular/RxJS upload-path reliability (27% failure → recovered)
- **Defend:** C#/.NET and MS SQL Server are not on the page *(out of rails: candidate inventory has no C#/.NET; no pool bullet names them)* — narrate Python/TypeScript OOP + production APIs and a willingness to ramp on .NET; Angular is MIME/RxJS production work, not component/routing depth *(out of rails: CaseStudyPrep pool has no Angular UI-build bullet)*; lead-entry order *(out of rails: all three Experience leads were graded major; SignalWeaver cannot move into Experience)*
- **Depth prep:** Flask/FastAPI REST and EC2 deploy; Angular RxJS + S3 presigned-URL fault tolerance; Postgres/SQL DAL; Docker Compose + GitHub Actions; timed LC mediums; STAR stories on MCFN stakeholder scoping and the Vylet 79%→89% bug-fix (collaboration/iterative delivery)

## Likelihood

- **Resume screen:** Medium — REST + front end + Postgres + deploy, Angular and TypeScript in bullets, contracted/live production, and Expected May 2028 are on the page; the first scan is Data Engineer / agentic founder, not an Angular web intern
- **Overall hire odds:** Medium — resume-first consultancy intern funnel (not OA-gated); posting is ~2 days old (early-pool leverage); loop still filters on coding plus a teamwork/client-service behavioral; C#/.NET is a ramp
- **Funnel filters:** iCIMS resume screen → recruiter/phone → technical (web stack + DS&A) → behavioral · no standard OA documented · in-office Cary/Raleigh · no employer sponsorship
- **Outside the resume:** Apply in this early window; referral into Technology & Innovation beats another polish pass; prep DS&A plus Angular-upload and Flask/FastAPI walkthroughs; journal collaboration stories — the page reads sole-engineer/founder
