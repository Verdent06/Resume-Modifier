# Software Engineer, Intern (Summer or Winter) at Stripe

## Verdict

- **Score:** 10.0 / 10 (0 demerits — 0 emergency, 0 major, 0 minor)
- **Eligibility:** eligible — preferred ≥1 year university is met; no class-year or return-to-school knockout on this posting; work-auth yes / sponsorship no; locations acceptable
- **Track:** full-stack + fintech-backend / payments infrastructure
- **Pipeline:** 1 cycle(s) · exit: zero_demerits

## Screen Review

### First read

- MDC leads with a quantified campaign-finance ETL (~800 hours / 400 PACs) then a production Flask REST API on AWS EC2 and stakeholder-scoped sole-engineer delivery — generic intern SWE plus finance-adjacent shipping registers immediately.
- Vylet in the lead window carries Dockerized LangGraph, a named production defect fix (79%→89%), and a LangSmith eval harness (50%→90%) — tests/debug through use, not Copilot slogans.
- SignalWeaver closes the payments/fintech flavor without becoming notebook ML: FastAPI REST (9.1s p50 / 15.2s p99), React/TypeScript dashboard, Docker Compose + GitHub Actions CI with pytest. Binding ding: none at screen.

### Demerits

No demerits — clean screen.

### Misreads

- None material — the page reads as Python/TypeScript full-stack with financial-platform depth, not an ML-research intern (Payments Foundation Model intern *examples* are JD flavor, not this resume) and not a Ruby/Java Stripe specialist.

### Interview angles

- **Lead with:** MDC sole-engineer Flask REST on AWS EC2 + Requests/Pandas ETL and MCFN stakeholder scoping; SignalWeaver FastAPI REST + React/TypeScript + Docker/CI/pytest; Vylet Docker/Redis/Celery launch and 79%→89% qualification fix; CaseStudyPrep Angular/RxJS S3 recovery (27% upload-failure)
- **Defend:** No Ruby, Java, Go, Scala, or listed Javascript — inventory is Python/TypeScript (C++ is Skills-only); narrate ramp willingness on Stripe's posted languages, do not invent them. Flask API line has no QPS/latency — walk the 5-month sole-engineer contract and what you would measure. SignalWeaver 9.1s p50 is batch research latency, not a payment-auth SLA. LoRA/fine-tuning and audio-DSP are off the page on purpose; do not re-center the loop on ML research or plugin DSP. No Snowflake, Databricks, Copilot, Fusion, Tableau.
- **Depth prep:** File framing is **No OA**; intern-cycle reports describe HackerRank implementation (60–90 min, log/data processing, not pure LC) **[directional]**. Then Med–Hard tech screen + Integration/API (pagination, retries, idempotency; often open-book) + bug squash + HM behavioral. Flask/REST + EC2 deploy; Docker Compose + GitHub Actions/pytest; debug stories (Vylet name-collision, CaseStudyPrep presigned-URL recovery).

## Likelihood

- **Resume screen:** High — one page, junior Expected May 2028, Python/TypeScript through use, REST, React, Docker, CI, Git, stakeholder delivery, finance-adjacent platforms
- **Overall hire odds:** Low — A-tier ~2–3% with tech rounds as the intern bottleneck; page likely clears, then Med–Hard Integration/bug-squash still filter most of the class
- **Funnel filters:** Greenhouse resume screen → **No OA** on `companies.md` (intern-cycle HackerRank implementation OA is a web delta) → tech screen · Integration/API · bug squash · HM behavioral · Bottleneck: tech rounds · ~2–3% · US work-auth yes / no sponsorship · SF / Seattle / NY · ≥50% in office
- **Outside the resume:** Apply in the first wave (first_published 2026-08-31); do not submit from this agent; timed implementation OA if invited; Integration/API practice (docs, retries, idempotency); short STAR set (MDC stakeholder delivery, Vylet defect you owned); do not treat Ruby/Java fluency as required proof
