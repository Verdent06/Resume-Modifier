# 2027 Aerospace Software Engineer Undergraduate Intern at The Aerospace Corporation

## Verdict

- **Score:** 9.0 / 10 (1 demerit — 0 emergency, 0 major, 1 minor)
- **Eligibility:** eligible — GPA 3.66 ≥ 3.0; enrolled CS bachelor (Expected May 2028); Summer 2027 is after sophomore / rising junior; 10 weeks + return to Bachelor's Fall 2027; U.S. citizen / clearance-eligible
- **Track:** full-stack + flight-software IV&V / real-time embedded / mission-critical
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- One-page UMich CS intern resume: C++ lock-free / zero-alloc / CMake real-time **plugin** leads, then a production Flask REST API on AWS EC2, then a real-time co-op, then FastAPI + Docker / GitHub Actions CI — on-axis for VSSD flight-software IV&V whose bottleneck is the resume, not an OA.
- C++ and Python are proven in bullets; Java, C, CAD, ROS, MCU firmware, rocket club, and a clearance are not invented. Class year and GPA are in window.
- Binding ding: Granular has no sized runtime metric (latency / xrun / CPU).

### Demerits

- **minor** · `Granular Synthesizer Plugin` · metric-free — CMake real-time safety audit, zero-alloc `processBlock`, lock-free SPSC, and 16-voice stealing are the C++/V&V signal, but none sizes callback latency, xruns, or CPU

### Misreads

- A rushed FFRDC skim can file this as "hobby audio intern, no flight software" before the lock-free C++ / release-gate checklist and shipped Python land.

### Interview angles

- **Lead with:** Granular CMake real-time safety audit, zero-allocation `MemoryPool`, lock-free SPSC, 16-voice stealing (deterministic C++ / V&V-adjacent); MDC sole-ownership production Flask API on EC2; SignalWeaver pytest + GitHub Actions / Docker Compose; CaseStudyPrep Web Worker / 27% upload-failure debug
- **Defend:** Granular has no xrun/latency/CPU metric on the page. *(out of rails: pool has no verbatim impact-metric bullet; swap sets cannot invent one.)* No Java or C as a language. No CAD, SolidWorks-as-flight-work, ROS, MCU firmware, rocket club, aerospace internship, or clearance in-hand. Voice AI co-op is intern delivery / real-time, not a flight-software claim
- **Depth prep:** C++ real-time (slab allocators, SPSC, `processBlock` constraints, CMake release gates); Python production (Flask/EC2, Docker, pytest CI); V&V / test-plan stories; light DS&A plus a short project presentation if the 2023 intern loop repeats. Do not fake embedded MCU, SIL/HIL, or 6DOF simulation

## Likelihood

- **Resume screen:** High — eligible class year and GPA, C++ in bullets with lock-free/CMake depth in the lead slot, Python shipped on AWS, Docker/GitHub Actions CI; one minor dents the scan, it does not flip it to a no
- **Overall hire odds:** Medium — no published OA, so most front-end elimination is this PDF, then 2–3 Easy–Med practical rounds at a ~10–15% B-tier FFRDC intern accept rate. U.S. citizenship / clearance eligibility is a hard form knockout this page cannot show
- **Funnel filters:** Workday apply → resume screen (binding) → recruiter/HM (citizenship, GPA, El Segundo 10-wk, transcripts) → technical project walk; intern reports live coding + short presentation **[directional]** · no named OA · no intern sys design · U.S. citizenship / Secret-eligible · GPA 3.0 · return to Bachelor's
- **Outside the resume:** Apply in the first wave (posted 2026-09-15); form email `verdent06@gmail.com`; U.S. citizen **Yes**, sponsorship **No**; upload unofficial transcript; confirm El Segundo 10 weeks and Fall 2027 enrollment. Temporary housing assistance is **not** available. No Aerospace contact in `network.md`
