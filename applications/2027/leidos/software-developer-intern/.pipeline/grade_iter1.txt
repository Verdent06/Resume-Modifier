━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
RESUME GRADING REPORT (DEMERIT MODEL)
Role: Software Developer Intern at Leidos
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

REVIEWER PERSONA
I screen CABA / Intel-sector intern packets at Leidos for Annapolis Junction. The PDF is the gate (no named OA). I look for an enrolled CS student who already ships in Python or C++, shows OOP and a real SDLC (design, implement, debug/test, reuse), and can obtain TS/SCI with Poly — not a Java faker and not someone claiming a clearance in hand. Mission-software adjacency means systems discipline in the top half, not a hobby-audio or notebook-ML lead.

DEFECTS  (machine-read by scripts/validate.py demerits — valid JSON, this block only)
```json
{
  "defects": [
    {"entry": "Granular Synthesizer Plugin", "severity": "minor", "defect": "metric-free", "why": "lock-free SPSC, zero-alloc processBlock, and a CMake real-time safety audit are the C++/OOP signal, but none sizes callback latency, xruns, or CPU"}
  ]
}
```

WHAT WOULD TAKE THIS TO THE NEXT LEVEL
- Granular's C++ depth is real, but a sized runtime number (callback latency, xrun rate, or CPU) would let a CABA screener weigh the systems work instead of taking the checklist on faith.

LIKELIHOOD ESTIMATE
─────────────────────
Resume Screen Pass:  High — eligible class year, Python shipped on AWS, C++ lock-free/CMake depth in slot two; one minor dents the scan, it does not flip it to a no
Overall Hire Odds:   Medium — no named OA, so most front-end elimination is this PDF, then an Easy 2–3 round project + STAR loop at a ~20–25% C-tier gov-IT intern accept rate. Citizenship and ability to obtain TS/SCI with Poly before start (MD interview at intern expense) are form/investigation knockouts this page cannot show.
What Moves the Needle (outside the resume): Apply in the first wave on Workday; answer citizenship Yes and clearance as ability to obtain, never in-hand; be ready to travel to Maryland for the poly investigation at own expense.
