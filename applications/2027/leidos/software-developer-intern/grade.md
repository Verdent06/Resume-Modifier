# Software Developer Intern at Leidos

## Verdict

- **Score:** 9.0 / 10 (1 demerit — 0 emergency, 0 major, 1 minor)
- **Eligibility:** eligible — Summer 2027 maps to rising junior / going into junior year; JD requires going into Junior or Senior
- **Track:** full-stack + mission-software / intel-cleared-gov-IT / CABA
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- One-page UMich CS intern resume: MDC Flask REST on AWS EC2 leads, then a deep C++ lock-free / zero-allocation / CMake plugin in slot two, then a titled SWE co-op (debug + web reliability), then FastAPI + Postgres + pytest CI — on-axis for a resume-gated CABA intern.
- Python and C++ are proven in bullets; AWS, Docker, PostgreSQL, Git, and pytest are through use; class year is in window; GPA 3.66; no Java invented; no clearance claimed in hand.
- Binding ding: Granular has no sized runtime metric.

### Demerits

- **minor** · `Granular Synthesizer Plugin` · metric-free — lock-free SPSC, zero-alloc `processBlock`, and a CMake real-time safety audit are the C++/OOP signal, but none sizes callback latency, xruns, or CPU

### Misreads

- A rushed CABA skim can file this as "web intern plus hobby audio, no Java" before the lock-free C++ and shipped Python land.

### Interview angles

- **Lead with:** MDC sole-ownership production Flask API on EC2 and stakeholder-scoped 5-month delivery (agile analog); Granular zero-allocation `MemoryPool`, lock-free SPSC, CMake VST3/AU real-time audit (OOP/architecture + release-gate analog); SignalWeaver pytest + GitHub Actions (BDD/test-automation / DEVOPS analog); CaseStudyPrep 27% upload-failure debug
- **Defend:** Granular has no xrun/latency/CPU metric on the page. *(out of rails: pool has no verbatim impact-metric bullet; swap sets cannot invent one.)* No Java — say Python and C++ are the interview languages; do not claim Java, Eclipse, Oracle, MongoDB, or PHP. *(out of rails: Java is absent from pool and Languages inventory.)* Clearance is **ability to obtain** TS/SCI with Poly before start — never Secret/TS/SCI/Poly in hand. Voice-AI co-op is intern delivery / web reliability, not an ML-research claim
- **Depth prep:** C++ real-time (slab allocators, SPSC, `processBlock` constraints, CMake release gates); Python production (Flask/EC2, Docker, pytest CI); light DS&A / OOP for a 2–3 round Easy loop; STAR on MCFN scoping and the CaseStudyPrep failure-rate debug; honest clearance-eligibility story (citizen, no clearance held, will sit the Maryland interview at own expense)

## Likelihood

- **Resume screen:** High — eligible class year, C++ in bullets with lock-free/CMake depth as the #2 entry, Python shipped on AWS; one minor dents the scan, it does not flip it to a no
- **Overall hire odds:** Medium — no named OA, so most front-end elimination is this PDF, then Easy project + STAR at a ~20–25% C-tier gov-IT intern accept rate. Citizenship and TS/SCI-with-Poly *eligibility before start* are form/investigation knockouts this page cannot show
- **Funnel filters:** Workday apply → resume screen (binding) → recruiter (citizenship, ability to obtain TS/SCI with Poly, Annapolis Junction onsite, summer 2027) · No OA (`companies.md`) · no intern sys design · U.S. citizenship · ability to obtain TS/SCI with Poly before start (MD in-person interview at intern expense)
- **Outside the resume:** Apply in the first wave (posted 2026-09-09); answer citizenship **Yes** and clearance as **ability to obtain**, never in-hand; confirm Annapolis Junction onsite and possible school-year part-time; do not treat $48,100–$86,950 as a locked intern salary (job-level guideline)
