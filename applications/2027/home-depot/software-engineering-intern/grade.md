# 2027 Summer Internship — Software Engineering at The Home Depot

## Verdict

- **Score:** 6.0 / 10 (4 demerits — 0 emergency, 1 major, 1 minor)
- **Eligibility:** eligible — Expected May 2028; intern May 17–Jul 30 2027 (returns Fall 2027); no class-year gate; GPA 3.66 ≥ 3.0 preferred
- **Track:** full-stack
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- CaseStudyPrep leads with Angular + RxJS fault-tolerant uploads and a Web Worker UI — the web-front-end preferred floor lands in the first 7 seconds.
- MDC and SignalWeaver back it with a production Flask REST API on EC2 (~800 hours saved) and a React/TypeScript + FastAPI ship with p50/p99 latency.
- Binding ding: Lyndbrook is still search-fund deal-sourcing on a retail product-team intern req, so the page looks padded after an otherwise on-axis spine.

### Demerits

- **major** · `Lyndbrook Capital` · reads as filler — PWSID entity database and acquisition shortlisting are still search-fund deal-sourcing, not web/product SWE; no UI, no production service, no Git/CI — filler on a retail product-team intern req.
- **minor** · `Technical Skills` · HTML/CSS skills-only — JD prefers HTML/CSS exposure; the token sits on the Languages line with no bullet that names markup or styling.

### Misreads

- A rushed screener could bucket this as a PE/search-fund data contractor (Vylet + Lyndbrook) instead of a product-team web intern — lead Angular and the React dashboard have to win the first pass.
- HTML/CSS on the Skills line can look like a checkbox with no markup story behind it.

### Interview angles

- **Lead with:** CaseStudyPrep Angular/RxJS upload reliability and Web Worker UI; MDC sole-engineer production Flask REST on EC2; SignalWeaver React/TypeScript dashboard + FastAPI latency
- **Defend:** Lyndbrook is a short data-engineering contract, not the core story *(out of rails: every pool bullet is PE deal-sourcing; omit blocked by min_entries 5 + no_bullet_deletion)* — pivot to MDC production API and CaseStudyPrep Angular. HTML/CSS not named in a bullet *(out of rails: no pool line mentions markup/styling; dropping it fails the language gate)* — point at Angular/React UIs. No jQuery, Java, Sentry, Snowflake, Databricks, Tableau, GCP, Copilot, Fusion, Kubernetes, or Go.
- **Depth prep:** Angular RxJS + S3 presigned URLs; React/TypeScript dashboard; Flask REST/EC2 deploy; Easy CoderPad arrays/strings/hash maps + basic SQL if OA fires; STAR on pairing, ambiguity, and customer-facing delivery (intern values)

## Likelihood

- **Resume screen:** Medium — Angular, React/TypeScript, production Flask REST with an 800-hour ETL win, and FastAPI latency numbers are enough for a C-tier retail intern screen; Lyndbrook still looks like padding.
- **Overall hire odds:** Medium — resume remains the binding gate (~15–25%); Easy CoderPad if it fires is not the hard part. Closing the filler entry would make this a High screen.
- **Funnel filters:** Workday CareerDepot resume screen (bottleneck) → possible 60–90 min CoderPad Easy–Medium (arrays/strings/hash maps/basic SQL) **[directional]** → recruiter/HM 2–3 rounds · Easy · no intern sys design on this posting · ~15–25%
- **Outside the resume:** Apply in the first Workday wave (posted 2026-08-31); LC Easy–Medium + basic SQL; STAR on pairing and customer-facing delivery mapped to intern values
