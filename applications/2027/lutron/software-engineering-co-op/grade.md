# Spring 2027 - Software Engineering Co-Op at Lutron Electronics

## Verdict

- **Score:** 8.0 / 10 (2 demerits — 0 emergency, 0 major, 2 minor)
- **Eligibility:** ambiguous — GPA 3.66 ≥ 3.20, CS degree, US citizen / no sponsorship, Philadelphia onsite yes; JD rising-junior-at-start vs sophomore spring for a May 2028 grad at January 2027
- **Track:** full-stack + smart lighting / automated shading / building-control application software
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- One-page UMich CS intern resume: MDC leads with a production Flask REST API on AWS EC2 plus a quantified PAC ETL — generic intern application SWE in Python, not an ML/PE-deal lead.
- CaseStudyPrep in the lead window carries quantified debug (27% upload-failure) and real-time UI-thread work (5ms / 60 FPS); Granular then shows lock-free C++ with a VST3/AU release; SignalWeaver adds a React dashboard plus pytest CI — on-axis for a lighting-control application-software co-op whose bottleneck is the resume.
- Binding dings: Granular has no runtime metric; Vylet's third-slot founder tagline still reads PE/search-fund.

### Demerits

- **minor** · `Granular Synthesizer Plugin` · metric-free — lock-free SPSC and zero-allocation `processBlock` are the C++/real-time analog, but neither bullet sizes callback latency, xruns, or CPU.
- **minor** · `Vylet` · PE-deal founder framing — third Experience is a PE/search-fund lead-sourcing founder role; debug-first bullets help, the tagline does not.

### Misreads

- A rushed Philadelphia skim can file this as "web/data intern plus hobby audio and a PE founder, no lighting-control software" before the lock-free C++ and shipped Python land.

### Interview angles

- **Lead with:** MDC sole-engineer Flask REST on AWS EC2 + Requests/Pandas ETL (~800 hours / 400 PACs); CaseStudyPrep 27% upload-failure debug and Web Worker real-time (5ms / 60 FPS); Granular zero-allocation `MemoryPool` and lock-free SPSC (deterministic constraints / release artifact); SignalWeaver React dashboard + pytest / GitHub Actions.
- **Defend:** Granular has no xrun/latency/CPU metric on the page *(out of rails: pool has no verbatim impact-metric bullet; a third Granular bullet overflows at 96% fill)*. Vylet tagline is PE/search-fund *(out of rails: header is fixed; omit drops below five entries; Lyndbrook is the same GTM lane)*. No Java, C#, firmware, or Lutron product names — say Python and C++ are the interview languages; ramp on the lighting/shading application stack. CaseStudyPrep title still says Voice AI — open with the RxJS/S3 debug and Web Worker, not the product category. Rising-junior-at-start: Expected May 2028; January 2027 start is sophomore spring — do not lie if they ask a binary.
- **Depth prep:** C++ real-time (slab allocators, SPSC, `processBlock` constraints); Python production (Flask/EC2, Docker, pytest CI); Angular/RxJS failure recovery; React/TypeScript dashboard; light DS&A if a coding screen appears; code-review / debug STAR for an Easy–Med loop. FTE analog (not this co-op): LC-easy pastebin then a light-scheduling-app design chat.

## Likelihood

- **Resume screen:** High — eligible GPA, Python shipped first, quantified debug and real-time in the lead window, C++ systems as the lead project; two minors dent the scan, they do not flip it to a no.
- **Overall hire odds:** Medium — no OA confirmed on 5616, so most front-end elimination is this PDF, then Easy–Med recruiter/tech at a ~15–25% C-tier intern accept rate. Rising-junior-at-start and Philadelphia 5-day onsite are form gates this page cannot show.
- **Funnel filters:** Jibe/iCIMS resume (binding) → unpublished intern OA · recruiter/HM project walk + STAR · Easy–Med · GPA 3.20 · CS/CE/SE · rising junior/senior/master's at start (if enforced) · US work auth · no sponsorship · Philadelphia onsite 5 days/week.
- **Outside the resume:** Apply in this first-wave window (posted 2026-09-25). Form email **`verdent06@gmail.com`**. Honest US citizen / no sponsorship and Philadelphia onsite **Yes**. If they enforce rising junior at January 2027 start, the radio may auto-reject — do not lie. No Lutron contact in `network.md`. Packet only from this agent — see `written-answers.md`.
