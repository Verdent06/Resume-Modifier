# Software Engineer Co-Op - Enterprise Finance Applications - Summer 2027 at Fifth Third Bank

## Verdict

- **Score:** 8.0 / 10 (2 demerits — 0 emergency, 0 major, 2 minor)
- **Eligibility:** eligible — Expected May 2028 + Summer 2027 maps to rising junior; JD asks rising juniors and seniors; GPA 3.66 ≥ 3.0 floor; no-sponsorship knockout is a separate gate and is cleared
- **Track:** full-stack + fintech-backend / bank-IT modernization (enterprise finance applications)
- **Pipeline:** 4 cycle(s) · exit: writer_peak

## Screen Review

### First read

- MDC leads with a production Flask REST API on AWS EC2 plus a quantified campaign-finance ETL (~800 hours / 400 PACs) — generic intern SWE plus finance-adjacent shipping registers immediately.
- CaseStudyPrep carries the JD-named Angular/TypeScript stack through an RxJS/S3 debug story; SignalWeaver closes the enterprise-finance flavor with FastAPI REST, a React/TypeScript dashboard, and Docker/CI.
- Binding ding: Angular is a MIME-rejection aside, not an owned UI, and two of the three MDC bullets are unquantified.

### Demerits

- **minor** · `MDC` · metric-free hook and architecture bullets — The lead Flask-on-EC2 bullet and the PAC-ranking engine bullet ship real systems with no numbers; only the ETL line is sized (~800 hours / 400 PACs), so a screener cannot tell how big the API or ranking work was.
- **minor** · `CaseStudyPrep.AI` · Angular only as MIME aside — The JD’s named frontend appears once as ‘WAV files Angular silently rejected’ inside an RxJS/S3 audio-debug bullet — no component, module, or UI ownership a micro-frontend team can probe.

### Misreads

- A rushed screener matching “Angular micro-frontends” may bucket this as a Python/Flask intern who merely collided with Angular, not someone who can sit on a Fifth Third frontend squad.
- The unquantified Flask/ranking lines can read as a student ETL with one strong data-pull rather than a production finance-ops API.

### Interview angles

- **Lead with:** MDC sole-engineer Flask REST on AWS EC2 + Requests/Pandas ETL (~800 hours / 400 PACs); SignalWeaver FastAPI REST + React/TypeScript dashboard + Docker Compose / GitHub Actions pytest; Vylet Dockerized pipeline and 79%→89% qualification fix; CaseStudyPrep RxJS/S3 recovery (27% upload-failure) as the honest Angular/TypeScript debug story
- **Defend:** Angular is MIME-aside, not owned UI *(out of rails: no pool bullet describes an Angular component, module, dashboard, or micro-frontend; swap sets cannot bridge React→Angular)*. No Java, Spring Boot, or Node.js — inventory is Python / TypeScript / C++ / SQL; narrate ramp willingness on JVM if the team is Spring-heavy, do not invent it. Flask API and PAC-ranking lines have no QPS/latency — walk the 5-month sole-engineer MCFN contract and what you would measure. CaseStudyPrep (Dec 2025–May 2026) overlaps MDC (Jan–May 2026); if asked, explain the real overlap honestly — do not invent “part-time” wording that is not in the pool. SignalWeaver 9.1s p50 is batch research latency, not a bank-API SLA. LoRA/fine-tuning and audio-DSP are off the page on purpose
- **Depth prep:** Easy DS&A if an unpublished assessment appears; Flask/REST + EC2 deploy; Angular/RxJS debug (presigned URLs, MIME) without claiming UI ownership; Docker Compose + GitHub Actions/pytest; STAR for stakeholder delivery (MDC) and production defect ownership (Vylet); Cincinnati onsite logistics from Ann Arbor/Northville

## Likelihood

- **Resume screen:** High — CS/Econ, 3.66, May 2028, production REST/AWS, TypeScript/React plus an Angular-stack co-op, and finance-adjacent delivery in the lead window clear this C-tier resume gate.
- **Overall hire odds:** Medium — Fifth Third’s intern bottleneck is the resume and the published loop is Easy with no intern sys design, so this page should get a read; remaining risk is a cold Workday apply in a ~15–25% funnel, a behavioral filter round, and stack talk that may wander into Java they correctly did not claim.
- **Funnel filters:** Workday `53careers` resume screen (bottleneck) → recruiter/HM → peer behavioral + project depth; intern OA unpublished · no intern sys design · ~15–25% · GPA 3.0 · no visa sponsorship · onsite Cincinnati · apply by 2026-10-09
- **Outside the resume:** Apply on Workday in this window (posted 2026-08-20, close 2026-10-09). No Fifth Third contact in `network.md`. Short STAR set (MDC stakeholder/cloud, Vylet defect). Do not treat Java or owned Angular UI as required proof.
