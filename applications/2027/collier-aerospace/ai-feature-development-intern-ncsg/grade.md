# AI Feature Development Intern - NCSG at Collier Aerospace

## Verdict

- **Score:** 8.0 / 10 (2 demerits — 0 emergency, 0 major, 2 minor)
- **Eligibility:** Eligible on class year — in-progress B.S. Computer Science (Expected May 2028) for Summer 2027; US citizen. **School/cohort risk:** JD copy seeks North Carolina State students for the NCSG intern cohort; candidate is University of Michigan (transcript will show it).
- **Track:** ai-ml + aerospace-CAE / HyperX .NET desktop (divergence)
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- Lead is a live LangGraph pipeline + LangSmith eval (30x, 50%→90%) and Gemini embeddings with SQL freshness — the JD's custom AI workflows and context-retrieval bar.
- CaseStudyPrep ONNX local inference (40% cost cut) plus Web Worker concurrency (5ms / 60 FPS), then Granular lock-free C++ (the OOP / thread-safety or-list), then SignalWeaver LoRA + pgvector. GPA 3.66 and a live GitHub.
- Binding dings: no C#/.NET; no MCP or JSON/XML through use. The NCSU/NCSG sentence is not on the PDF but is the real apply-form-adjacent filter.

### Demerits

- **minor** · `resume` · preferred C#/.NET absent — HyperX tooling is C#/.NET preferred. C++ in Granular and Python in Vylet/SignalWeaver satisfy the OOP or-list; a .NET desktop screener still sees zero C#.
- **minor** · `resume` · MCP and JSON/XML not shown — Responsibilities name MCP servers; required working knowledge names JSON/XML. Embeddings, SQL freshness, and pgvector are the retrieval analog, not MCP or those formats.

### Misreads

- A HyperX .NET screener skimming titles can bucket this as a PE/search-fund founder resume and miss that the work is agent orchestration, eval, local ONNX, and lock-free C++.
- A keyword pass for C#, MCP, or JSON/XML can file the page as a miss even though C++ and RAG-style retrieval are present.

### Interview angles

- **Lead with:** Vylet LangGraph + LangSmith eval + embeddings/SQL freshness as "custom AI workflows / context retrieval"; SignalWeaver LoRA (81%→96%) and pgvector (49ms p50) as local-model + RAG analog; CaseStudyPrep ONNX as running a smaller model locally.
- **Defend:** No C#/.NET in the pool — say so, then map Granular C++ (MemoryPool, SPSC atomics, zero-allocation audio thread) onto extending a large desktop OOP codebase. No MCP or JSON/XML bullets — map pgvector + Gemini embeddings onto RAG, not MCP. *(out of rails: pool has no C#, .NET, MCP, JSON, or XML bullet; swap sets cannot bridge)*
- **Depth prep:** Walk the eval harness (13 archetype labels, Pydantic gates, 50%→90%); ONNX VAD vs cloud Whisper; SPSC acquire/release vs a mutex; how you would wrap HyperX as an MCP tool you have not built yet. STAR for collaboration (CaseStudyPrep co-op). Honest UMich / not NC State if asked.

## Likelihood

- **Resume screen:** Medium — AI workflows, local models, RAG-style search, and C++ concurrency clear the technical screen if a human reads past the PE title; C# absence and the NCSU sentence dent it.
- **Overall hire odds:** Low — NCSG is a North Carolina Space Grant cohort the JD addresses to NC State students. Rippling does not ask school as a knockout, but the unofficial transcript will. If that gate is waived, funnel is resume-first C-tier (`companies.md` ~15–25%) with no published OA.
- **Funnel filters:** Rippling `5a8bf9a3-c4f5-4c5a-ba9a-188979106827`; US-citizenship radio; unofficial transcript required; intern OA unpublished. AI evaluations off on this posting.
- **Outside the resume:** Apply 2026-09-18 (posted 2026-09-16). No Collier contact in `network.md`. Do not claim NC State. Form email `verdent06@gmail.com`. See `written-answers.md`.
