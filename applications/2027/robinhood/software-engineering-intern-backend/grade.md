# Software Engineering Intern, Backend (Summer 2027) at Robinhood

## Verdict

- **Score:** 9.0 / 10 (1 demerits — 0 emergency, 0 major, 1 minor)
- **Eligibility:** eligible — Expected May 2028 is Spring 2028, inside Winter 2027 / Spring 2028
- **Track:** full-stack
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- Vylet leads with a named correctness defect (79%→89% qualification) then Redis/Celery workers and an injection-safe asyncpg/SQL freshness layer — production backend plus the transactional-data flavor this intern JD wants.
- SignalWeaver closes the spine: async FastAPI REST, PostgreSQL/pgvector, Docker Compose, GitHub Actions pytest. CaseStudyPrep adds AWS S3 fault-tolerance (27% upload-failure recovery).
- Binding ding: Granular is real C++ systems work with no sized latency, sitting off-axis for a Python/relational backend intern.

### Demerits

- **minor** · `Granular Synthesizer Plugin` · off-axis DSP, metric-free — slab alloc and lock-free SPSC are real, but no number a recruiter can size, and the intern JD tests Python + relational correctness, not JUCE.

### Misreads

- Granular without a number can read as hobby DSP rather than leftover systems depth, and a rushed pass that stops at "LangGraph" on the second Vylet bullet can still bucket the page as an agent intern.

### Interview angles

- **Lead with:** Vylet name-collision fix (79%→89%) + Redis/Celery cycle + asyncpg/SQL freshness; SignalWeaver FastAPI REST (9.1s p50 / 15.2s p99) + pgvector + Docker/CI pytest; CaseStudyPrep S3 presigned-URL recovery (27% upload-failure)
- **Defend:** Granular has no callback-latency number and is not brokerage infra *(out of rails: pool bullets 1–6 have no impact metric; anti-deletion kept the two-bullet floor)* — script the audio-thread rules if asked, then steer back to Vylet/SignalWeaver. No Django, Go, Kafka, Kubernetes, or Memcached on the page — inventory is Python/FastAPI, PostgreSQL, Redis, AWS/Docker; narrate ramp, do not invent. LangGraph is pipeline infra, not an ML-research lead.
- **Depth prep:** Med–Hard CodeSignal until **800+** is routine (`companies.md` bottleneck: OA bar); Python services, Redis/Celery backpressure, PostgreSQL/asyncpg and what a transaction means here; FastAPI + pytest/CI; one STAR on Vylet correctness and one on SignalWeaver FastAPI/Postgres path; recruiter "why Robinhood / fintech"

## Likelihood

- **Resume screen:** High — one page, class year in window, Python/Redis/SQL/FastAPI/PostgreSQL/Docker/CI through use, named production defect; DSP tail is a ding, not a no
- **Overall hire odds:** Medium — B-tier <1%; resume should clear Greenhouse, then Med–Hard CodeSignal 800+ still eliminates almost everyone. Survivable screen, not a lock
- **Funnel filters:** Greenhouse resume → CodeSignal (800+; community ~815+ **[directional]**) → ~3 rds Med–Hard · no intern sys design · Bottleneck: OA bar · <1% · hybrid ≥3 days/week Menlo Park / Bellevue / NYC · $60/hr Zone 1
- **Outside the resume:** Apply in this first-wave window (posted 2026-09-14, rolling). Timed Med–Hard CodeSignal. Short STAR set (Vylet name-collision, SignalWeaver FastAPI/Postgres). Do not invent Django, Go, or Kafka.
