# Software Engineer – 2027 Internship Program (June Start) at Old Mission

## Verdict

- **Score:** 9.0 / 10 (1 demerit — 0 emergency, 0 major, 1 minor)
- **Eligibility:** eligible — Expected May 2028 matches the JD graduation window (December 2027 or May 2028); GPA 3.66 ≥ 3.0; CS major; internship June 1 2027 is after junior year (rising senior = at least junior standing)
- **Track:** full-stack (Chicago prop/MM C++ trading-applications / electronic-trading systems flavor; no track divergence)
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- C++ real-time systems (Granular — zero-allocation MemoryPool, lock-free SPSC, processBlock() safety audit) plus Python production (MDC Flask/EC2, CaseStudyPrep sub-5ms / 60 FPS) clear a selective Chicago prop/MM SWE screen.
- CS + Economics, SignalWeaver (49ms p50 financial-news search, 9.1s p50 research scores), and May 2028 class year surface genuine markets curiosity without fabricating a trading intern.
- Binding ding: Granular never lands a processBlock() callback latency, xrun, or CPU number — the C++ differentiator is design-dense but unsized.

### Demerits

- **minor** · `Granular Synthesizer Plugin` · hot-path latency unmeasured — lock-free SPSC and MemoryPool never land a time; the CMake closer is a real-time safety audit (zero heap, zero locks), not callback latency, xrun, or CPU.

### Misreads

- A rushed screen could bucket Granular as hobby audio DSP rather than trading-tech-adjacent real-time systems because the closer is a release/safety checklist, not a latency number.
- CaseStudyPrep’s “Voice AI” title plus ONNX/Whisper third bullet can still misfile the top of Experience as ML if the reader skips the sub-5ms / 60 FPS hook.

### Interview angles

- **Lead with:** Granular lock-free SPSC / zero-allocation MemoryPool / processBlock constraints; CaseStudyPrep sub-5ms main-thread and 60 FPS (frame as real-time systems, not Whisper); MDC sole-ownership production Flask/EC2 + 800-hour ETL; SignalWeaver 49ms p50 semantic search and 9.1s p50 research scores.
- **Defend:** Granular has no processBlock latency/CPU/xrun in the pool — narrate the real-time safety constraint (no heap, no mutex on the audio thread) as the ship gate, pivot measured latency to CaseStudyPrep *(out of rails: Granular hot-path timing — full 6-bullet pool is architecture/capacity/DSP/release-audit only)*. TypeScript is Skills-only; interview in C++ (the intern stack) or Python. No trading-desk internship — CS + Economics plus SignalWeaver is the honest markets story. Do not lead with LoRA/agent pipelines.
- **Depth prep:** Timed DS&A in C++ (preferred) or Python until a medium is a ~20-minute solve; HackerRank OA is CS-fundamentals MCQ (~30 in 60m) plus 2 med–hard coding. Phone/video tech: project deep-dive (Granular memory/threading, CaseStudyPrep real-time path). Chicago onsite (~2h): low-level C++/systems — containers, memory, OS.

## Likelihood

- **Resume screen:** High — C++ and Python are in bullets, production shipping has metrics, class year and GPA are in window; one minor dents polish, it does not flip the screen.
- **Overall hire odds:** Low — S-tier Chicago prop/MM intern funnel (~1–2%); the resume buys the HackerRank invite, not the offer. Binding filters are the OA then phone tech and a ~2h low-level C++ onsite.
- **Funnel filters:** Greenhouse resume screen → HackerRank OA (CS MCQ ~30/60m + 2 med–hard coding) → phone/video tech → Chicago onsite (~2h, containers/memory/OS) · Chicago onsite · GPA ≥ 3.0 · grad window Dec 2027 or May 2028 · start June 1 2027 · no finance experience required.
- **Outside the resume:** Timed C++ OA plus OS/memory/containers drill; apply in the first wave; a warm intro at a ~200-person firm is high-leverage; do not fake a trading internship.
