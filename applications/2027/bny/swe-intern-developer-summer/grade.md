# 2027 Summer Internship Program - Engineering (Developer) at BNY

## Verdict

- **Score:** 8.0 / 10 (2 demerits — 0 emergency, 0 major, 2 minor)
- **Eligibility:** eligible — Expected May 2028 matches graduating Dec 2027 or May 2028; GPA 3.66 ≥ 3.0; US citizen / no sponsorship
- **Track:** full-stack + fintech-backend / custody-bank / financial-market infrastructure
- **Pipeline:** 3 cycle(s) · exit: writer_peak

## Screen Review

### First read

- Eligible UMich CS+Econ intern (May 2028, 3.66) with a production Flask REST API on AWS EC2, a live product with a named defect fix, and FastAPI/React/CI — this is a Developer intern screen, not ML-research.
- Finance-adjacent work sits in the lead window (campaign-finance PAC pipeline; PE/search-fund product; financial-research platform).
- Binding ding: the MCFN API is unsized, and CSS from the posting never appears on the page.

### Demerits

- **minor** · `MDC` · production API unsized; third bullet restates ownership — The lead Flask REST on EC2 has no callers, endpoints, or traffic; ~800 hours / 400 PACs size the ETL. The third bullet repeats sole-engineer / REST-on-EC2.
- **minor** · `resume` · CSS never appears — JD names CSS with Python and JavaScript; React/Angular/nginx imply UI but CSS/HTML are not in Skills or bullets.

### Misreads

- A rushed screener might bucket the Flask line as a student deploy because the service has no size next to a strong ETL metric.
- A keyword pass might miss CSS and treat the page as Python-only despite TypeScript/React/Angular through use.

### Interview angles

- **Lead with:** MDC Flask REST on EC2 shipped to MCFN (ingestion → rankings → public research workflow); Vylet name-collision debug 79%→89% plus Docker/CI discipline; SignalWeaver FastAPI + React/TypeScript + GitHub Actions pytest.
- **Defend:** No Java — inventory is Python/TypeScript/SQL/C++; ramp on JVM if the rotation is Java-heavy. CSS not claimed *(out of rails: no pool bullet names CSS/HTML/styling; listing HTML/CSS in Skills reopened a through-use ding)*. MCFN API has no request/latency/user number *(out of rails: MDC pool does not size the service)*.
- **Depth prep:** Likely HackerRank Easy–Hard (arrays/strings, graphs, DP, greedy). Walk Flask/EC2 deploy, RxJS S3 recovery, and Vylet ownership-verification defect. OOP if they follow campus BNY loops. Not Copilot, not custody-platform theater.

## Likelihood

- **Resume screen:** High — eligible class year/GPA, production Python API + TypeScript/React + CI, finance-adjacent flavor in the lead window.
- **Overall hire odds:** Medium — B-tier resume-gated intern (~8–12%); paper should convert to OA, then timed coding plus a behavioral filter still eliminate.
- **Funnel filters:** Oracle Cloud HCM knockouts (GPA 3.0, no sponsorship) → likely HackerRank → tech + behavioral. No intern sys design published.
- **Outside the resume:** Apply in this first wave (posted 2026-08-24). Timed mediums. No BNY contact in `network.md` — a real employee referral would warm the pile.
