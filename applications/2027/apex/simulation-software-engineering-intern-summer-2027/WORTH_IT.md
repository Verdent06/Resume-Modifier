# WORTH_IT — Apex Simulation Software Engineering Internship (Summer 2027)

**Verdict: YES** (7 / 10 worth-it)

Resume/project fit against `persona.md` Screen Criteria vs `context.md` pool — not eligibility (computed **ineligible** vs JD text; see caveat). This is Ashby `d3e21f84-3637-4521-833d-fd07b3ca5f2e` **Simulation Software**, not the Ground Software twin (`2d5ad921-241f-4e7a-b9ff-9d01763da88c`) and not Embedded Systems.

## Reasons

1. **C++ screen is a match.** JD requires C/C++. Pool has Granular `processBlock()` / lock-free SPSC / zero-alloc slab in bullets. No need to invent C as distinct from C++. Modeling and embedded are pluses — analog is hard real-time C++, not MCU firmware.

2. **Simulation domain is not in the pool.** No 6DOF, SITL, HITL, Gazebo, ROS, MATLAB/Simulink. Persona: inventing them is worse than omitting them. Closed-loop analog on-page is CaseStudyPrep timing + Granular real-time audit + SignalWeaver p50/p99 + CI — honest, not a HITL claim.

3. **Wrong-twin risk is manageable.** Ground Software PDF is architecture + React/FastAPI. This PDF leads real-time, puts C++ first in Skills, uses FastAPI+CI instead of a React dashboard, and omits React from Frameworks. Do not attach the Ground Software file to this Ashby id.

4. **Eligibility is the binding caveat, not a form knockout.** JD text: Fall 2026 or Spring 2027. Candidate: Expected May 2028. Sibling Ground Software intern (same season, same $45/hr LA program) required Fall 2027 or Spring 2028 — this Simulation window is likely stale copy. Ashby form has **no graduation-date field**. Recruiter can still no-pile the PDF date.

5. **Calibration.** B-tier (`companies.md`); intern $45/hr; bottleneck resume + tech (~5–8% directional). Apply-early intern funnel (`recruiting.md` §8). Named gaps (SITL/HITL/6DOF, modeling, embedded, ROS) are honest.

**Stop: no — unless they enforce the written Fall 2026 / Spring 2027 window against May 2028.** Continue full packet + `written-answers.md`. State Summer 2027 and Expected May 2028 honestly in Why Apex.
