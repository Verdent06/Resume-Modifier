# Simulation Software Engineering Internship (Summer 2027) at Apex

## Verdict

- **Score:** 7.0 / 10 (3 demerits — 0 emergency, 0 major, 3 minor)
- **Eligibility:** ineligible — JD text requires graduating Fall 2026 or Spring 2027; candidate Expected May 2028 (Spring 2028). Sibling Ground Software intern for the same intern season required Fall 2027 or Spring 2028. This Ashby form has no graduation-date field. If they enforce the written Simulation window, the PDF date is an auto-no.
- **Track:** robotics (closed-loop spacecraft V&V / 6DOF / SITL / HITL)
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- CaseStudyPrep leads on Web Worker / sub-5ms / 60 FPS real-time plus a 27% S3/RxJS upload recovery — closed-loop timing, not a Voice-AI pitch.
- Granular C++ zero-alloc `processBlock`, lock-free SPSC, and a real-time safety audit are the language the JD requires, on page 1. No invented ROS / Gazebo / 6DOF / SITL / HITL / MATLAB. Not packaged as firmware or as the Ground Software React twin.
- Binding dings: Granular never sizes the real-time win; Vylet still reads PE/LangGraph; SignalWeaver is financial-research FastAPI/CI.

### Demerits

- **minor** · `Granular Synthesizer Plugin` · metric-free — Lock-free SPSC, zero-alloc processBlock, and a real-time safety audit are the C++ closed-loop signal this simulation desk wants, but none of the three bullets sizes the outcome (callback latency, xruns, CPU)
- **minor** · `Vylet` · off-axis product — Docker/Redis/Celery and a 79%→89% defect fix are real, but the page frames a PE/search-fund LangGraph lead-gen product — a skim at a spacecraft-V&V desk can bucket it as GTM/agentic rather than simulation/test engineering
- **minor** · `SignalWeaver` · off-axis stack — FastAPI p50/p99 instrumentation and Docker/GitHub Actions CI are the closest test-platform analog on the page, but the entry is a financial-research scorer wrapping MPNet sentiment — a simulation screener can read it as leftover full-stack/ML rather than closed-loop V&V

### Misreads

- Granular without a number can read as hobby DSP rather than mission-critical real-time discipline — a skim may underrate the C++ this Simulation req screens for.
- Vylet's founder/PE tagline can bucket the resume as agentic GTM rather than spacecraft V&V, even though the Docker/Celery workers are real.
- SignalWeaver's MPNet/financial-research line can bucket the page as the Ground Software twin or an ML intern.

### Interview angles

- **Lead with:** Granular C++ zero-alloc `processBlock`, lock-free SPSC, real-time safety checklist; CaseStudyPrep real-time frontend (Web Worker, <5ms, 60 FPS, 27% upload recovery); MDC sole-owned Flask REST on EC2 as delivery proof
- **Defend:** Granular has no xrun/latency/CPU metric on the page *(out of rails: pool has no verbatim impact-metric bullet; swap sets cannot invent one)* — script the audio-thread constraints and what you would measure next. Vylet PE/LangGraph framing *(out of rails: product identity is the pool header; Node 3 swap overflowed the page; cannot drop the entry)* — pivot to Docker/Redis/Celery workers and the 79%→89% defect fix. SignalWeaver financial/MPNet *(out of rails: entire pool is financial-research)* — pivot to p50/p99 instrumentation and Docker/GitHub Actions CI as a test-platform analog. Do not claim 6DOF, SITL, HITL, ROS, Gazebo, MATLAB, firmware, Octopus OS, a satellite internship, or clearance.
- **Depth prep:** lock-free concurrency and C++ real-time rules; what closed-loop / SITL / HITL *means* conceptually (do not claim you built them); REST + Docker CI walkthrough. OA platform unpublished for this Simulation posting; Embedded twin reports (directional only) were 2 LC-medium / 70m then coding + light sys design + behavioral. Do not prep as Ground Software frontend or as firmware/microcontrollers.

## Likelihood

- **Resume screen:** Medium — C++ and real-time on page 1, no invented sim stack; Experience still leads Voice-AI / PAC ETL / PE LangGraph; modeling/embedded pluses are absent
- **Overall hire odds:** Medium — B-tier Ashby funnel is resume-then-tech (~5–8% directional); unpublished OA/practical plus LA onsite still have to see C++ DS&A live. If the written Fall 2026 / Spring 2027 window is enforced against Expected May 2028, odds collapse to Low
- **Funnel filters:** Ashby resume → recruiter → HM → technical/practical assessment → LA onsite → offer. Simulation intern OA unpublished. Bottleneck: resume + tech. Onsite Factory One / Playa Vista. Export-controlled US Person. Graduation window as written vs sibling Ground Software posting.
- **Outside the resume:** Apply in the first wave of this 2026-09-04 posting; answer US Person honestly; a simulation/GNC referral (HM > recruiter > engineer); prep LC-mediums plus a lock-free/C++ walkthrough. **Do not attach the Ground Software PDF.**
