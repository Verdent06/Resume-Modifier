# Ground Software Engineering Internship (Summer 2027) at Apex

## Verdict

- **Score:** 8.0 / 10 (2 demerits — 0 emergency, 0 major, 2 minor)
- **Eligibility:** eligible — Expected May 2028 is Spring 2028; JD requires graduating Fall 2027 or Spring 2028. Summer 2027 only (Spring requires pausing academics). US Person / export control is an apply-form knockout; candidate is a US citizen.
- **Track:** full-stack + aerospace / satellite-bus manufacturing / mission-critical spacecraft ops / ground-segment software
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- CaseStudyPrep leads on Web Worker / sub-5ms / 60 FPS real-time frontend plus a 27% S3/RxJS upload recovery — production latency, not a Voice-AI pitch.
- MDC Flask REST on EC2, SignalWeaver React/TypeScript + FastAPI, and Granular C++ lock-free `processBlock` cover architecture + backend + frontend + deploy + the satellite-ops systems identity. No invented Rust/Vue; not packaged as firmware.
- Binding dings: Granular never sizes the real-time win, and Vylet still reads as PE/LangGraph lead-gen.

### Demerits

- **minor** · `Granular Synthesizer Plugin` · metric-free — Lock-free SPSC, zero-alloc processBlock, and a real-time safety audit are the systems signal this satellite-ops shop wants, but none of the three bullets sizes the outcome (callback latency, xruns, CPU)
- **minor** · `Vylet` · off-axis product — Docker/Redis/Celery and a 79%→89% defect fix are real, but the page frames a PE/search-fund LangGraph lead-gen product — a skim at a ground-software desk can bucket it as GTM/agentic rather than satellite-ops engineering

### Misreads

- Granular without a number can read as hobby DSP rather than mission-critical real-time discipline — a skim may underrate the C++ systems evidence Apex ground software wants.
- Vylet's founder/PE tagline can bucket the resume as agentic GTM rather than ground-segment software, even though the Docker/Celery workers are the deploy proof.

### Interview angles

- **Lead with:** CaseStudyPrep real-time frontend (Web Worker, <5ms, 60 FPS, 27% upload recovery); Granular C++ zero-alloc `processBlock`, lock-free SPSC, real-time safety checklist; MDC sole-owned Flask REST on EC2; SignalWeaver React/TypeScript dashboard + FastAPI REST
- **Defend:** Granular has no xrun/latency/CPU metric on the page *(out of rails: pool has no verbatim impact-metric bullet; swap sets cannot invent one; 16-voice swap overflowed the page)* — script the audio-thread constraints and what you would measure next. Vylet PE/LangGraph framing *(out of rails: product identity is the pool header; Node 3 swap overflowed; cannot drop the entry)* — pivot to Docker/Redis/Celery workers and the 79%→89% defect fix, not the GTM story. Do not claim Rust, Vue, Octopus OS, a satellite internship, firmware, or clearance.
- **Depth prep:** lock-free concurrency and C++ real-time rules; REST/API + React dashboard + EC2/Docker deploy walkthrough; software architecture trade-offs (why REST, why a worker queue). OA platform unpublished for this Ground Software posting; Embedded twin reports (directional only) were 2 LC-medium / 70m then coding + light sys design + behavioral. Do not prep as firmware/microcontrollers.

## Likelihood

- **Resume screen:** High — real-time lead, C++ and React in bullets, architecture + backend + frontend + deploy on one page, no invented stack
- **Overall hire odds:** Medium — B-tier Ashby funnel is resume-then-tech (~5–8% directional); unpublished OA/practical plus LA onsite still have to see DS&A and architecture live, and US Person is a binary apply-form knockout
- **Funnel filters:** Ashby resume → recruiter → HM → technical/practical assessment → LA onsite → offer. Ground Software intern OA unpublished. Bottleneck: resume + tech. Onsite Factory One / Playa Vista. Export-controlled US Person.
- **Outside the resume:** Apply in the first wave of this 2026-09-03 posting; answer US Person honestly on Ashby; a ground-software referral (HM > recruiter > engineer); prep LC-mediums plus a lock-free/C++ walkthrough and a React+API deploy story
