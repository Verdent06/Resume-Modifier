# WORTH_IT — Apex Ground Software Engineering Internship (Summer 2027)

**Verdict: YES** (8 / 10 worth-it)

Resume/project fit against `persona.md` Screen Criteria vs `context.md` pool — not eligibility (already computed eligible). This is Ashby `2d5ad921-241f-4e7a-b9ff-9d01763da88c` **Ground Software**, not the Embedded Systems twin.

## Reasons

1. **Full-stack screen is a match.** JD tests software architecture plus preferred Rust/C/C++ backend and Vue/React frontend, plus design/dev/deploy of satellite-ops software. Pool has Flask REST on AWS EC2, React/TypeScript + FastAPI, Docker/Redis/Celery, and C++ in bullets. Architecture + backend + frontend + deploy exist without inventing a stack.

2. **Preferred languages are not exclusive knockouts.** C++ and React are in inventory and on-page. Rust, Vue, and C-as-distinct-from-C++ are **not** in the pool — persona: inventing them is worse than omitting them. Not a worth-it knockout.

3. **Differentiator is carryable.** Apex identity is mission-critical spacecraft ops / ground-segment software. Pool has lock-free C++ `processBlock()` (Granular) and real-time frontend (CaseStudyPrep Web Worker, <5ms, 60 FPS, 27% upload recovery). That is systems discipline, not firmware — the Embedded twin's packaging.

4. **Eligibility is clean.** Expected May 2028 = Spring 2028 (JD window). US citizen / US Person / no sponsorship. Summer 2027 only; relocate to LA: Yes. Spring would require pausing academics — not this packet.

5. **Calibration.** B-tier (`companies.md`); intern $45/hr; bottleneck resume + tech (~5–8% directional). Apply-early intern funnel (`recruiting.md` §8). Named gaps (Rust, Vue, satellite internship, Octopus OS) are honest, not hard mismatches.

**Stop: no. Continue full pipeline + `written-answers.md`.**
