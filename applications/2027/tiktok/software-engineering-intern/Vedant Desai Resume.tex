%% ============================================================================
%%  applications/2027/tiktok/software-engineering-intern/Vedant Desai Resume.tex
%% ----------------------------------------------------------------------------
%%  Initial re-pipeline rebalance for TikTok SWE Intern — Agentic Creation (TTEP).
%%  screen_track = full-stack; differentiator = agentic/LLM-orchestration
%%  systems (agent servers, prompt/skill workflows, MCP tooling, KB/RAG).
%%  track_divergence = true — leads with full-stack spine (frontend, backend/
%%  API, data layer via CaseStudyPrep, MDC, SignalWeaver) while keeping
%%  agentic depth prominent at the top (Vylet leads: pipeline, eval harness,
%%  name-collision fix, asyncpg DAL). Projects: SignalWeaver
%%  (LangGraph + pgvector RAG + FastAPI + React) — MatchStream retired.
%%  4-entry structure (min_entries=4). Lyndbrook and Granular Synth omitted.
%%  iter-2 grading-response: Vylet #3/#4 → pool #2+#7 (agentic eng depth);
%%  MDC → pool #1+#3 (metric ETL + Flask); dropped Skills C++ (no bullet
%%  evidence; gate only requires Python from JD∩inventory).
%%  gate-fix page_fill: collapsed SignalWeaver tagline; trimmed Skills to
%%  Languages+Frameworks (AI/ML keywords remain in bullets) to fit one page.
%%  iter-3 grading-response: Vylet #4 pool #7 → pool #4 (metricked
%%  geography-first discovery, ~30 leads/month; shorter for page budget).
%%  Dates defect out_of_rails — Project: Vylet header has no date range.
%%
%%  Class path: three levels up to applications/template.cls.
%% ============================================================================

\documentclass[
	11pt
]{../../../template}

\name{Vedant Desai}
\contact{(248) $\hspace{-0.1em}$ 704 $\hspace{-0.4em}$ -- $\hspace{-0.4em}$ 4852 \\ vedantde@umich.edu \\ \href{https://github.com/Verdent06}{github.com/Verdent06} \\ \href{https://linkedin.com/in/vedantde06}{linkedin.com/in/vedantde06}}

\begin{document}

	% ---- EDUCATION (fixed) --------------------------------------------------
	\begin{rSection}{E}{ducation}
		\begin{rSectionEntry}
			{University of Michigan}
			{Expected May 2028}
			{B.S. in Computer Science and Economics}
			{}
			{Ann Arbor, MI}

			\item \textbf{GPA:} 3.66 / 4.0
			\item \textbf{Coursework:} Data Structures \& Algorithms, Discrete Mathematics
		\end{rSectionEntry}
	\end{rSection}

	% ---- EXPERIENCE -----------------------------------------------------------
	\begin{rSection}{E}{xperience}
		\begin{rSectionEntry}
			{Vylet}
			{May 2026 -- Present $\vert$ \href{https://vyletdata.com}{vyletdata.com} $\vert$ \href{https://github.com/Verdent06/Lead-Gen-Agentic-Pipeline}{GitHub}}
			{Founder --- Automated lead-sourcing platform for PE/search-fund acquisition prospecting --- live product generating \$1,500 MRR across three paying clients}
			{}
			{}

			\item Launched Vylet, automating a ~30-minute manual process per business into a Dockerized LangGraph pipeline generating 30 scored leads in 30 minutes --- a 30x speedup --- with Redis/Celery workers on a recurring cycle.
			\item Engineered a LangSmith eval pipeline spanning 20 adversarial business test cases across 13 archetype labels --- manufacturers, SaaS tools, PE holding companies, geographic mismatches --- then layered deterministic Pydantic consensus gates to lift extraction faithfulness from 50\% to 90\%.
			\item Diagnosed a name-collision defect in ownership-verification logic that was incorrectly rejecting valid acquisition targets sharing a name with an unrelated business elsewhere; the fix lifted the pipeline's lead-qualification rate from 79\% to 89\% with zero change to sourcing volume.
			\item Shipped a geography-first discovery mode decoupled from a pre-set vertical, surfacing ~30 leads/month in niche verticals --- custard shops, bowling alleys --- structurally invisible to vertical-first discovery.
		\end{rSectionEntry}

		\begin{rSectionEntry}
			{CaseStudyPrep.AI}
			{Dec 2025 -- May 2026}
			{Software Engineer Co-op (Voice AI)}
			{}
			{Remote}

			\item Eliminated the silent-audio problem in a voice-AI product --- most frames sent to Whisper were dead air --- by running Silero VAD client-side via ONNX Runtime, filtering silence before upload and cutting cloud inference costs by 40\%.
			\item Eliminated a 27\% audio upload failure rate with fault-tolerant RxJS logic that detects expired S3 presigned URLs, regenerates them mid-flight, and negotiates MIME types for WAV files Angular silently rejected.
			\item Moved audio processing off the UI thread into a Web Worker with an async stream handoff, reducing main-thread blocking time to under 5ms and keeping the real-time audio visualizer at a smooth 60 FPS during active inference.
		\end{rSectionEntry}

		\begin{rSectionEntry}
			{Michigan Data Consulting (MDC)}
			{Jan 2026 -- May 2026}
			{Data Engineer --- Michigan Campaign Finance Network}
			{}
			{Ann Arbor, MI}

			\item Replaced manual Michigan campaign-finance research --- portal searches capped by the Bureau of Elections, irregular Excel exports, hand normalization at ~2 hours per committee --- with a Requests + Pandas ETL that ingests filings directly, eliminating ~800 hours of manual pulls across 400 tracked PACs.
			\item Delivered a production Flask REST API on AWS EC2 as the sole engineer on a 5-month MCFN contract, wiring ingested data and PAC rankings into the nonprofit's public-facing research workflow.
		\end{rSectionEntry}
	\end{rSection}

	% ---- PROJECTS -----------------------------------------------------------
	\begin{rSection}{P}{rojects}
		\begin{rSectionEntry}
			{SignalWeaver}
			{\href{https://github.com/Verdent06/SignalWeaver}{github.com/Verdent06/SignalWeaver}}
			{}
			{LangGraph, FastAPI, pgvector, React, TypeScript}
			{}

			\item Orchestrated a 5-node LangGraph pipeline (fetch $\rightarrow$ classify $\rightarrow$ embed $\rightarrow$ score $\rightarrow$ explain) with per-node timing from the same 90-run batch: fetch 7.3s p50, classify 1.1s p50 ($\sim$29ms/article over 40-article batches), embed 1.1s p50, score under 1ms p50, explain 3ms p50 --- fetch dominated wall time ($\sim$80\% of p50 latency).
			\item Implemented semantic search over stored financial news with pgvector cosine similarity (768-d MPNet embeddings), hitting 49ms p50 / 99ms p99 over 90 queries returning top-5 or top-10 matches.
			\item Built a React/TypeScript dashboard for composite scores and fundamental/sentiment/macro breakdowns; used it to analyze 90 distinct tickers with scores persisted to Postgres for history views.
			\item Built a FastAPI service wrapping fundamentals, MPNet sentiment, and regression logic behind async REST endpoints; instrumented end-to-end /analyze at 9.1s p50 / 15.2s p99 across 90 successful runs on 90 distinct tickers.
		\end{rSectionEntry}
	\end{rSection}

	% ---- TECHNICAL SKILLS -------------------------------------------------
	\begin{rSection}{T}{echnical Skills}
		\begin{rSet}{Languages}{Python, TypeScript, SQL}
		\end{rSet}
		\begin{rSet}{Frameworks}{React, Angular, FastAPI, Flask, RxJS}
		\end{rSet}
	\end{rSection}

\end{document}
