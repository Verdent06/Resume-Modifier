# Software Engineering Co-Op (Summer 2027) at Saab, Inc.

## Verdict

- **Score:** 8.0 / 10 (2 demerits — 0 emergency, 0 major, 2 minor)
- **Eligibility:** eligible — Expected May 2028 + Summer 2027 = completed junior / rising senior; JD requires completed sophomore or greater; GPA 3.66 ≥ 3.0; CS major; U.S. citizen
- **Track:** full-stack + air-traffic-management / surveillance software (safety-critical ATM systems)
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- One-page UMich CS intern resume: MDC leads with a production Flask REST API on AWS EC2 plus a quantified PAC ETL — generic intern SWE in Python, not an ML/PE-deal lead.
- CaseStudyPrep in the lead window carries real-time / fault-tolerant Angular work; Granular then shows lock-free C++ OOP with a VST3/AU release; SignalWeaver adds FastAPI + pytest CI — on-axis for Surveillance Software whose bottleneck is the resume, not an OA.
- Binding dings: Linux/Unix is never named; Granular's four systems bullets have no runtime metric.

### Demerits

- **minor** · `resume` · Linux/Unix absent — JD lists a Linux or Unix development environment; Docker, EC2, and CMake imply it, a 30-second Linux scan is empty.
- **minor** · `Granular Synthesizer Plugin` · metric-free — lock-free SPSC, zero-alloc `processBlock`, Gaussian LUT, and a CMake real-time safety audit are the C++ signal, but none sizes callback latency, xruns, or CPU.

### Misreads

- A rushed ATM-software skim can file this as "web/data intern plus hobby audio, no Linux" before the lock-free C++ and shipped Python land.

### Interview angles

- **Lead with:** MDC sole-engineer Flask REST on AWS EC2 + Requests/Pandas ETL (~800 hours / 400 PACs); Granular zero-allocation `MemoryPool` and lock-free SPSC plus CMake VST3/AU real-time audit (C++ OOP / deterministic constraints); CaseStudyPrep Web Worker / 27% upload-failure debug on Angular; SignalWeaver pytest + GitHub Actions.
- **Defend:** Unix/Linux is implied by Docker/EC2/CMake but unnamed *(out of rails: Unix/Linux never appears in the pool or skills inventory)*. Granular has no xrun/latency/CPU metric on the page *(out of rails: pool has no verbatim impact-metric bullet; swap sets cannot invent one)*. No Java, Subversion, JUnit/GTest, or existing clearance — say C++ and Python are the interview languages; do not claim them. CaseStudyPrep title still says Voice AI — open with the Web Worker / Angular systems bullets, not the product category.
- **Depth prep:** C++ real-time (slab allocators, SPSC, `processBlock` constraints, CMake release gates); Python production (Flask/EC2, Docker, pytest CI); Angular/RxJS UI debug; light DS&A / OOP for a possible technical video on a 2–3 round Easy loop; code-review / release-gate stories.

## Likelihood

- **Resume screen:** High — eligible class year, C++ in bullets with lock-free/CMake depth as the lead project, Python shipped first; two minors dent the scan, they do not flip it to a no.
- **Overall hire odds:** Medium — no named OA, so most front-end elimination is this PDF, then Easy recruiter/tech rounds at a ~20–25% C-tier intern accept rate. Citizenship is a hard form knockout this page cannot show.
- **Funnel filters:** Workday resume screen (binding) → recruiter phone → possible technical video (C++/Python/Java OOP) → panel · Easy · no standard intern OA · no intern sys design · U.S. citizenship required · GPA 3.0 · completed sophomore+ · East Syracuse onsite.
- **Outside the resume:** Apply in the first wave on Workday (req R-03237, posted 2026-09-03, open through 2027-01-01); answer citizenship yes; confirm Collamer onsite and housing sign-on if outside commuting distance; prep light DS&A plus OOP/C++ real-time stories.
