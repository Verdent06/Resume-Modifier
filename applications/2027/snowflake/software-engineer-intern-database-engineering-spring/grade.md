# Software Engineer Intern (Database Engineering) - Spring 2027 at Snowflake

## Verdict

- **Score:** 8.0 / 10 (2 demerits — 0 emergency, 0 major, 2 minor)
- **Eligibility:** eligible — actively enrolled UMich CS+Econ Expected May 2028; Spring 2027 dates (Jan 4/19–Mar 26/Apr 9/May 7) fit; US citizen, no sponsorship. JD *desired* 3rd/4th year is not a form knockout (Spring 2027 is year 2 of a 3-year May 2028 plan; `context.md` still labels Junior). Required OS / software-engineering *course titles* are not on the education line — DSA is.
- **Track:** full-stack + database-engine / query-processing / high-performance C++ systems
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- MDC leads with a Requests + Pandas PAC ETL (~800 hours / 400 committees) plus a sole-owned Flask REST API on EC2 — data processing shipped to a stakeholder, not a class project.
- Granular sits second: C++ zero-alloc MemoryPool and lock-free SPSC FIFO on `processBlock()` — the concurrency/memory differentiator this Database Engineering intern names, not an invented query engine or OS course.
- Binding dings: Granular never sizes the real-time win, and Vylet still reads as PE/lead-gen under the Founder tagline.

### Demerits

- **minor** · `Granular Synthesizer Plugin` · metric-free — Zero-alloc MemoryPool and lock-free SPSC FIFO are the C++ concurrency signal this intern wants, but neither bullet sizes the outcome (callback latency, dropped buffers, CPU)
- **minor** · `Vylet` · off-axis product — asyncpg SQL freshness and a 79%→89% defect fix are real database/debug evidence, but the Founder tagline still frames a PE/search-fund lead-sourcing product — a query-engine skim can bucket it as GTM/agentic

### Misreads

- Granular without a number can read as hobby DSP rather than hot-path systems discipline — a skim may underrate the C++ memory/concurrency evidence Snowflake screens for.
- Vylet's Founder/PE tagline can bucket the resume as agentic GTM rather than database/debug engineering, even though the bullets are SQL freshness and a named production fix.

### Interview angles

- **Lead with:** MDC PAC ETL + Flask REST on EC2 (sole engineer, 5-month contract); Granular zero-alloc `MemoryPool` + lock-free SPSC (JD systems programming / concurrency); Vylet asyncpg SQL + name-collision defect; SignalWeaver FastAPI + pgvector (database concepts); CaseStudyPrep 27% S3 recovery
- **Defend:** Granular has no xrun/latency/CPU metric on the page *(out of rails: pool bullets 1–6 are architecture-only; adding the CMake audit overflowed to two pages)* — script the audio-thread constraints and what you would measure next. Vylet PE/LangGraph framing *(out of rails: product identity is the pool header)* — pivot to injection-safe SQL and the 79%→89% fix, not the GTM story. OS / software-engineering *course titles* are not on Education — do not invent them; map DSA + Granular concurrency to the systems bar. Do not claim Java, C++20, Spark, Hadoop, FoundationDB, Snowflake SQL, or a warehouse internship.
- **Depth prep:** C++ atomics / lock-free / zero-alloc hot paths; Medium–Hard arrays/graphs plus one concurrency problem for CodeSignal (`companies.md`) or HackerRank 90–120 min **[directional, Extern]**; SQL concepts you can defend (asyncpg, pgvector cosine, injection-safe timestamps) — not Snowflake QUALIFY/FLATTEN as if you used them; Flask vs FastAPI; one STAR debug story. Light intern sys design — query-engine internals only at interview depth, not as resume claims.

## Likelihood

- **Resume screen:** High — eligible May 2028 CS 3.66; C++ concurrent/memory in slot 2; ETL + SQL/pgvector in bullets; no invented Java/Spark/Hadoop
- **Overall hire odds:** Medium — A-tier ~3–5% (`companies.md`) with CodeSignal then Med–Hard tech as the bottleneck. The PDF clears a first read; concurrency coding, Menlo Park Spring 2027 onsite (40h), and the DSP/PE frames still have to clear. Desired 3rd/4th year is a preference gap, not a form auto-reject.
- **Funnel filters:** Ashby knockouts (dual sponsorship, work auth, U.S. person, 40h in-office, relocate) → resume → CodeSignal (`companies.md`; HackerRank also reported **[directional]**) → 3–4 rds Med–Hard · light intern sys design · Bottleneck: **tech rounds**. US citizen; no sponsorship now or later.
- **Outside the resume:** Apply in this first-wave window (posted 2026-09-25). Form email **`verdent06@gmail.com`**. Both sponsorship Booleans **No**. Location is **Northville, MI** — relocate **Yes** to Menlo Park; do not spoof a Bay Area home. Hours **40**. No Snowflake contact in `network.md`. Packet only — see `written-answers.md`. Never email this pack.
