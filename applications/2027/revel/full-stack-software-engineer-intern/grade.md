# Internship: Full Stack Software Engineer at Revel

## Verdict

- **Score:** 9.0 / 10 (1 demerits — 0 emergency, 0 major, 1 minor)
- **Eligibility:** eligible — Expected May 2028; Summer 2027 = rising junior graduating 2028; JD requires Rising Junior or Senior graduating 2027 or 2028 + ITAR US person; U.S. citizen
- **Track:** full-stack + hardware-control / aerospace-industrial C2
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- One-page UMich CS intern resume: real-time browser UI (Web Worker / 60 FPS viz) leads, C++ lock-free / zero-alloc plugin in slot two, then a shipped Flask REST API on EC2, then React/TypeScript + FastAPI + Postgres — on-axis for a resume-bottleneck Ashby full-stack seat at a hardware-control company.
- TypeScript, Python, and C++ are in bullets; React is through use on SignalWeaver; class year is in window; no Node/Next/Rust/Snowflake/Databricks/Copilot/Fusion/Tableau invented.
- Binding ding: Granular has no sized runtime metric (latency / xrun / CPU).

### Demerits

- **minor** · `Granular Synthesizer Plugin` · metric-free — CMake real-time safety audit, zero-alloc `processBlock`, and lock-free SPSC are the systems/C++ signal, but nothing sizes callback latency, xruns, or CPU

### Misreads

- A rushed C2 skim can file this as "voice-AI intern plus hobby audio" before the 60 FPS operator-style visualizer, Flask/EC2 API, and React/TS dashboard land.

### Interview angles

- **Lead with:** CaseStudyPrep Web Worker / 60 FPS visualizer and 27% upload-failure debug (browser UI + live data); SignalWeaver React/TypeScript dashboard + FastAPI REST + Docker/GitHub Actions CI; Granular CMake real-time safety audit, zero-allocation `MemoryPool`, lock-free SPSC; MDC sole-ownership Flask API on EC2
- **Defend:** Granular has no xrun/latency/CPU metric on the page. *(out of rails: pool has no verbatim impact-metric bullet; swap sets cannot invent one.)* Lead title says Voice AI — bullets are UI-thread / RxJS / S3, not an LLM-research claim. React is in Projects, not the lead; Angular is the honest JS-family proof on the co-op. No Node.js, Next.js, Rust, clearance, or hardware-control internship
- **Depth prep:** React/TS dashboard trade-offs; Flask/FastAPI + Postgres data flow; C++ real-time (`processBlock` zero-alloc, SPSC, release gates); live-data UI / Web Workers. Light DS&A if an unpublished coding screen appears. Do not fake RevelCode, Node/Next, or industrial C2 work

## Likelihood

- **Resume screen:** High — eligible class year, React/TS + FastAPI + Flask REST on the page, C++ real-time in slot two; one minor dents the skim, it does not flip a yes
- **Overall hire odds:** Medium — B-tier hardware-control startup, resume-bottleneck Ashby funnel (~5–10% directional), unpublished intern loop after the PDF. ITAR is a form knockout this page cannot show
- **Funnel filters:** Ashby resume (human-read) → unpublished intern loop · no published OA · no intern sys design · ITAR US person · rising junior or senior graduating 2027 or 2028 · on-site LA or SF · apply-by 2026-12-02 04:00 UTC
- **Outside the resume:** Apply before the 2026-12-02 window; form email `verdent06@gmail.com`; US-person **Yes**; open to relocation LA/SF **Yes**; be ready to walk the React dashboard, the Flask/EC2 API, and the `processBlock` constraints — no OA to hide behind
