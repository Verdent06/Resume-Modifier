# Software Development Intern - 2027 at Altamira Technologies

## Verdict

- **Score:** 9.0 / 10 (1 demerits — 0 emergency, 0 major, 1 minor)
- **Eligibility:** eligible on class-year/degree — `Expected May 2028` CS undergrad, returning to school after Summer 2027; U.S. citizenship is a Jobvite knockout the resume cannot prove — confirm before submit
- **Track:** full-stack (screen) + national-security data processing / intelligence extraction (differentiator; no track divergence)
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- Leads with shipped data-processing work: MDC's Requests+Pandas ETL (~800 hours eliminated across 400 PACs) and a production Flask REST API on AWS EC2 — the intelligence-extraction rhyme a Fairborn recruiter wants, not a tutorial repo.
- Polyglot in bullets, not just Skills: Python (MDC/Vylet), TypeScript/React (SignalWeaver), SQL/Postgres (Vylet tech + SignalWeaver pgvector/Postgres), C++ (Granular lock-free audio thread). Live GitHubs on both projects; Docker Compose + GitHub Actions CI/pytest on SignalWeaver.
- Mission-fit differentiator is on the page: Lyndbrook's EPA ECHO + MassGIS geospatial entity database (800+ Day-1 targets) and Google Maps cross-ref of 2,500+ entities — multi-source / geospatial aggregation that maps onto PostGIS-flavored intel work.

### Demerits

- **minor** · `Granular Synthesizer Plugin` · metric-free — both C++ bullets are lock-free / zero-alloc decisions (MemoryPool slab, SPSC FIFO, atomic shared_ptr swap) with no sized outcome (latency, CPU, xruns); the systems-language proof is deep but unscorable at a 30-second screen.

### Misreads

- A rushed skim can bucket Granular as an "audio hobby" and miss that it is the only C++ / real-time-systems proof on a JD that lists C++ first — the candidate then reads narrower (Python data shop) than the page actually is.

### Interview angles

- **Lead with:** MDC ETL → production Flask/EC2 as the "I ship data-processing software" story; Lyndbrook EPA/MassGIS entity resolution as the geospatial/multi-source intel rhyme; SignalWeaver as the full-stack + CI walk-through (FastAPI/Postgres/React/TS/Docker/GitHub Actions).
- **Defend:** Granular has no latency/CPU number *(out of rails: all six C++ pool bullets are design params or a qualitative real-time checklist; no swap-set adds a measured outcome; dropping C++ would forfeit the JD's systems-language signal)*. Narrate the constraint (audio thread: zero heap, zero locks) rather than inventing a benchmark. Do not overclaim Java — it is not on the page.
- **Depth prep:** conversational "walk me through" of the ETL, PWSID entity-resolution, and lock-free C++ decisions (this loop is not an OA gauntlet); Postgres/SQL and one C++ memory-ordering thread; STAR for teamwork and learn-and-apply-new-tech (JUCE/C++ from scratch is the proof).

## Likelihood

- **Resume screen:** High — on-axis data-pipeline lead, polyglot in bullets, geospatial/entity-data on the page, full-stack plus CI, live GitHub, `Expected May 2028`, one-page parseable layout. Jobvite human-read, no standardized OA, so this PDF is the front-end filter and it clears it.
- **Overall hire odds:** Medium — a small cleared intern class with no OA gauntlet means the resume opens the conversation; the binding filters after that are the U.S.-citizenship knockout, onsite Fairborn willingness, and a practical walk-through, not LeetCode. Tracker peers in the 8–10 full-stack band are all still Applied (0/17 interviews), so headcount and knockouts bind more than another polish pass.
- **Funnel filters:** ~2–3 rounds · practical/conversational · Jobvite (no known standardized OA) · no intern sys-design · Bottleneck: U.S. citizenship + resume screen · onsite Fairborn, OH.
- **Outside the resume:** Confirm citizenship on the Jobvite knockout before submit. Apply now (req posted ~2 days ago; intern cycle rewards the first wave). A Fairborn / Parsons / Wright-Patt referral beats another resume pass (`recruiting.md`: HM > recruiter > engineer > cold apply).
