# 2027 Launch Program: Data Science Intern at Red Ventures

## Verdict

- **Score:** 9.0 / 10 (1 demerits — 0 emergency, 0 major, 1 minor)
- **Eligibility:** eligible — Expected May 2028 vs graduating Winter 2027 or Summer 2028; Fort Mill hybrid May 2027 start with relocate; US citizen / no OPT or other sponsorship; Python (R not in inventory)
- **Track:** ai-ml + digital media / performance-marketing consumer-decision properties
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- MDC leads with irregular Excel → Requests+Pandas ETL (~800 hours / 400 PACs) and PAC rankings shipped to MCFN on a Flask REST API / AWS EC2 — ingest → clean/rank → stakeholder delivery, not ML-research and not a SWE intern sibling.
- Lyndbrook EPA+MassGIS merge plus Review Velocity (800 → 280, 35% precision) is the predictive-scoring analog in the lead window; SignalWeaver adds held-out LoRA NLP (81%→96%) and an out-of-sample regression.
- Binding ding is small: the only on-page SQL line (Vylet asyncpg freshness) never sizes the win. R / recsys / attribution platforms / Tableau are correctly unclaimed.

### Demerits

- **minor** · `Vylet` · SQL/DAL bullet metric-free — The asyncpg/SQL freshness bullet is the only on-page SQL proof, but it closes on architecture (re-scrapes, injection-safe timestamps) with no sized impact; an applied-DS screen looks for query/aggregate/insight SQL and does not find it.

### Misreads

- A keyword-first pass for R / recsys / Tableau / attribution can bucket this as "wrong stack" even though a React/Postgres dashboard, PAC rankings, and a held-out regression are on the page.
- The SQL/DAL line can read as database plumbing rather than the language-floor proof a Python+SQL DS screen is looking for, because it never sizes the freshness win.
- A skim that stops on "Founder / $1,500 MRR" can file this as a GTM intern and miss the Pandas ETL, Review Velocity score, and LoRA classification this Launch DS seat tests.

### Interview angles

- **Lead with:** MDC (irregular filings → Pandas ETL → PAC rankings shipped to MCFN on EC2; ~800 hours / 400 PACs) and Lyndbrook (EPA/MassGIS entity database → Review Velocity shortlist at 35% precision) as the ingest → score → insight loop; SignalWeaver LoRA 81%→96% plus out-of-sample regression if they ask for NLP/LLM or predictive modeling; Vylet SQL freshness / LangGraph 30x if they ask for relational querying or LLM pipelines
- **Defend:** SQL on the page is Vylet asyncpg freshness / re-scrape, not a JOIN that produced a ranking — say Pandas/Python did the analysis *(out of rails: pool has no JOIN/window/GROUP BY analytics bullet; only SQL pool bullet has no impact metric)*. No R, recsys, attribution platforms, Tableau, Snowflake, Databricks — walk React/Postgres, Pandas, and LoRA instead of inventing them *(out of rails: those tools are not in inventory or swap sets)*. This is Launch **Data Science** intern 8233284, not a SWE intern. Adding Vylet's 79%→89% quality bullet overflowed the page.
- **Depth prep:** Easy Python/SQL + a predictive take-home analog, not LC OA (`company.md`). Walk a non-builder through one finding (MDC ranking or Lyndbrook shortlist). SignalWeaver 3.39% R² as "the score is not just fitting noise," not investment advice. LoRA is Financial PhraseBank sentiment, not a recommender. Fort Mill Mon–Thu relocate is a yes. Behavioral is a filter (`recruiting.md` §6).

## Likelihood

- **Resume screen:** High — Summer 2028 window clears; Python/SQL/Pandas ETL, scored operational insight, and held-out NLP/regression are in the top half
- **Overall hire odds:** Medium — Red Ventures is C-tier with a resume bottleneck then a likely Python/SQL modeling take-home (~15–25%, `companies.md`); the loop still has to defend **Fort Mill hybrid**, no-OPT, and walk modeling without claiming R/recsys/attribution
- **Funnel filters:** Greenhouse **8233284** + human resume screen (posted 2026-09-25) → recruiter/HM (Fort Mill Mon–Thu, May 2027, grad window, no OPT) → unpublished intern loop (Easy–Med Python/SQL + take-home analog; intern OA unpublished). No intern sys design. Bottleneck: resume then take-home · ~15–25%. Comp **$3,846 bi-weekly**
- **Outside the resume:** Apply in this first wave. Honest US citizen / no OPT and Fort Mill relocate yes. GPA dropdown **3.7** (nearest tenth of 3.66). No Red Ventures contact in `network.md` — do not pick I Know Someone. Form email `verdent06@gmail.com`. Form employment: CaseStudyPrep.AI + Vylet only (MDC extracurricular). See `written-answers.md`
