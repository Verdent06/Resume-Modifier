# AI Engineer Intern at DiligenceVault

## Verdict

- **Score:** 9.0 / 10 (1 demerits — 0 emergency, 0 major, 1 minor)
- **Eligibility:** eligible — JD only requires pursuing BS/MS in CS, AI, Data Science, Engineering, or related; no exclusive class-year or GPA gate. B.S. Computer Science and Economics, Expected May 2028 (rising junior at a Summer 2027 / 3-month term).
- **Track:** ai-ml (applied AI product/agent engineering; investment diligence / document intelligence is within-track emphasis — no divergence)
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- Lead is a live Dockerized LangGraph pipeline with LangSmith eval (50%→90% faithfulness) and Gemini embeddings plus stale-data re-scrape — the JD's agent / embeddings / evaluate-then-iterate bar, with Python-stack delivery (Pandas, Flask, FastAPI) on the page.
- SignalWeaver pgvector semantic search (49ms p50) and MDC irregular-Excel ETL cover retrieve-over-messy-docs and document-intelligence analogs; Lyndbrook is the investment-research / public-data curiosity line the JD names as a plus. GPA 3.66 and a live GitHub clear a resume-first C-tier JazzHR screen.
- Binding ding: RAG is Skills-only — a keyword scan never sees the token in a bullet.

### Demerits

- **minor** · `Skills` · RAG only in Skills — Skills lists RAG; no bullet says RAG. Semantic search and Gemini embeddings are the analog, but a keyword scan for the JD's RAG line finds only the Skills row.

### Misreads

- A JazzHR keyword pass for "RAG" could file the Skills line as stuffing and miss that pgvector cosine search plus Gemini embeddings is the retrieve-then-generate system.

### Interview angles

- **Lead with:** Vylet LangGraph + LangSmith eval (20 adversarial cases, Pydantic consensus gates, 50%→90% faithfulness) as the analog to "build and test practical AI" and PoC→production; then SignalWeaver pgvector semantic search as the diligence-doc / unstructured-news retrieval story; then MDC irregular filings → Flask/EC2 as messy-data plumbing shipped to a real stakeholder.
- **Defend:** No pool bullet contains the word RAG — say so, then walk semantic search + embeddings as retrieve-then-generate over messy text. No buy-side or DiligenceVault-platform internship exists; map Lyndbrook regulatory aggregation and MDC campaign-finance research onto "how fund diligence / investment research actually work" without claiming allocator experience. Do not invent Snowflake, Databricks, Copilot, Fusion, Tableau, TensorFlow-as-used, or Kubernetes. *(out of rails: no pool bullet contains RAG; swap sets cannot insert it)*
- **Depth prep:** Walk the eval harness (archetype labels, faithfulness lift, when the consensus gate hard-fails); pgvector cosine vs a DDQ/document RAG; embedding freshness / re-scrape; how you would test an automation against a real diligence workflow.

## Likelihood

- **Resume screen:** High — LangGraph + LangSmith eval, Gemini embeddings, pgvector semantic search, messy-document ETL, Dockerized delivery, and a live GitHub clear the applied-AI intern bar on a resume-first C-tier JazzHR screen.
- **Overall hire odds:** Medium — C-tier ~15–25% with the resume as the bottleneck (`companies.md`); no published OA. The remaining RAG-keyword ding is small; the unpublished loop still eliminates if the candidate cannot walk a diligence-doc retrieve-then-generate story.
- **Funnel filters:** JazzHR `Yo3RhxiDyM` resume + essays (AI thing built, GitHub, 3 why-intern bullets, availability, stipend/hr) → unpublished intern loop. No published OA. No intern sys design. No class-year / GPA / visa knockout on this req.
- **Outside the resume:** Apply with a live GitHub and a concrete "something built with AI" essay mapped to document RAG/agents; a warm intro beats cold JazzHR (`recruiting.md` HM > recruiter > engineer > cold apply).
