# Backend Developer Intern at Eulerity

## Verdict

- **Score:** 5.0 / 10 (5 demerits — 0 emergency, 1 major, 2 minor)
- **Eligibility:** ineligible — JD requires based in the NYC tri-state area with weekly in-office; candidate location is Ann Arbor, MI. Class standing at internship is rising senior (Expected May 2028 vs May–August 2027); JD has no class-year gate, so that comparison would pass. 20 hrs/week is commitable. Java 1-year is a skill filter, not class-year math.
- **Track:** full-stack + marketing-automation / agentic-marketing (Java services + AI tooling)
- **Pipeline:** 2 cycle(s) · exit: writer_peak

## Screen Review

### First read

- Leads with a production Flask REST API on AWS EC2 (MDC), then a Dockerized LangGraph/LangSmith agentic pipeline with eval gates and a 79%→89% defect fix (Vylet) — backend spine plus agentic differentiator in the top half.
- SignalWeaver closes with FastAPI async REST, 5-node LangGraph, and Docker Compose + GitHub Actions CI; CaseStudyPrep shows third-party S3 API retry work.
- Binding ding: zero Java anywhere. This is a Java-backend intern req; the page reads as a strong Python services intern.

### Demerits

- **major** · `resume` · Java wholly absent — JD 1-year Java floor has no Skills or bullet evidence; Flask/FastAPI lead does not substitute
- **minor** · `Michigan Data Consulting (MDC)` · metric-free aggregation bullet — parse/normalize/rank PAC engine has no sized outcome next to the 800-hour ETL line
- **minor** · `resume` · weak marketing-automation domain signal — agentic/backend is loud; ads/listings/reputation adjacency is not

### Misreads

- A Java-shop screen will bucket this as the wrong stack in under 10 seconds and never reach the LangGraph eval story.
- MDC's aggregation line without a number can read as spreadsheet tooling rather than the production API spine.
- PE lead-gen + campaign-finance can be misread as fintech/data rather than the agentic-marketing OS Eulerity actually builds.

### Interview angles

- **Lead with:** MDC Flask REST API sole-ownership on AWS EC2; Vylet Dockerized LangGraph + Redis/Celery, LangSmith eval (50%→90% faithfulness), qualification-defect fix (79%→89%); SignalWeaver FastAPI p50/p99 and GitHub Actions CI
- **Defend:** Java 1-year gap *(out of rails: no Java/Spring/JUnit in the live pool; MatchStream commented-out and forbidden; Skills cannot list Java)* — be honest: backend work is Python/Flask/FastAPI; do not claim Spring. Martech domain *(out of rails: no ads/listings/campaign-ops bullets)* — frame Vylet lead-sourcing automation and MDC campaign-finance APIs as workflow-automation analogs, not fake martech. MDC aggregation metric *(out of rails: only one MDC pool bullet carries an impact number and it is already on the page)* — narrate what you would measure next (PAC rank freshness, endpoint latency)
- **Depth prep:** REST/API design and third-party API retries (S3 presigned URLs); LangGraph orchestration + LangSmith eval/consensus gates; Docker/CI delivery; do not bluff Java. Complete the Dev Matcher form the JD requires (pipeline did not fill it). Location: only proceed if you can actually be NYC-tri-state for weekly in-office

## Likelihood

- **Resume screen:** Low — shipped APIs and agentic pipelines are real; Java is the first filter and it is missing
- **Overall hire odds:** Low — Greenhouse/human-read funnel at a ~73-person Java backend shop, plus NYC tri-state knockout and a required Dev Matcher form; agentic depth would help a Java-matching candidate
- **Funnel filters:** Resume + Dev Matcher form · no named OA · 2–3 practical rounds likely · bottleneck: Java bar + NYC location + form completion · D-tier intern pay ($17–19/hr)
- **Outside the resume:** Fill https://dev-matcher-pro.lovable.app/ or the application is discarded; confirm NYC presence; a defendable Java service would move the needle more than another Python agent bullet
