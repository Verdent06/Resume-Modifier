# WORTH_IT

**Verdict: YES**

Resume/project fit against the live JD (Workday req 10158145, WDW CS/CE Interns Spring 2027) vs `context.md` pool — not eligibility (already computed eligible: Expected May 2028 + Jan 2027 arrival = junior; JD requires Junior, Senior, or Graduate by arrival; GPA 3.66 ≥ 3.0 preferred; CS major; U.S. citizen / unrestricted work authorization; 18+).

## Reasons

1. **Required OOP + named languages are real.** JD requires OOP; day-to-day may use C++, C#, Python, Node.js. Pool has C++ (Granular lock-free real-time), Python (MDC Flask REST, Vylet pipeline, SignalWeaver FastAPI), TypeScript. C# and Node.js are **not** in inventory — preferred/may-use, not a required exclusive. Do not invent them.

2. **Safety analytics / data-metrics work maps without a Disney internship.** JD: "data analysis, metrics, and projects that identify safety-related trends." Pool has Requests+Pandas ETL and PAC ranking metrics (MDC), entity databases and scoring (Lyndbrook), SQL/Postgres freshness and FastAPI serving (Vylet, SignalWeaver). That is ingest → aggregate → serve — the Safety Assurance / QE-shaped work on this multidisciplinary posting.

3. **Ride/show process-control and HW/SW interface analog exists, not as a robotics intern.** JD also lists software services for ride/show process-control systems and hardware/software interface resolution. Granular is C++ under a hard real-time constraint (zero-alloc `processBlock()`, lock-free SPSC UI-to-audio). CaseStudyPrep is live-system debug (27% S3 upload failure). That is systems discipline next to hardware, not ROS/controls/ride-vehicle fabrication. `screen_track` is full-stack; differentiator is parks ops / process-control / safety-analytics / HW-SW. MatchStream/FRC Java is commented out — do not invent it.

4. **Preferred platform stack is carryable.** Git, Docker, GitHub Actions CI, AWS (EC2, S3), SQL/PostgreSQL are all through use. Linux, TDD/BDD, PowerApps/PowerAutomate, and prior Disney College Program are preferred gaps — not worth-it knockouts.

5. **Production shipping exists.** Sole-engineer Flask REST on AWS EC2 to a nonprofit stakeholder, a live product with paying clients and a named defect fix (79%→89%), and a voice-AI co-op with fault-tolerant recovery. Persona-shaped bar is code other people used plus a deploy path.

6. **Calibration.** Disney Parks CS/CE is B-tier (`companies.md`) — resume is the bottleneck; posting opened 2026-08-24 and closes 2026-08-31 (`recruiting.md` intern funnel: apply in the first wave). Six-month onsite Lake Buena Vista is logistics, not a skill fail. Do not invent Java, Spring, Node, Snowflake, Databricks, Tableau, Copilot, Fusion, or Sentry.

**Stop: no. Continue full pipeline + `written-answers.md`.**
