# WORTH_IT

**Verdict: YES**

Resume/project fit against the live Greenhouse JD (`4396622009` / `R-401`) vs `context.md` pool — not eligibility (already computed **eligible**: Expected May 2028; Winter 2027 intern is January 2027 start / 12–16 weeks, junior-year term, returns after; CS is an allowed field; US citizen / no sponsorship).

## Reasons

1. **Data-engineering screen is a real match.** JD tests relational/object DB design, schema, query optimization, cloud data platforms, ETL/ELT pipelines, data cleanliness/ingestion, and dashboarding (QuickSight/Kibana **or custom visualization**). Pool has Python + SQL through use, Requests/Pandas ETL, Postgres, a production Flask REST API on AWS EC2 (MDC), injection-safe SQL freshness (Vylet), and a React/TypeScript dashboard persisted to Postgres (SignalWeaver). That is ingest → transform → store → serve / report — the job, not a generic SWE screen and not ML-research.

2. **Fair analogs exist without inventing Aurora/ES/QuickSight.** Campaign-finance ETL (messy filings → PAC rankings), Lyndbrook entity resolution on EPA/MassGIS, Vylet Dockerized pipelines with a SQL freshness layer, and SignalWeaver’s custom dashboard map to stores + pipelines + reporting. Do not claim AuroraDB, Elasticsearch, Kibana, or QuickSight.

3. **Quality / schema analog is carryable.** Vylet’s stale-timestamp SQL checks, the name-collision defect (79% → 89%), and Lyndbrook’s PWSID entity database are data-quality and schema work. Catalog/lineage and IaC tools are not in inventory — do not invent Terraform or Elasticsearch.

4. **Named gaps stay honest.** No Snowflake, Databricks, Tableau, Copilot, Fusion, Sentry, AuroraDB, Elasticsearch, QuickSight, Kibana, or ROS in the pool. Those are familiarity/pluses or company-stack names, not exclusive gates on this intern JD. Custom visualization is explicitly allowed. Skip ML-research framing (no LoRA lead).

5. **Calibration.** Kodiak is B-tier dual-use AV (`companies.md` row this packet). Intern bottleneck is the resume (Greenhouse + Endorsed AEDT; no published DE OA). Req first_published 2026-09-04 (`recruiting.md` §8 first wave). Mountain View onsite with intern-sourced housing is a self-relocate logistics yes, not a skill fail. $10,000/month is paid. Autonomy interest is a preference — absence of FRC/ROS is honest, not a knockout on this DE req.

**Stop: no. Continue full pipeline + `written-answers.md` + `company-notes.md`.**
